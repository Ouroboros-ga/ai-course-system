# 编辑器/IDE
.idea/
.vscode/
.vs/
.obsidian/
.trae/
*.swp
*.swo
*~

# 系统文件
.DS_Store
Thumbs.db

# 前端通用
node_modules/
dist/
build/
# The course-build route is application source, not a generated build folder.
!frontend/src/app/pages/course/build/
!frontend/src/app/pages/course/build/**
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
backend/.env
.env.local
.env.development.local

# Java 后端
target/
*.class
*.jar
*.war
*.ear
.gradle/
build/
*/nbproject/
*/.classpath
*/.project
*/.settings/
# Keep application source under the route named build/.
!frontend/src/app/pages/course/build/
!frontend/src/app/pages/course/build/**

# Python 后端
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# setuptools/uv 可编辑安装的构建产物（backend/、nexus/ 各自会生成一份）
*.egg-info/
env/
venv/
ENV/
backend/.env
.venv

# 数据库文件（重要：不要提交到版本控制）

database/*.sqlite
database/*.sqlite3

*.sqlite
*.sqlite3

# 环境变量和敏感配置
backend/.env
.env
.env.local
.env.development
.env.development.local
.env.production.local
*.env.*.local
frontend/src/.env

# Codex / local agent runtime
.agents/
.workbuddy/
.claude/

# Local databases
database/*.db
database/*.sqlite
database/*.sqlite3
backend/database/*.db
backend/database/*.sqlite
backend/database/*.sqlite3

# Runtime/generated files
logs/
temp/
tmp/
uploads/
videos/
audios/
generated/

# Local Codex and test scratch directories
.codex_tmp/
.pytest_tmp/
.pytest-*/
backend/.tmp/
backend/.pytest_tmp_*/

# P1 shadow trace stores (G3 shadow / G5A canary runtime artifacts)
p1_shadow_artifacts/
p1_shadow_evidence/
p1_shadow_learning_events/
p1_shadow_memory_candidates/
p1_shadow_safety_dryrun/
p1_shadow_graph/
p1_shadow_course_evidence/
backend/p1_shadow_course_evidence/

# Local Judge0 source checkout used only for container deployment.
deploy/judge0-source/

# 阶段8 本地对象存储目录（仅开发环境运行时数据，不入 Git）
backend/media/
/media/

# Local presentation inspection artifacts
/presentation.pptx
/presentation_modified.pptx
/presentation_modified.pptx.inspect.ndjson

# Local courseware-derived parsing audits must not be exported with source code.
/docs/phase1/parse-verification/

# Python virtual environments (named variants like .venv-paddlenlp)
.venv-*/

# PaddleNLP downloaded model weights and taskflow cache
.paddlenlp/

# Local human gold source materials (authorization/privacy controlled)
/research/product1_graph_retrieval/human_gold/

# Local Dense experiment runtime and downloaded model weights
/research/product1_graph_retrieval/runtime/

# Independent local Shadow-1 demo logs (not ORM/production records)
/research/product1_graph_retrieval/demo_runs/

# Trained model binary weights (large, regenerable via scripts)
# Metadata files (JSON, MD, manifests) inside artifacts/ are explicitly kept.
/research/**/artifacts/**/*.pdparams
/research/**/artifacts/**/*.safetensors
/research/**/artifacts/**/*.bin
/research/**/artifacts/**/*.onnx
/research/**/artifacts/**/*.pth
/research/**/artifacts/**/*.ckpt
/research/**/artifacts/**/cache/

# 依赖缓存 / 本地 QA 产物（不入库）
.pnpm-store/
backend/.qa/

# Local SQLite database backup files (never commit)
database/*.db.*

.corpus_cache/
