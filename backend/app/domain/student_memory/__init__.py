"""
P1-06 Student memory and privacy-control domain.

Student memory is course-scoped, evidence-backed, auditable, and deletable.
Every memory entry retains references to P1-07 LearningEvidence and a
generation reason. Disabled memories are neither read nor written.
Cross-course access is denied by default.

Version: 1.0 (draft, to be frozen at G1)
"""
