# 05 作品代码（代码与模型清单）

> 赛题要求：《05—作品代码》作品代码、模型文件或模型文件 ServiceID、技术报告等材料，确保可复现。
> 说明：本目录不直接存放全部源码（源码在仓库本身）；本清单给出可复现入口与模型/ServiceID 填写位。

## 一、代码清单（可复现入口）

| 组件 | 位置 | 说明 |
|---|---|---|
| 后端 | `backend/` | FastAPI 应用；`uv sync` + `uv run python -m uvicorn app.main:app --port 8000` |
| 数据库迁移 | `backend/alembic/` | `uv run alembic upgrade head`（基线 PostgreSQL 16 + pgvector / 本地 SQLite） |
| 前端 | `frontend/` | Vue 3；`npm install && npm run dev`（shadow 前端 `/app/**`） |
| 部署 | `deploy/` | Docker Compose、systemd、nginx、Judge0、PaddleOCR |
| 学科知识库 | `knowledge_data/` | 90 节点/82 关系（9 门课）+ `validate.py` 校验 |
| 微调管线 | `backend/finetune/` | 数据集生成（164+18）/ 评测基准 10 用例 / 批量评测 / 训练脚本 |
| 测试 | `backend/tests/` | 全量回归 2665 passed（2026-08-17 基线）；XH-202620 新增单测见下 |

## 二、XH-202620 新增代码（本轮改造）

| 能力 | 文件 | 验证 |
|---|---|---|
| 讯飞星火 LLM Provider | `backend/app/common/llm_client.py`（SparkClient）、`backend/app/core/config.py` | `tests/test_llm_spark_provider.py` 6 passed |
| 学科知识库检索服务 | `backend/app/platform/knowledge/discipline_kb.py` + `backend/app/api/v1/endpoints/discipline_knowledge.py` | `tests/test_discipline_knowledge.py` 13 passed |
| 微调管线 | `backend/finetune/`（4 脚本 + 10 用例基准 + README） | prepare_dataset/evaluate 已实测，训练待 GPU |

## 三、模型文件 / ServiceID（待填写）

- [x] 基座 LLM：**DeepSeek** `deepseek-chat`（`LLM_PROVIDER=deepseek`，OpenAI 兼容
      `https://api.deepseek.com/v1`；2026-08-20 接入并完成 10 用例实测 9/9）；密钥不提交（.env 配置）
- [ ] 微调模型：`（待填写）`——训练执行后提交 LoRA 权重/合并模型下载地址，或云端微调 ServiceID；
      **未训练前不填写**（诚实口径：管线可复现）
- [ ] 嵌入模型：本地 BGE（`platform/knowledge/`），无外部依赖

## 四、技术报告

- 架构与能力现状：[README.md](../../README.md)、[docs/DOCUMENTATION_INDEX.md](../../docs/DOCUMENTATION_INDEX.md)
- XH-202620 差距分析与产品定位：[docs/phase1/2026-08-20_XH202620差距分析与产品定位.md](../../docs/phase1/2026-08-20_XH202620差距分析与产品定位.md)
- 典型问题测试案例：[docs/phase1/XH202620_典型问题测试案例集.md](../../docs/phase1/XH202620_典型问题测试案例集.md)

## 复核红线

- 打包前用 `git status` 确认无 `.env`、密钥文件、数据库快照入库；
- 压缩包内不含真实 Key（一律以 `.env.example` 模板 + 占位符提交）。
