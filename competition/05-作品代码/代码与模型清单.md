# 05 作品代码（代码与模型清单）

> 赛题要求：《05—作品代码》作品代码、模型文件或模型文件 ServiceID、技术报告等材料，确保可复现。
> 说明：本目录不直接存放全部源码（源码在仓库本身）；本清单给出可复现入口与模型填写位。
> **赛题允许"模型文件或 ServiceID 二选一"：本提交已提供完整微调模型文件（LoRA adapter），
> 故不需要星火 MaaS ServiceID。**

## 一、代码清单（可复现入口）

| 组件 | 位置 | 说明 |
|---|---|---|
| 后端 | `backend/` | FastAPI 应用；`uv sync` + `uv run python -m uvicorn app.main:app --port 8000` |
| 数据库迁移 | `backend/alembic/` | `uv run alembic upgrade head`（基线 PostgreSQL 16 + pgvector / 本地 SQLite） |
| 前端 | `frontend/` | Vue 3；`npm install && npm run dev`（shadow 前端 `/app/**`） |
| 部署 | `deploy/` | Docker Compose、systemd、nginx、Judge0、PaddleOCR |
| 学科知识库 | `knowledge_data/` | 112 节点/106 关系（11 门课）+ `validate.py` 校验 |
| 微调管线 | `backend/finetune/` | 数据集 v2.2 生成（**2212 训练/50 评测**，基准 10 问只进 eval 集）/ 评测基准 10 用例 / 批量评测 / 完整 LoRA 训练脚本（已实测） |
| 测试 | `backend/tests/` | 全量回归 2665 passed（2026-08-17 基线）；XH-202620 新增单测见下 |

## 二、XH-202620 新增代码（本轮改造）

| 能力 | 文件 | 验证 |
|---|---|---|
| 讯飞星火 LLM Provider | `backend/app/common/llm_client.py`（SparkClient）、`backend/app/core/config.py` | `tests/test_llm_spark_provider.py` 6 passed |
| 学科知识库检索服务 | `backend/app/platform/knowledge/discipline_kb.py` + `backend/app/api/v1/endpoints/discipline_knowledge.py` | `tests/test_discipline_knowledge.py` 13 passed |
| 微调管线 | `backend/finetune/`（4 脚本 + 数据卡 + 评测基准 + 训练脚本） | prepare/evaluate 实测；2026-09-09 云端 4090 完成 7B LoRA 3 epochs 实测（见三） |

## 三、模型文件（已提供，ServiceID 二选一无需提供）

- [x] 基座 LLM：**DeepSeek** `deepseek-chat`（`LLM_PROVIDER=deepseek`，OpenAI 兼容
      `https://api.deepseek.com/v1`；2026-08-20 接入并完成 10 用例实测 9/9）；密钥不提交（.env 配置）
- [x] 微调模型 ①（**星火 X2.5-4B LoRA，2026-09-10 实测产出，主交付**）：基座 `XHToken/Spark-X2.5-4B`
      （Apache-2.0）；数据 v3 **3127 训练 / 231 评测**（来源 dev-liu `d1c2be0`）；3 epochs / 588 步 /
      26 分钟（4090），train_loss 0.2032；**同分布 231 条 eval_loss：基座 2.39647 → 微调 0.05497**
      （ppl 10.98 → 1.057）；基准 10 问自动 4/8 vs 4/8（**如实，不宣称提升**）。
      adapter：`competition/05-作品代码/finetune_提交包/model_files/adapter_spark_x25_4b_final.zip`
      （SHA256 `a5333425d9b42b5837628cc6d3819de77a821007a2b54ee57c5c16265fcafcf1`）
- [x] 微调模型 ②（**Qwen2.5-7B LoRA，2026-09-09 实测产出，对照交付**）：数据 v2.2 2212 条，
      train_loss 0.667–0.669；自洽提交包见 `competition/05-作品代码/finetune_提交包/`；
      adapter zip SHA256 `548974c198ec81f54a6ac7caa7d358531eab8aa12d4697841aa6dfec7d1cdb50`；
      训练/评测/数据详情见包内 evidence 与 `docs/phase1/2026-09-09_XH202620微调交付记录.md`
- [x] 微调模型 ServiceID（云端微调）：**无需提供**（赛题允许"模型文件或 ServiceID 二选一"，已选模型文件）
- [ ] 嵌入模型：本地 BGE（`platform/knowledge/`），无外部依赖

## 四、技术报告

- 架构与能力现状：[README.md](../../README.md)、[docs/DOCUMENTATION_INDEX.md](../../docs/DOCUMENTATION_INDEX.md)
- XH-202620 差距分析与产品定位：[docs/phase1/2026-08-20_XH202620差距分析与产品定位.md](../../docs/phase1/2026-08-20_XH202620差距分析与产品定位.md)
- 典型问题测试案例：[docs/phase1/XH202620_典型问题测试案例集.md](../../docs/phase1/XH202620_典型问题测试案例集.md)
- 微调交付记录（2026-09-09）：[docs/phase1/2026-09-09_XH202620微调交付记录.md](../../docs/phase1/2026-09-09_XH202620微调交付记录.md)

## 复核红线

- 打包前用 `git status` 确认无 `.env`、密钥文件、数据库快照入库；
- 压缩包内不含真实 Key（一律以 `.env.example` 模板 + 占位符提交）。
