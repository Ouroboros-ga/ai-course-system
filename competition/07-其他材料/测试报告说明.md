# 07 其他材料（测试报告 / 测试脚本 / 规模化数据）

> 赛题要求：《07—其他材料》测试脚本、测试报告、大规模用户使用数据报告等辅助材料（可选）。

## 一、测试报告（已有可复用）

| 项 | 数据 | 证据来源 |
|---|---|---|
| 全量回归基线 | 2749 passed / 6 failed / 22 skipped（2026-08-20 R10 全量复跑）；6 个失败均为**可选环境依赖缺失**（`graphrag` 模块未安装、本地 BGE 模型文件未下发），集中于 GraphRAG/R2 shadow 用例，与本次改造文件零相关（能力默认关闭） | `docs/phase1/功能现状审计表.md` + R10 全量复跑记录 |
| 历史回归基线 | 2665 passed / 1 failed / 22 skipped（2026-08-17 记录，环境完整时） | `docs/phase1/功能现状审计表.md` |
| XH-202620 新增后端单测 | 星火 Provider 6 + 学科知识库 20 + 助研写作 7 + 趋势分析 8 + 学情诊断 6 + 相关回归 | `backend/tests/test_llm_spark_provider.py`、`test_discipline_knowledge.py`、`test_research_writing_assist.py`、`test_research_trends.py`、`test_course_diagnosis.py` |
| XH-202620 新增前端测试 | API 契约 75（含学科知识库 +2）、前端单测 57 | `frontend/src/api/__tests__/apiContracts.test.cjs`、`npm run test:unit` |
| 学科知识库数据校验 | 112 节点 / 106 关系（11 门课），schema 与引用完整性通过 | `knowledge_data/validate.py` |
| 微调管线验证 | 指令集生成 197+31（自动发现 11 门课节点文件；基准 10 问只进 eval 集）、评测基准 10 用例、评测 fail-closed、训练缺依赖/缺 CUDA 报错退出 | `backend/finetune/` 脚本实测记录 |

## 二、测试脚本清单

- 后端：`backend/tests/`（pytest，`cd backend && uv run pytest -q`）
- 学科知识校验：`python knowledge_data/validate.py`
- 前端：`frontend/`（`npm run test:unit` / `npm run build` / `npm run smoke:app`）
- 微调评测：`python backend/finetune/evaluate.py --base-url ... --api-key ... --model ...`
  （需自有端点 Key，fail-closed）

## 三、规模化使用数据（加分项，待真实采集后填写）

- [ ] 用户规模：________（≥50 人方获加分）
- [ ] 使用周期：________
- [ ] 关键效果指标：________
- [ ] 数据来源与采集方式说明：________

## 四、合规提示

- 测试与规模化数据若涉及真实用户，须脱敏/假名化并在材料 02 声明范围内；
- 规模化数据报告须能说明采集方式，不得虚构。
