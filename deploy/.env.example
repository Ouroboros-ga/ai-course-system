# 主栈 docker-compose.yml 环境变量示例
# 复制本文件为 .env 并填入真实值后再执行 docker compose up。
# 切勿将含真实密码的 .env 提交到版本控制。

# PostgreSQL 密码（同时用于 backend 的 AI_COURSE_DATABASE_URL）
POSTGRES_PASSWORD=

# Neo4j 密码（用户名固定为 neo4j）
NEO4J_PASSWORD=
