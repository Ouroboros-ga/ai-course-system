# 独立 PaddleOCR 服务依赖锁（固定版本，避免漂移）。
# 主后端不安装这些；仅本服务容器安装。
# CPU 推理：paddlepaddle（非 paddlepaddle-gpu）。

# PaddlePaddle CPU 运行时（Python 3.11）
paddlepaddle==2.6.2

# PaddleOCR（检测+识别+方向分类，CPU 可用）
paddleocr==2.7.3

# 服务框架
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4

# 图片处理（PDF 渲染 + 图片解码）
Pillow==11.0.0
pdf2image==1.17.0
numpy==1.26.4
shapely==2.0.6
pyclipper==1.3.0.post5
lmdb==1.4.1
