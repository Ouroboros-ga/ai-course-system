# 前端废弃界面/组件审计（2026-08-20）

> **背景**：项目历经多轮前端重构（legacy 路由 + shadow `/app/**` 双前端），存在较多残余界面。
> **方法**：扫描 `frontend/src` 全部 307 个源码文件（.vue/.js/.mjs，排除测试），以文件主名 +
> kebab/snake 变体统计跨文件引用；对候选再做精确 grep 核验（frontend 全部 + backend 误报排除）；
> 已确认无 `import.meta.glob` 自动导入盲区；最后以 **Vite 全量构建 + 57 单测 + 75 契约测试**验证。
> **结果**：34 个零引用候选 → **删除 30 个**，保留 4 个（有测试/入口消费者）。

## 一、已删除（30 个，提交见 git）

| 分组 | 文件 | 判定 |
|---|---|---|
| 旧聊天组件 | `components/chat/`（DraggableAvatar / DesktopLayout / MobileLayout / ChatPanel / PptPlayer / SplitVideoPlayer / HistorySidebar / ChatTopNav） | legacy 聊天界面残余，无任何页面引用 |
| 旧首页组件 | `components/home/ui/`（BackTop / ScrollArrow） | 新首页未使用 |
| 旧个人中心 | `components/profile/LoginIn/`（MappingEditor / PPTGenerationDialog / MenuGrid / UserCard） | 旧登录态组件树残余 |
| 旧学习组件 | `components/student/`（ChatLearningArea / CourseSelection / CourseStructure） | 旧学生学习界面 |
| 死组合式函数 | `composables/`（useCognitiveTracking / useD3 / useWebSocket） | 仅文件头自引用，零消费 |
| 未接线页面 | `features/safety-policy/SafetyPolicyEditor.vue`、`features/student-memory/StudentMemoryManager.vue`、`features/teacher-workspace/components/CourseTaskPanel.vue` | 后端有 API 但前端未接线 |
| 学习工作台旧组件 | `features/student-learning/components/`（CourseOutlineRail / LearningAssistantPanel / LearningMediaStage / LearningNotesPanel / LearningWorkspaceHeader / WorkspaceDrawer） | 被新实现取代 |
| shadow 残余 | `app/ui/SfxPlannedPanel.vue` | 未使用 UI 面板 |
| 死 API 客户端 | `api/retrieval_demo.js`、`api/script_editor.js` | 无前端调用方 |

## 二、保留（4 个，有消费者）

| 文件 | 保留理由 |
|---|---|
| `app/components/course/TeacherProposalPanel.vue` | 被契约测试 `api/__tests__/courseBuildContracts.test.cjs` 读取 |
| `config/featureFlags.js` | 有专属测试 `config/__tests__/featureFlags.test.js` |
| `features/evidence-viewer/dev/DevPage.vue` | 独立 dev 沙盒页（`dev/index.html` 挂载） |
| `scripts/smoke-shadow-app.mjs` | package.json `smoke:app` 脚本入口 |

## 三、未删除但需知悉

- `components/home/sections/Feature.vue`：主名"Feature"与 featureFlags/features 子串重合导致无法用
  文本扫描判定；经精确 grep 无真实引用，但保守保留（可后续人工确认后移除）。
- `frontend/docs/`（REFACTOR-PLAN / REDESIGN / component-spec 等）仍描述已删组件树：属历史规划文档
  （仅参考，不作为现状依据），未修改。

## 四、验证证据

- `npm run build` ✅（9s，无缺失模块）
- `npm run test:unit` ✅ 57 passed / 0 failed
- 契约测试 `apiContracts.test.cjs` ✅ 75 passed / 0 failed

## 附：审计脚本

临时脚本 `backend/.pytest_tmp/frontend_audit.py`（方法：stem + kebab 变体跨文件计数）已删除；
需要复现时按"收集源码 → 变体计数 → 精确 grep → 构建验证"流程执行。
