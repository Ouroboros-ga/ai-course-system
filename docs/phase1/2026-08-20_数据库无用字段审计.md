# 数据库无用字段审计（2026-08-20）

> **审计方法**：提取 `backend/app/models/*.py` 全部 SQLModel 字段（2592 个）→ 统计各字段名在
> `backend/app/`（业务代码，扣除定义文件自身 1 次）的出现次数 → 零业务引用的 35 个字段，再交叉
> 核对 `backend/tests`、`backend/alembic`、`frontend/src`。
> **结论**：35 个字段为"建表后从未被业务逻辑消费"的死字段；**本轮仅审计留档，不执行删列**
> （删除需新 Alembic 迁移并在生产库 120.26.104.247 执行，属部署窗口事项，需另行授权）。

## 一、A 类：零引用死字段（30 个，建议随部署窗口移除）

无任何 app/tests/frontend 引用，仅存在于 0001 基线建表。

| 模型 | 字段 | 说明推断 |
|---|---|---|
| `StudentEnrollment` | `is_column_header` / `is_row_header` / `is_row_section` / `num_cols` / `num_rows` / `table_data` | 旧表格组件配置残留 |
| `PlaybackCapabilityProfile` | `max_initial_load_ms` / `measured_avg_fps` / `measured_drop_rate` / `measured_init_ms` / `measurement_notes` / `min_browser_version` / `min_device_memory_gb` / `profile_metadata` / `requires_hw_accel` / `target_fps` / `target_resolution` | 播放能力画像字段，从未写入/读取 |
| `QAContext` | `embedding_vector` / `response_time_ms` / `tokens_used` / `understanding_reason` | 旧问答链路元数据 |
| `AgentLLMDiagnosticRecord` | `diagnostic_id` | 诊断记录冗余标识 |
| `CourseAvatarBinding` | `job_metadata` | 头像绑定任务元数据 |
| `CourseReleaseArtifact` | `checkpoint_id` | 发布制品检查点 |
| `GraphNodeReview` | `canonical_node_id` | 图节点审核冗余引用 |
| `LearningProjectionOutbox` | `embedding_revision` | 投影 outbox 嵌入版本 |
| `KnowledgeSearchHistory` | `clicked_points` | 检索历史点击点 |
| `LearningEvidenceLink` | `link_id` | 证据链接标识 |
| `LearningJumpHistory` | `correct_answer_rate` | 跳转历史正确率 |
| `LabRecord` | `acl_id` | 实验室记录 ACL 引用 |

## 二、B 类：有测试/迁移引用（5 个，暂不删除）

| 模型 | 字段 | 引用来源 |
|---|---|---|
| `AgentLogMigrationRecord` | `redacted_event_rows` / `redacted_trace_rows` | 测试 + 迁移记录（2 处） |
| `StudentEnrollment` | `avg_understanding_level` | 0032 数据修复迁移写入；测试 1 处 |
| `StudentEnrollment` | `parent_ref` | 测试 1 处 |
| `CourseAvatarBinding` | `teacher_authorization_confirmed_at` | 测试 2 处 |

## 三、建议

1. **A 类 30 字段**：移除成本低（模型删字段 + 新迁移 `drop_column`），但必须与生产库迁移
   一起发布（否则模型查询会因列缺失报错）。建议纳入下次部署窗口，随 Alembic 迁移执行。
2. **B 类 5 字段**：涉及迁移逻辑/测试，如需移除需同步改造 0032 迁移逻辑与测试，风险较高，建议保留。
3. 已废弃保留字段（`NodeProgress.understanding_score`、`UnderstandingAnalysis`、
   `StudentEnrollment.avg_understanding_score` 等）属六维认知冻结契约的审计保留项，**不在**本次
   清理范围（删除会破坏历史可审计性）。

## 附：审计脚本（已删除，可复现）

临时脚本 `backend/.pytest_tmp/field_audit.py` 已在审计完成后删除；方法同上，需要时按
"提取字段 → app/ 计数 → 交叉核对 tests/alembic/frontend" 复现。
