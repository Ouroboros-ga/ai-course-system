# XH-202620 微调交付记录(2026-09-09,7B LoRA · 数据 v2.2)

> 状态:已交付(adapter 模型文件 + 训练/评测记录);ServiceID 待星火 MaaS。
> 关联:材料 05(`competition/05-作品代码/代码与模型清单.md`)、数据卡
> (`backend/finetune/data/DATACARD.md`)、交付对照表(`backend/finetune/export/微调交付_模型与对照表.md`)。

## 1. 做了什么

在云 GPU RTX 4090 24GB(Linux)上对 **Qwen/Qwen2.5-7B-Instruct** 执行 PEFT LoRA SFT,
训练数据为 XH 分支 v2.2 指令集 **2212 条**(13 类任务,评测 50 条防污染)。

- 配置:r=16 / alpha=32 / dropout=0.05;7 类投影层;AdamW lr=2e-4;
  bs=1 × grad-accum 16(有效 16);max_len 1024;bf16;3 epochs = 417 步;
  seed 2026(与 `prepare_dataset_v2.py` 同源可复现)。
- 实测:单次训练 ≈15 分钟(909s,7.3 samples/s);**train_loss 0.6675/0.6690(两次独立运行一致)**;
  可训练参数 40.37M(占全量 0.53%)。
- 逐步 loss 曲线(417 点):epoch1 mean 1.028 → epoch2 0.585 → epoch3 0.394,末点 0.310,
  CSV:`backend/finetune/export/loss_curve_qwen25_7b_v22.csv`。

## 2. 交付物清单

| 物 | 位置 | 说明 |
|---|---|---|
| adapter 交付 zip(不入库,本地/网盘留存) | `backend/finetune/export/adapter_qwen25_7b_v22_final.zip` | 11 文件 146.4MB;SHA256 `548974c1…cdb50` |
| adapter 原始目录(不入库) | `backend/finetune/lora_output_qwen25_7b_v22/` | adapter_model.safetensors(161.5MB,392 张量)+ config + tokenizer + model_card |
| 交付对照表(A–G 全表) | `backend/finetune/export/微调交付_模型与对照表.md` | 清单/hash、训练记录、loss 曲线、数据速查、评测、材料05 对照、费用 |
| 基座 vs 微调评测明细 | `backend/finetune/export/eval_基座vs微调_结果与样例.txt` | 逐条判定 + 6 组样例输出 |
| loss 曲线 CSV | `backend/finetune/export/loss_curve_qwen25_7b_v22.csv` | 417 点 |
| model card | `backend/finetune/export/model_card_qwen25_7b_v22.md` | 用途/边界/复现 |
| SHA256 清单 | `backend/finetune/export/SHA256SUMS_微调交付.txt` | zip 与 11 文件逐项 |
| 云 GPU 一键包(上传用) | `backend/finetune/export/cloud_gpu_pack/` | 脚本+数据+README+AutoDL/Matpool 操作清单 |
| **自洽提交包(评审用)** | `competition/05-作品代码/finetune_提交包/` | README_提交清单 + model_files(zip/hash/model_card) + evidence(对照表/曲线/评测/记录) + code(复现) + maas(ServiceID 用) |
| MaaS 提交物(ServiceID 用) | `backend/finetune/export/spark_train_*.jsonl` + `manifest.json` | 2212 条 messages/Alpaca、评测 50、防污染 10 |

## 3. 评测结果(诚实口径)

基准 10 问(仅进评测集,防污染)云端 4090 实测,同一解码设置(do_sample=False,max_new 384):

- **自动 contains 8 题:基座 4/8 vs 微调 3/8** —— 该口径下微调**未提升**,
  **材料中不得宣称"评测提升"**;
- C2(contains_grouped)与 C3(judge0_manual)未计入(需沙箱/分组判定);
- 50 条指令评测集待补(需全量自动+沙箱人工,遵循"证据不进训练集");
- 定性观察(非通过率证据):微调输出更贴近课程语料风格(结构化要点+教材出处、篇幅收敛)。

## 4. 成本与实测坑(记录供后续)

- 云 4090:基座下载 15GB + 训练 3 epochs ≈15 分钟;实例占用 ~1.2h,费用 ≈2–4 元。
- 本地笔记本 RTX 5070 Laptop 8GB/WDDM:3B 实测 ~2s/样本,**7B 3 epochs 预计 6–9h 且逐步降频** → 不建议本地全量。
- 版本组合约束:transformers 4.57.6 + peft 0.20 下 **梯度检查点不可用**(backward 报 requires_grad),
  且 7B bf16 **batch≥2 在长样本批会 OOM** → 采用 bs1/accum16 稳定跑通(已固化于 cloud_gpu_pack)。

## 5. 待办

- [ ] 星火 MaaS 上传 `spark_train_messages.jsonl` 训练 → ServiceID → 回填材料 05;
- [ ] (可选)50 条评测集全量评测 + judge0_manual 用例人工;若需提升,先扩同源数据或叠加 RAG 再评。

## 6. 数据集统合与 Spark-X2.5 基座准备(2026-09-09 增补)

### 6.1 训练集统合(已交付)

- 统合目录:`competition/05-作品代码/finetune_提交包/dataset/`
  (`sft_train_v2.2_messages.jsonl` 2212 / `sft_train_v2.2_alpaca.jsonl` 2212 /
  `sft_eval_v2.2.jsonl` 50 / `benchmark10_only.jsonl` 10 / `DATASET_MANIFEST.md`)
- 同源事实:训练集与 MaaS 提交物 `spark_train_messages.jsonl` **SHA256 一致**;
  画像 2212 条 / 4728 消息 / 587,119 字符 / 单条 ≤793 字符 / 中文 1343 + 英文 869。
- 新数据集接入管线:`backend/finetune/intake_dataset.py`(自测通过)——输入 ChatML 或
  Alpaca JSONL,自动完成格式归一、画像统计、**基准 10 问防污染检查**、生成双格式、
  更新统合目录与 manifest;防污染命中时以退出码 2 阻断。

### 6.2 新基座选型评估(星火 X2.5 / Qwen 新版)

- **星火 X2.5-1.7B / X2.5-4B**(`XHToken/Spark-X2.5-*`,Apache-2.0,自研 `spark2_5` 架构,
  3 滑窗 + 1 全注意力混合,原生 1M 上下文):权重 ModelScope 亦可取(1.7B ~3.4GB / 4B ~8.6GB);
  **LoRA 目标层为 `q_k_v_proj/out_proj/gate_proj/up_proj/down_proj/g_proj`**(与 Qwen 不同)。
- 版本兼容性实测:**transformers 4.57.6 可用**(1.7B 加载成功,1707.7M 参数,chat_template 正常);
  **5.16.0 不可用**(`_tied_weights_keys` list/dict)、**5.17.0 不可用**(RoPE 校验 `rope_parameters` 冲突)。
  → 结论:直接复用已验证的 4.57.6 + peft 0.20 栈,无需 LLaMA-Factory。
- Qwen 新版评估:Qwen3.8-Max 2.4T(API)/ Qwen3.8-Flash 125B MoE(开源,单卡不可训);
  Qwen3.5 系列 0.8B–397B 中,**≤9B(0.8B/2B/4B/9B)单卡可训**、27B 以上需 QLoRA 或多卡;
  单卡对比上 5090(32GB)优于 4090(24GB,单价高但工时短、单任务成本接近)。
- 训练包(自洽):`backend/finetune/export/spark_x25_pack/`(脚本 + 双格式数据 + LLaMA-Factory 配置 +
  评测脚本 + README;含上述实测结论)。

### 6.3 当前状态

- **等待用户提供新训练集**;到货后执行:
  `python backend/finetune/intake_dataset.py --input <新集> --tag <标签>` →
  `python backend/finetune/train_lora.py --base-model <星火/Qwen 基座> --data-file ... --target-modules ...` →
  评测 → 拉回 adapter → 更新提交包并提交推送。

## 7. 星火 X2.5-4B 训练交付(2026-09-10 完成,新数据集 v3)

### 7.1 数据(来源 `origin/dev-liu` 提交 `d1c2be0`)

- 训练 **3127 条 / 评测 231 条**(ChatML;6948 消息、731,221 字符、单条上限 708;多轮 296 条;
  中文 3113 / 英文 14);自有 v2 生成 + 外部课程子集合并(剔除 trivia/RFC/非课程轶事约 900 条),
  **基准 10 问零泄漏**(接入时自动复检 0 命中)。
- 统合:以标签 `v3` 接入 → `dataset/v3_train_messages.jsonl`、`v3_train_alpaca.jsonl`、
  `v3_eval.jsonl`,并追加 `DATASET_MANIFEST.md` 记录。

### 7.2 训练(云 4090 24GB,`--gpu-profile 4090`)

| 项 | 值 |
|---|---|
| 基座 | `XHToken/Spark-X2.5-4B`(Apache-2.0,4.11B dense,混合注意力 3×SWA(512)+1×full,原生 1M 上下文) |
| LoRA | r16/α32/drop0.05;targets `q_k_v_proj,g_proj,out_proj,gate_proj,up_proj,down_proj`;可训练 **32.45M(0.78%)** |
| 超参 | bs2×accum8(有效 16),cutoff 2048,lr 2e-4 cosine+3% warmup,bf16,梯度检查点,seed 2026 |
| 结果 | 3 epochs / 588 步 / **26.0 分钟**(1562.6s);**train_loss 0.2032**;末步 loss 0.0014 |
| 训练中 eval_loss(231) | epoch1 0.1053 → **epoch2 0.0987(最优)** → epoch3 0.1068(轻微回升) |
| 版本约束 | transformers **4.57.6** 可用;4.57.1+ 必需;实测 **5.16.0 / 5.17.0 不可用**(tied-weights / RoPE 校验变更) |

### 7.3 评测(全部真实执行,诚实口径)

- **同分布 231 条 token 级对照:基座 eval_loss 2.39647(ppl 10.98)→ 微调 0.05497(ppl 1.057)**
  ——领域拟合显著改善;该集与训练同源同风格,不代表开放域泛化。
- **基准 10 问(防污染)自动 contains 8 题:基座 4/8 vs 微调 4/8**(C6 基座过/微调未过,C9 相反)
  ⇒ 严格 markers 口径**无净提升,材料不得宣称"评测提升"**;C2/C3 需分组判定与沙箱。
- 逐条判定 + 6 组样例:`competition/05-作品代码/finetune_提交包/evidence/eval_spark_x25_4b.txt`。

### 7.4 交付物

| 物 | 位置 |
|---|---|
| adapter zip(95.2MB,SHA256 `a5333425…cfcf1`) | `competition/05-作品代码/finetune_提交包/model_spark_x25_4b/adapter_spark_x25_4b_final.zip` |
| model card | `model_spark_x25_4b/model_card_spark_x25_4b.md` |
| 评测与曲线(含 **588 点逐步 loss 曲线**) | `model_spark_x25_4b/evidence/spark_x25_4b_评测与曲线.md`、`spark4b_loss_curve.csv`、`spark4b_loss_curve_eval.csv` |
| 云端运行包(脚本+数据+评测) | `backend/finetune/export/spark_x25_cloudrun/` |
| 1.7B | 按用户决定**不训练**;权重下载已中止并清理 |
