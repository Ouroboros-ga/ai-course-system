# M7 人工冒烟记录

## 1. 记录信息

| 项目 | 结果 |
| --- | --- |
| 日期 | 2026-07-10 |
| 分支 | `refactor/codemind-v3` |
| 测试数据库 | `.pytest_tmp` 下隔离 SQLite，不是生产库 |
| 外部网络 | 自动化测试已阻断 |
| 真实 LLM/PPT/TTS | `NOT_RUN` |
| 真实 Duix | `WAITING_FOR_HARDWARE` |

## 2. 自动化全链路记录

命令：

```powershell
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_m7_demo_flow.py -q
```

结果：`1 passed, 42 warnings in 1.56s`。该测试使用 TestClient、隔离数据库和受控 fake，覆盖登录、上传、解析、课程/脚本、PPT、映射、TTS、数字人状态、发布、选课、播放器初始化、进度、问答、测验及前置知识跳转/返回。它证明业务调用链和契约可离线回归，不证明真实外部服务可用。

关键回归命令：

```powershell
backend\.venv\Scripts\python.exe -m pytest `
  backend\tests\test_m7_demo_flow.py `
  backend\tests\test_m4b_main_flows.py `
  backend\tests\test_r1_adapter_migration.py `
  backend\tests\test_r1d_duix_avatar_provider.py `
  backend\tests\test_r2_task_runtime.py `
  backend\tests\test_r2b_video_task.py `
  backend\tests\test_r2b_ppt_task.py `
  backend\tests\test_r2c_tts_batch_task.py -q
```

结果：`50 passed, 438 warnings in 8.11s`。警告主要为现有 `datetime.utcnow()` 弃用提示。

## 3. 数据准备记录

使用 `.pytest_tmp/m7_seed.db` 执行 `prepare_m7_demo.py` 两次，均成功返回相同课程、脚本和节点 ID，确认准备过程幂等。未传入 MP4，因此没有伪造视频兜底结果。

## 4. 演示步骤状态

| 步骤 | 离线自动化 | 本地页面人工 | 真实外部服务/硬件 | 证据 |
| --- | --- | --- | --- | --- |
| 教师登录 | PASS | 见第 7 节页面冒烟 | 不涉及 | `test_m7_demo_flow.py::_login_headers` |
| 上传课件 | PASS | 见第 7 节页面冒烟 | 真实解析 NOT_RUN | `test_m7_demo_flow.py::test_m7_complete_demo_flow_uses_only_controlled_external_fakes` |
| 文档解析 | PASS | 数据状态已断言 | 真实解析 NOT_RUN | `DoclingDocument.status` 断言 |
| 课程与脚本 | PASS | 见第 7 节页面冒烟 | 真实 LLM NOT_RUN | `CourseScript`、`ScriptNode` 断言 |
| PPT/节点映射 | PASS | 见第 7 节页面冒烟 | 真实 PPT NOT_RUN | `KnowledgePageMap` 断言 |
| TTS | PASS | 见第 7 节页面冒烟 | 真实 TTS NOT_RUN | `audio_url/audio_duration` 断言 |
| 数字人状态 | PASS | 见第 7 节页面冒烟 | Duix WAITING_FOR_HARDWARE | `VideoGenerationTask.status` 断言 |
| 发布 | PASS | 见第 7 节页面冒烟 | 不涉及 | `CourseStatus.PUBLISHED` 断言 |
| 学生选课 | PASS | 见第 7 节页面冒烟 | 不涉及 | enroll 响应断言 |
| 分屏播放 | PASS | 见第 7 节页面冒烟 | 真实视频 NOT_RUN | player init 节点、视频、PPT 断言 |
| 进度保存 | PASS | 见第 7 节页面冒烟 | 不涉及 | `LearningProgress` 断言 |
| 问答/测验 | PASS | 见第 7 节页面冒烟 | 真实 LLM NOT_RUN | chat/quiz 响应与消息记录断言 |
| 前置知识跳转 | PASS | 见第 7 节页面冒烟 | 真实 LLM NOT_RUN | `LearningJumpHistory` 断言 |

## 5. 尚未完成的人工项

1. 使用真实预生成 MP4/音频验证播放、时间推进和刷新后断点恢复。
2. 真实课件解析、LLM、PPT 和 TTS 的赛前人工窗口验证。
3. Duix Docker、GPU/CPU 性能、路径挂载和生成耗时验证。
4. 使用真实预生成 MP4 演练断网兜底。
5. 投屏、扬声器和现场网络演练。

这些项在实际执行前保持 `待验证/NOT_RUN/WAITING_FOR_HARDWARE`，不得写为 PASS。

## 6. 复测记录模板

| 时间 | 提交 | 环境 | 步骤 | 结果 | 错误信息 | 恢复动作 | 操作人 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  | PASS/FAIL/BLOCKED |  |  |  |

每次完整人工冒烟后追加一组记录，不覆盖历史结果。P0 必须附恢复耗时，P1 必须标明使用的是实时服务还是预生成兜底。
## 7. 本地页面人工冒烟（2026-07-10）

运行方式：后端使用 `.pytest_tmp/m7_seed.db` 隔离数据库；前端使用已通过构建的 `frontend/dist` 静态服务。两个服务仅绑定 `127.0.0.1`，完成后按 PID 停止。未调用真实 LLM、PPT、TTS 或 Duix。

| 检查项 | 结果 | 现场证据/限制 |
| --- | --- | --- |
| 首页渲染 | PASS | 标题为“超星AI互动智课系统”，主要导航和内容正常，无阻塞遮罩 |
| 教师登录 | PASS | `DemoTeacher` 登录后显示教师角色和“智课管理”导航 |
| 教师课程历史 | PASS | 显示 1 门已发布兜底课程、2 个知识点、2 分钟 |
| 教师课程详情 | PASS（有 P1） | 两个节点和讲稿可见，页面无明显重叠；历史页显示已发布，但详情页底部仍显示“发布课程” |
| 学生登录 | PASS（有 P1） | `DemoStudent` 登录成功；学生个人页仍出现“教师数字人设置”入口文案 |
| 学生选课 | PASS | 点击“开始学习”后，教师学生列表接口返回 `total_students=1`、`DemoStudent` enrollment |
| 学生课程详情 | PARTIAL | 两个节点可见；Markdown 兜底课件提示幻灯片转换失败，且未安装音频/视频 |
| 分屏播放器 | PARTIAL | 页面、两节点导航、文本页和控制条成功渲染且无明显重叠；没有预生成媒体，无法验证真实播放 |
| 进度 | PARTIAL | 选课后 `LearningProgress` 已初始化；`current_timestamp=0`，真实时间推进和断点恢复待媒体验收 |
| 问答/测验 | NOT_RUN | 会触发真实 LLM，按本阶段规则未在页面人工测试 |
| 前置知识跳转 | NOT_RUN | 会触发真实分析能力，按本阶段规则未在页面人工测试；离线 TestClient 已覆盖 |
| 浏览器控制台 | PASS（有限制） | 数据重建后无新增业务异常；有一次测试环境首次连接空库的历史 Network Error，以及无媒体自动播放的浏览器 `NotAllowedError` |
| 本地存活预检 | PASS | `-SkipTests -SkipBuild -CheckLive`：后端和前端均为 HTTP 200 |

本轮人工页面冒烟没有把 `PARTIAL/NOT_RUN` 项写成通过。赛前必须安装真实预生成 MP4/音频并重新执行播放、进度、问答/测验和前置跳转检查。

## 8. 最终回归汇总

- 一键离线关键回归：`79 passed, 448 warnings in 9.02s`。
- 前端构建：沙箱内首次 `spawn EPERM`；提升权限运行同一 `npm.cmd run build` 后通过，Vite 转换 2153 个模块。
- 全量后端：`174 passed, 28 failed, 11 errors, 484 warnings in 11.56s`。
- R2C 基线：`173 passed, 28 failed, 11 errors, 442 warnings`。
- 对比结论：新增 1 个通过用例；失败数和错误数未增加；新增 42 条 warning 与 M7 用例单跑的 42 条既有 `datetime.utcnow()` 弃用告警一致。
- 全量历史失败分类未变化：`test_f5_mapping_fix.py` 过期枚举、`test_new_features.py` 缺失 fixture/旧响应结构、`test_prerequisite_jump.py` 旧鉴权与模型约束、`test_split_video_player.py` 旧错误断言、`test_video_generation.py` 旧 provider/路由断言。
