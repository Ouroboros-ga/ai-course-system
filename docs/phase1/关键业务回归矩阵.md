# M4B 关键业务回归矩阵

更新时间：2026-07-08

本矩阵只记录 M4B 已建立的离线后端回归测试。所有外部 LLM、TTS、PPT、数字人、声音复刻和 HTTPX 类远程调用均通过 `backend/tests/fakes.py`、fixture、monkeypatch 或 dependency override 隔离；不得据此声明外部真实服务可用。

| 功能名称 | 测试文件 | 测试用例 | 成功路径覆盖情况 | 失败路径覆盖情况 | fake/mock 外部服务 | 涉及数据库表 | 是否影响 8 月决赛 | 当前风险 |
|---|---|---|---|---|---|---|---|---|
| 用户注册、登录、角色识别 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_user_register_login_and_role_identification` | 覆盖 `/api/v1/user/register`、`/api/v1/user/login`、`/api/v1/user/me`，验证 token 和默认 student 角色 | 未覆盖错误密码、重复注册 | 无外部服务 | `users` | 是 | 仅覆盖最小鉴权主路径 |
| 教师上传课件并创建课程 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_teacher_upload_document_fake_success_and_failure` | 覆盖 `/api/v1/document/upload` 成功创建 `Course`、`DoclingDocument`、`CourseScript`、`ScriptNode`、`ChatHistory` | 覆盖 `document_service.process_document` 抛错后的统一 500 响应和错误信息 | `document_service.process_document` fake；后台 TTS monkeypatch no-op | `courses`、`docling_documents`、`docling_texts`、`course_scripts`、`script_nodes`、`chat_history` | 是 | 未覆盖真实解析器，真实文件转换仍需人工验收 |
| 文档解析 fake 成功/失败 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_teacher_upload_document_fake_success_and_failure` | `DocumentProcessResult` 结构化 fake 驱动真实 endpoint 入库 | fake 抛出业务失败异常，验证响应 `code=500` 和 `data.error` | 文档处理 fake | 同上 | 是 | 当前只验证 endpoint 对异常的现状处理 |
| 教学脚本读取、保存、快照、回滚 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_teacher_script_mapping_publish_enrollment_and_course_lifecycle` | 覆盖 `/course/{id}/save`、`/course/{id}`、`/script/snapshot`、`/script/versions`、`/script/rollback/{script_id}` | 未覆盖权限失败和不存在课程 | 无外部服务 | `courses`、`course_scripts`、`script_nodes` | 是 | `document.py` 存在重复 save 路由，M4A 已锁定现状 |
| 知识点与 PPT 页面映射 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_teacher_script_mapping_publish_enrollment_and_course_lifecycle` | 覆盖 `/api/v1/mapping/{course_id}/auto`、`GET /mapping/{course_id}`、`PUT /nodes/{node_id}`、`POST /apply`，验证映射应用回 `ScriptNode.page_start` | 未覆盖无映射 apply 失败 | LLM fake 由 autouse fixture 提供，但本用例走非 LLM 主路径 | `knowledge_page_maps`、`script_nodes` | 是 | 历史 `test_f5_mapping_fix.py` 仍期望不存在的 `ScriptNodeType.KNOWLEDGE_POINT` |
| 课程发布、下架、删除 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_teacher_script_mapping_publish_enrollment_and_course_lifecycle` | 覆盖 publish、unpublish、delete，并验证删除后课程不存在 | 未覆盖非教师权限失败 | 无外部服务 | `courses` 及 delete endpoint 清理的关联表 | 是 | 删除是破坏性业务操作，当前只在临时 DB 验证 |
| 学生选课、重复选课、退课、我的课程 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_teacher_script_mapping_publish_enrollment_and_course_lifecycle` | 覆盖选课写入 `StudentEnrollment` 和 `LearningProgress`、重复选课、退课 | 记录首选课响应因 `enrollment.enrollment_at` 字段错误返回 500；记录 `/api/v1/document/my-courses` 当前被动态路由遮蔽为 404 | 无外部服务 | `student_enrollments`、`learning_progress`、`node_progress` | 是 | 已确认业务缺陷：首选课响应 500 但 DB 已写入；我的课程路由不可达 |
| 播放器初始化与播放器进度保存 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_student_player_progress_chat_quiz_and_prerequisite_flows` | 覆盖 `/api/v1/player/init/{course_id}` 扁平响应和 `/api/v1/player/progress/save` | 未覆盖课程不存在 | 无外部服务 | `learning_progress` | 是 | 播放器 init 不是统一响应结构，测试按当前公开行为锁定 |
| 学习进度同步、详情、续学 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_student_player_progress_chat_quiz_and_prerequisite_flows` | 覆盖 `/api/v1/progress/sync`、`/detail/{course_id}`、`/resume/{course_id}` | 未覆盖非法理解等级以外的失败分支 | 无外部服务 | `learning_progress`、`node_progress` | 是 | 仅覆盖单节点进度 |
| AI 问答与测验 fake LLM | `backend/tests/test_m4b_main_flows.py` | `test_m4b_student_player_progress_chat_quiz_and_prerequisite_flows` | 覆盖 `/api/v1/chat/ask` 使用默认 fake LLM；覆盖 `/api/v1/chat/quiz` 使用 JSON LLM fake | 未覆盖 LLM business_failure 到 endpoint 的完整落库 | `FakeLLMClient`；测验局部 JSON LLM monkeypatch | `chat_history`、`chat_messages` | 是 | 默认 fake LLM 只适合问答，测验必须返回合法 JSON |
| 前置知识缺口分析、跳转、返回 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_student_player_progress_chat_quiz_and_prerequisite_flows` | 覆盖 `/api/v1/prerequisite/analyze-gap`、`/jump`、`/return`，验证 `LearningJumpHistory` 写入 | 未覆盖未登录失败；历史测试中大量未登录请求返回 401 | prerequisite analyzer monkeypatch | `learning_jump_history` | 是 | 历史 `test_prerequisite_jump.py` 与当前鉴权和模型约束不一致 |
| TTS 节点音频合成 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_tts_video_and_ppt_fake_external_paths` | 覆盖 `/api/v1/document/course/{course_id}/node/{node_id}/synthesize-audio`，验证 `audio_url` 落库，文件写入临时目录 | 未覆盖 TTS business_failure 到 endpoint 的完整状态记录 | `FakeTTSClient` | `script_nodes` | 是 | endpoint 内部重新 import `tts_client`，依赖 fixture patch 模块全局 |
| 数字人视频生成 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_tts_video_and_ppt_fake_external_paths` | 覆盖 `/api/v1/video-gen/node/{node_id}/generate`、`/task/{task_id}`，验证任务完成 | 覆盖服务不可用 503；覆盖 digital human `business_failure` 当前仍被标记为 completed 且 `dh_video_path=""` | `FakeDigitalHumanClient`、`FakeTTSClient`；人脸素材解析 monkeypatch | `video_generation_tasks` | 是 | 已确认业务缺陷：数字人业务失败未记录为 failed/error |
| PPT 生成 | `backend/tests/test_m4b_main_flows.py` | `test_m4b_tts_video_and_ppt_fake_external_paths` | 覆盖 `/api/v1/ppt/generate-sync` fake success，PPT 文件写入临时目录 | 覆盖 PPT `business_failure` 返回 `code=500`、`status=failed`、错误信息 | `FakePPTClient`、`FakeLLMClient`；PDF 转换 monkeypatch no-op | `courses` | 是 | 仅覆盖 `auto_parse=False`，未覆盖生成后自动解析 |
| 外部服务 fake 模式稳定性 | `backend/tests/test_m4b_fakes.py` | 6 个 fake 自测 | 覆盖 LLM、TTS、PPT、数字人、声音复刻、HTTPX success | 覆盖 timeout、service_unavailable、malformed_response、business_failure | 全部 fake | 无直接 DB | 是 | fake 证明可控模式，不证明业务服务完整处理所有失败 |
## M4C 更新：已修复的 M4B 主流程缺陷

更新时间：2026-07-08

| 修复项 | 代码证据 | 测试证据 | 当前状态 |
|---|---|---|---|
| 学生首次选课响应不再引用不存在的 `enrollment_at` | `backend/app/api/v1/endpoints/document.py` 的 `enroll_course` 使用 `enrollment.enrolled_at` | `backend/tests/test_m4b_main_flows.py::test_m4b_teacher_script_mapping_publish_enrollment_and_course_lifecycle` | 已修复 |
| `GET /api/v1/document/my-courses` 不再被动态文档详情路由遮蔽 | `backend/app/api/v1/endpoints/document.py` 中 `@router.get("/my-courses")` 注册在 `@router.get("/{document_id}")` 前 | `backend/tests/test_m4a_route_contract.py::test_route_contract_document_my_courses_precedes_document_id` 和 M4B 主流程 | 已修复 |
| 数字人结构化业务失败不再标记为完成 | `backend/app/services/video_generation_service.py` 检查 `status` 和空 `video_path`，失败时记录 `GenerationStatus.FAILED` 与 `error_message` | `backend/tests/test_m4b_main_flows.py::test_m4b_tts_video_and_ppt_fake_external_paths` | 已修复 |

M4C 验证命令：

```text
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_m4a_isolation.py backend\tests\test_m4a_route_contract.py backend\tests\test_m4b_fakes.py backend\tests\test_m4b_main_flows.py -q
```

结果：`25 passed, 131 warnings`

全量后端测试仍存在历史基线失败：`120 passed, 28 failed, 11 errors, 166 warnings`。这些失败不作为 M4C 新增回归处理，需在后续里程碑单独清理。
