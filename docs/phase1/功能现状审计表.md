# 功能现状审计表

更新时间：2026-07-08

状态分类固定为：

- 已完成并接入主流程
- 已完成但未接入主流程
- 部分完成
- 未完成
- 仅文档规划
- 占位代码
- 无法确认

## 功能状态总表

| 功能 | 状态 | 相关文件路径 | 路由、类、函数或组件 | 调用关系 | 判断依据 | 当前风险 | 是否影响 8 月决赛 | 建议测试方式 |
|---|---|---|---|---|---|---|---|---|
| 用户登录、注册、角色与用户统计 | 已完成并接入主流程 | `backend/app/main.py`、`backend/app/api/v1/endpoints/user.py`、`backend/app/models/user_model.py`、`frontend/src/api/user.js` | `/api/v1/user/login`、`user_login`、`User`、`UserRole` | 前端 `user.js` 调 `/user/*`；后端 endpoint 直接读写 `User` | `main.py:63` 注册；`user.py:33-240` 定义路由；`user_model.py:17` 定义 `User` | endpoint 直接操作模型，`user_service.py` 为空 | 是，登录和角色是全站入口 | API 集成测试使用临时 DB 和 dependency override；单元测试覆盖密码/角色分支 |
| 教师素材管理 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/asset.py`、`backend/app/models/asset_model.py`、`frontend/src/api/asset.js`、`frontend/src/components/profile/LoginIn/menu/TeacherAssetManager.vue` | `/api/v1/asset/upload`、`list_assets`、`TeacherAsset`、`TeacherAssetManager` | 前端素材管理组件通过 `asset.js` 调 endpoint；endpoint 写 `TeacherAsset` | `main.py:71` 注册；`asset.py:124-523` 路由；`asset_model.py:30` 模型 | 文件写入、缩略图、声音复刻外部依赖混在 endpoint | 是，教师素材影响数字人流程 | 单元测试 mock 文件、ffmpeg/时长、voice clone；集成测试用临时目录 |
| 声音复刻 | 部分完成 | `backend/app/api/v1/endpoints/asset.py`、`backend/app/common/tts_client.py` | `/api/v1/asset/{asset_id}/clone-voice`、`VoiceCloneClient` | endpoint 调 `voice_clone_client`，写 `TeacherAsset.clone_status` | `asset.py:411-523`；`tts_client.py:529-656` | 真实火山服务调用风险，状态轮询可测性弱 | 间接影响，若演示数字人声音复刻则影响 | monkeypatch `voice_clone_client`，不调用真实火山 |
| 文档/课件上传解析 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/services/document_service.py`、`backend/app/models/course_model.py`、`frontend/src/views/TeacherDashboard.vue` | `/api/v1/document/upload`、`DocumentService.process_document`、`DocumentParser` | 教师页上传后进入 `document.py`，再调 `document_service` 和模型 | `main.py:64` 注册；`document.py:149`；`document_service.py:1857` | `document.py` 2482 行，职责过多；解析依赖 Docling/Office/PDF 文件系统 | 是，教师建课核心功能 | 集成测试用小型假文件与 monkeypatch 解析器/LLM；冒烟测试上传到临时目录 |
| PPT 转 PDF/图片与页面播放 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/common/slide_converter.py`、`frontend/src/components/chat/player/PptPlayer.vue` | `/document/course/{course_id}/slides`、`/slide/{page_num}` | 后端生成/缓存 slide 图片，前端播放器读取 | `document.py:2106-2250`；前端调用见 `useStudentLearning.js:232` | LibreOffice/PDF 渲染依赖本机环境；导入 endpoint 会创建目录 | 是，分屏播放核心 | monkeypatch `get_or_create_pdf`、`render_pdf_to_images`，用静态假图片 |
| AI PPT 生成 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/ppt_generation.py`、`backend/app/services/ppt_generation_service.py`、`frontend/src/api/ppt_generation.js` | `/api/v1/ppt/generate`、`generate_ppt_sync`、`PPTGenerationService`、`XfyunPPTClient` | 前端对话框调 `/ppt/generate-sync`；service 先调 LLM，再调讯飞 PPT，再下载文件 | `main.py:74`；`ppt_generation.py:100-184`；`ppt_generation_service.py:37-463` | 自动化测试若不隔离会触发付费 LLM/讯飞 API | 是，若决赛演示 AI 生成 PPT | Fake `XfyunPPTClient` + patch `llm_client.chat`，不触网 |
| 知识点与 PPT 页面映射 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/mapping.py`、`backend/app/services/mapping_service.py`、`backend/app/models/mapping_model.py`、`frontend/src/api/mapping.js` | `/api/v1/mapping/{course_id}`、`auto_generate_mapping`、`ai_match_mapping`、`KnowledgePageMap` | 前端映射编辑器调用 mapping API；service 读页面文本和脚本节点，写映射表 | `main.py:72`；`mapping.py:39-217`；`mapping_model.py:13` | AI 匹配依赖 LLM；PPTX 页文本回退逻辑复杂 | 是，影响课件页同步 | 单元测试覆盖自动映射/手动映射；AI 匹配 patch LLM |
| 教学脚本编辑、版本、回滚 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`frontend/src/api/script_editor.js`、`frontend/src/views/TeacherDashboard.vue` | `/document/course/{id}/script/snapshot`、`versions`、`rollback`、`save_course_nodes` | 前端脚本编辑 API 直接打 `document.py` 路由 | `document.py:1128-1260`；`script_editor.js:9-26` | 路由在大文件内，与课程保存逻辑交织 | 是，教师改课件脚本核心 | 集成测试用临时 DB，覆盖 snapshot/version/rollback |
| 课程发布、下架、删除 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`frontend/src/views/TeacherHistory.vue` | `/publish`、`/unpublish`、`DELETE /document/course/{id}` | 教师历史页调用 document API；删除级联清理多个模型 | `document.py:1311-1394`；`TeacherHistory.vue:522-568` | 删除逻辑涉及多表和文件，回归风险高 | 是 | 集成测试用临时 DB，验证级联但不碰生产库 |
| 学生选课、退课、我的课程 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/models/course_model.py`、`frontend/src/composables/useStudentLearning.js`、`frontend/src/views/StudentHome.vue` | `/enroll`、`/unenroll`、`/my-courses`、`StudentEnrollment` | 学生前端进入课程时调 enroll，再加载课程和进度 | `document.py:1553-1761`；`useStudentLearning.js:123-177` | 选课会初始化进度，需隔离 DB | 是 | API 集成测试覆盖重复选课、退课、进度初始化 |
| 分屏播放器初始化与进度保存 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/player.py`、`frontend/src/api/player.js`、`frontend/src/components/chat/player/SplitVideoPlayer.vue` | `/player/init/{course_id}`、`save_player_progress`、`PlayerInitData` | 前端播放器调 player API，后端汇总课程、脚本、视频、进度、映射 | `main.py:73`；`player.py:66-424`；`player.js:14-46` | 聚合多个模型，测试夹具复杂 | 是 | TestClient + 临时 DB + 假视频/幻灯片路径 |
| 学习进度同步、详情、续学 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/progress.py`、`backend/app/services/progress_service.py`、`backend/app/models/progress_model.py`、`frontend/src/api/progress.js`、`frontend/src/composables/useStudentLearning.js` | `/progress/sync`、`/detail/{course_id}`、`/resume/{course_id}`、`LearningProgress`、`NodeProgress` | 前端学习中保存和恢复进度；后端写进度模型 | `main.py:66`；`progress.py:125-469`；`progress_model.py:27-121` | `progress.py` 也直接导入 `llm_client` 用于 continuation | 是 | 临时 DB 集成测试；LLM 分支 monkeypatch |
| 理解度分析与进度续接建议 | 部分完成 | `backend/app/api/v1/endpoints/progress.py`、`backend/app/services/progress_service.py`、`frontend/src/api/progress.js` | `/progress/analyze`、`/progress/continuation`、`UnderstandingAnalyzer` | endpoint/service 调 LLM 生成分析 | `progress.py:29`、`:468`；`progress_service.py:28-260` | LLM 输出质量无法用现有测试证明，不能声明准确率 | 可能影响 AI 问答后的学习反馈 | Mock LLM 返回固定结构，验证解析和入库 |
| AI 问答与测验 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/chat.py`、`backend/app/services/qa_service.py`、`backend/app/models/qa_model.py`、`frontend/src/composables/useStudentLearning.js` | `/chat/ask`、`/chat/quiz`、`QAService` | 学生输入问题后调用 `/chat/ask`，service 结合课程上下文和 LLM | `chat.py:160-411`；`qa_service.py:196-605`；`useStudentLearning.js:505-545` | LLM 和 RAG 上下文需 Mock；准确率不能伪造 | 是，学生互动核心 | 单元测试 Prompt/解析；集成测试 patch LLM |
| 错题/薄弱前置知识重讲跳转 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/prerequisite.py`、`backend/app/services/prerequisite_service.py`、`backend/app/models/progress_model.py`、`frontend/src/composables/usePrerequisiteJump.js` | `/api/v1/prerequisite/analyze-gap`、`jump`、`return`、`LearningJumpHistory` | 学生问答后前端调用分析，再执行跳转栈操作 | `main.py:68`；`prerequisite.py:28-503`；`usePrerequisiteJump.js:22-172` | LLM 判断只能证明流程，不证明效果准确率 | 是，属于差异化演示功能 | TestClient + patch `llm_client.chat`，验证跳转栈 |
| 知识库 CRUD/导入/检索 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/knowledge.py`、`backend/app/services/knowledge_service.py`、`backend/app/models/knowledge_model.py` | `/knowledge/bases`、`/points`、`/search`、`/import/*`、`/relations` | endpoint 调 service 操作知识库模型 | `main.py:67`；`knowledge.py:37-688`；`knowledge_service.py:33-838` | 未证明与学生主流程强绑定；不是 GraphRAG | 对 8 月决赛中等影响 | 服务层单元测试 + API 集成测试 |
| 泛雅 SSO、课程/选课同步、进度回调 | 部分完成 | `backend/app/api/v1/endpoints/platform.py`、`frontend/src/api/platform.js`、`frontend/src/views/SsoCallback.vue` | `/platform/sso/callback`、`syncUser`、`syncCourse`、`syncEnrollment`、`callback/progress` | 前端有 SSO 回调；后端 endpoint 直接处理平台数据和 httpx 推送 | `main.py:77`；`platform.py:72-502`；`platform.js:4-51` | 部分接口使用 `Depends(lambda: {"user_id": 1})`；外部泛雅不可在自动化中真实调用 | 若比赛现场不用泛雅则中等；若演示对接则高 | patch httpx；补 contract 测试 |
| TTS 节点音频合成 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/common/tts_client.py`、`frontend/src/components/chat/player/PptPlayer/KnowledgeProgressPage.vue` | `/document/tts/synthesize`、`/node/{node_id}/synthesize-audio`、`TTSClient` | 前端可触发单节点合成；后端调用 `tts_client.synthesize` 并写 `ScriptNode.audio_url` | `document.py:928-1047`、`:2270-2455`；`tts_client.py:444` 有 `MockTTSClient` | 直接 fetch 绕过统一 request；真实 TTS 付费风险 | 是，影响播放体验 | 使用 `TTS_PROVIDER=mock` 或 monkeypatch；验证 URL 和模型更新 |
| 数字人视频生成 | 部分完成 | `backend/app/api/v1/endpoints/video_generation.py`、`backend/app/services/video_generation_service.py`、`backend/app/common/digital_human_client.py`、`backend/app/models/video_generation_model.py` | `/video-gen/course/{course_id}/generate`、`/node/{node_id}/generate`、`VideoGenerationService` | endpoint 健康检查后创建后台任务；service 调 TTS 和数字人 | `main.py:75`；`video_generation.py:32-209`；`video_generation_service.py:43-265` | 真实 Gradio 和 TTS 依赖强；测试中路由命名存在旧断言冲突 | 是，若演示数字人则高 | patch health/generate/TTS；只测状态机和任务入库 |
| 视频播放/上传/远程视频 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/video.py`、`frontend/src/api/video.js` | `/video/stream/{filename}`、`/remote`、`/upload` | 前端视频 API 调后端；后端读写 `videos` 和 `temp_videos` | `main.py:76`；`video.py:53-250` | 远程路径使用 httpx，文件路径需防生产污染 | 是，播放相关 | 临时目录 + patch httpx |
| 前端教师主页面 | 已完成并接入主流程 | `frontend/src/router/index.js`、`frontend/src/views/TeacherDashboard.vue`、`frontend/src/views/TeacherHistory.vue` | `/teacher/create`、`/teacher/course/:courseId`、`/teacher/history` | 路由直接挂教师页面，页面调用 document/asset/mapping/ppt API | `router/index.js:46-65`；`TeacherDashboard.vue` 2760 行 | 页面超大，包含上传、脚本、素材、发布等多职责 | 是 | 前端冒烟：构建、路由打开、关键按钮存在；API 用 mock |
| 前端学生学习页面 | 已完成并接入主流程 | `frontend/src/router/index.js`、`frontend/src/views/StudentDashboard.vue`、`frontend/src/views/StudentPlayer.vue`、`frontend/src/composables/useStudentLearning.js` | `/student`、`/student/course/:courseId`、`/player/course/:courseId` | 路由挂学生页；组合式函数调用 document/progress/chat/prerequisite | `router/index.js:68-83`；`useStudentLearning.js:102-604` | `useStudentLearning.js` 业务集中，状态多 | 是 | 前端冒烟 + API mock；后端集成覆盖进度/问答 |
| 计算机垂类能力 | 未完成 | `backend/app/api/v1/endpoints/codebench.py`、`frontend/src/views/CodeBenchView.vue`、`docs/数据结构与算法可视化智能体教学功能规划.md` | `CodeBenchView`、`codebench.py` | 未接入 `main.py`；前端路由未挂 `CodeBenchView` | `codebench.py` 只有注释；`router/index.js` 未包含 codebench 路由 | 若误报会影响 9 月规划可信度 | 不影响 8 月决赛，禁止本阶段实现 | 不测试实现，只测试不存在于主流程 |
| BKT/HMM/LSTM 认知追踪 | 仅文档规划 | `docs/重构架构设计.md`、`backend/app/api/v1/endpoints/cognitive.py`、`frontend/src/components/cognitive/CognitiveDashboard.vue`、`frontend/src/stores/cognitive.js` | `CognitiveDashboard`、`cognitive.py` | 后端无路由注册；前端是占位 | `cognitive.py` 只有注释；前端 grep 显示占位注释 | 禁止在第一阶段实现 | 不影响 8 月决赛 | 标记为规划，不进入测试范围 |
| GraphRAG | 占位代码 | `backend/app/api/v1/endpoints/graphrag.py`、`frontend/src/components/graphrag/ReasoningPathViz.vue` | `ReasoningPathViz`、`graphrag.py` | 无后端路由，无前端主路由 | `graphrag.py` 只有注释；前端组件为占位注释 | 规划与实现容易混淆 | 不影响 8 月决赛，禁止实现 | 只做“未接入”断言，不测功能 |
| 复杂多智能体 | 占位代码 | `backend/app/api/v1/endpoints/agents.py`、`frontend/src/components/agents/*`、`frontend/src/stores/agent.js` | `AgentCollaborationViz`、`AgentMessagePanel` | 无后端路由，无前端主路由 | `agents.py` 只有注释；前端组件为占位 | 不得作为功能卖点或测试对象 | 不影响 8 月决赛，禁止实现 | 不进入 M4-M7 |
| 可视化智能体/数据结构可视化 | 仅文档规划 | `docs/数据结构与算法可视化智能体教学功能规划.md`、`backend/app/api/v1/endpoints/visualization.py` | `visualization.py` | 无注册路由 | `visualization.py` 只有注释 | 规划先于实现 | 不影响 8 月决赛 | 不进入测试范围 |

## 无法确认项

| 项 | 无法确认原因 | 现有证据 | 建议 |
|---|---|---|---|
| 真实 LLM 答复质量、准确率 | 本阶段禁止调用真实付费 LLM；现有代码只证明调用链存在 | `llm_client` 多 provider，service 中多处调用 `llm_client.chat` | M4-M7 只验证流程和解析，不声明准确率 |
| 真实 TTS 音质和时长准确性 | 本阶段禁止调用真实 TTS；`MockTTSClient` 可证明隔离路径 | `tts_client.py:444` `MockTTSClient` | 只验证音频 URL、状态、字段更新 |
| 真实讯飞 PPT 生成质量 | 本阶段禁止调用真实 PPT 服务 | `ppt_generation_service.py:37-463` 外部 httpx API | Fake client 验证业务流程 |
| 真实数字人生成质量 | 本阶段禁止调用数字人服务 | `digital_human_client.py:38-206` | Fake client 验证任务状态机 |
| 旧比赛 PDF 中承诺功能的完整度 | PDF 和申报材料不是实现证据 | 实际代码审计为准 | 只在差距分析中引用，不作为完成证据 |
