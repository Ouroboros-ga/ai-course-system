# 功能现状审计表

### 2026-08-09 Ubuntu 服务器环境与外部链路复核

- 证据：实际 Ubuntu 22.04 命令、生产 `.env` 的 secret-safe readiness、`deploy/` Compose、生产 bootstrap、合成文件/OCR/LanceDB/BGE 冒烟和服务器 frozen-lockfile 前端构建。完整记录见 [服务器环境一致性与外部链路审计](2026-08-09_服务器环境一致性与外部链路审计.md)。
- 已修复：HTTP 下 `crypto.randomUUID` 缺失、Nginx 上传 60 MiB 限制、Linux 大小写 import、BGE 相对路径、GraphRAG Worker 包发现/预算传递，以及 PaddleOCR 2.7 的 Pillow 输入和返回结构不兼容。
- 已接真实链路：LibreOffice/Poppler、PaddleOCR CPU、LanceDB、固定版本本地 BGE、隔离 GraphRAG 3.1.1 Worker。DocumentIR 正式注册表在 Provider 失败时 fail-closed；`document_parse_pipeline.py` 旧的“降级 Fake”注释已纠正。
- 仍非正式：Stage 8 TTS/数字人、头像恶意文件扫描、PPT 付费 Provider、外部对象存储、V2 shadow、Student Memory/Safety Governance、通用 AgentGateway queued 分支和 ResearchAgent 后续 Provider。不得把 Demo/Stub/disabled 状态写成环境安装完成即可用。
- 资源边界：服务器 7.1 GiB 内存且无 swap，OCR、GraphRAG 构图、Judge0 压测必须串行；GraphRAG 的 US$0.70 仅为输入费用预估闸门，不是供应商账单硬上限。
- 最终开关：GraphRAG Worker/BGE/预算拒绝链已通过但真实课程外发未获数据范围/目的地级授权，`GRAPHRAG_ENABLED=false`；Judge0 官方镜像/Compose 已安装，但安全的 cap-only 配置无法 privatize mounts，官方 `privileged` 配置不在共用应用主机启用，`JUDGE0_ENABLED=false`。两个状态均 fail-closed。

### 2026-08-07 ResearchAgent P0

- 证据：`backend/app/platform/agents/research/`、`providers/research/paper_search.py`、`api/v1/endpoints/research_agent.py`、`frontend/src/app/pages/course/research/ResearchWorkspacePage.vue`。
- 已实现：独立 ResearchAgent 类型与四节点工作流；arXiv Atom 元数据检索、进程内 3 秒节流与 24 小时缓存、PII 脱敏、API + Agent 内部 `ResearchScopePort` 的课程 `course.question.ask` 双重权限、metadata-only EvidenceGate、来源核验页面和 API 契约测试。外部结果强制标记补充参考，不能修改掌握度、推荐或课程图谱。
- 未实现：ResearchProject/CorpusSnapshot 持久化，Semantic Scholar/OpenAlex/Crossref，全文解析与撤稿状态，趋势图、证据综合、写作生成、Vale/LaTeX/Pandoc，完整 GitHub 仓库复现 Worker。页面按 `research_preview` 展示，不能作为已上线证明。
- 对历史/比赛构想的修正：现有 `WebResearchPort` 不是论文检索端口；课程 GraphRAG 不能直接吸收外部候选；`codebench.py` 为空；Judge0 只适合受限代码片段，不能代替完整仓库容器复现。现行依据改为 [研究智能体整体架构与前端设计](研究智能体整体架构与前端设计.md)。

### 2026-08-07 管理员配置与开放 API 兼容层

- 证据：`backend/app/api/v1/endpoints/admin_platform.py`、`backend/app/services/platform_admin_service.py`、迁移 `0041`、`frontend/src/app/pages/admin/PlatformAdminPage.vue`。
- 结论：平台管理员已能管理用户筛选、用户名、启用状态、全局 user/admin 角色及密码重置；LLM/TTS/PPT 配置为数据库持久化、密钥掩码和保存后可达性探针。未配置或探针失败返回明确 unavailable，不伪造 Provider 成功。
- 迁移：历史 student/teacher 归一为 user；针对已部署数据库中的大写 `TEACHER`，迁移 `0043` 明确升级为 `ADMIN` 并写入 `platform.admin`，`STUDENT` 归一为 `USER`；课程内 teacher/owner 仍保留为 Course Access 语义。旧 JWT/泛雅角色只兼容读取，不再新增。
- 泛雅·超星 AI PDF 示例不与内部路由/数据模型重复实现，新增可移除的 `/api/v1/compat/*` 参考适配包（`backend/app/external_apis/fanya_chaoxing_ai/`）。删除该目录时主应用自动跳过挂载，独立签名豁免也不会登记；外部 URL 材料导入、ASR 与未映射 ID 暂返回结构化不可用，后续接入前不得宣传为已提供真实能力。

### 2026-08-09 账户用户名与展示名称收敛

- 证据：`backend/app/api/v1/endpoints/user.py`、`backend/app/services/platform_admin_service.py`、`frontend/src/app/pages/admin/PlatformAdminPage.vue`、`frontend/src/app/pages/account/AccountPage.vue`。
- 已修复：此前管理员页面编辑 `real_name`（展示名称）、登录却校验 `username`，使同一账号出现两个不一致名称。现统一以 `users.username` 作为登录、管理员管理、个人中心和右上角展示的唯一账户名称；`real_name` 保留为外部同步资料，不进入账户名称链路。
- 兼容行为：登录请求仍使用 `username` 参数，先精确匹配用户名；仅当该值全为数字且用户名未命中时，按不可变数值用户 ID 查找。个人修改用户名保持 ID 不变且检查重名；改密码仍验证原密码，管理员重置密码仍使旧 JWT 失效。

> **2026-07-28 状态说明：** 本文早期条目中的“旧 `/document/upload`、CourseScript、解析未实现”等描述属于历史审计快照，不再代表当前统一建设链。当前事实以 [统一课程建设实施状态](统一课程建设实施状态.md)、[PageDesign 待打通能力清单](PageDesign待打通能力_配置与开发分工清单.md)、实际路由和运行结果为准。旧条目保留仅用于追溯，不得据此判断当前功能已完成或未完成。

> **2026-08-10 智能备课真实材料复测：** 高密度 PDF 在材料证据 Map/Reduce 中可触发递归拆分并耗尽原 64 次共享调用预算；默认值已调整为 160，仍受 600,000 字总材料、25 个基础 Map 批次和并发 2 的硬上限约束。此前发现的证据 ID 幻觉、描述性列表超限和 Map 嵌套 `stage` 均已按程序回填/归一化处理。该次端到端任务在原预算下未完成，新的 160 次预算尚待真实复测；失败路径继续保证不覆盖原课程草稿。

> **2026-08-10 证据归因粒度修正：** 先前“模型不返回证据 ID、服务端按输入范围回填”的实现错误地把全课程证据并集赋给每个 Map/Reduce 产物和每个大纲/讲稿节点，导致教师端所有节点显示同一批 `source_block_refs`。该实现不再是现状证据：Initial Prep 现改为基于标题/主题/文本的确定性局部绑定与层级受限聚合；旧版未审核草稿须基于同一 corpus 重建为新版本，不原地修改或自动改写已锁定/已发布版本。

> **2026-08-07 学习数据审计：** 新学习链使用 `learning_events` 与 release/node 维度的学生投影，学习上下文与教师统计走 `/facade` API；旧 `LearningProgress`、`NodeProgress`、`StudentEnrollment` 保留迁移审计，不再是新页面的正式数据源。认知/推荐 Agent Port 扩展仅已冻结为 planned 契约，未改动其内部实现。

> **2026-08-08 学习链实现复核：** 新学习页已从统一 `learning-context` 恢复当前 release 的最近锚点；学习轨道显示课程完成摘要、未学习/学习中/已完成与已掌握/待掌握/需要更多证据等双层状态。学生点击状态详情后按需读取现有 `/cognitive/course/{course_id}/state`，不为全部知识点批量请求；认知详情降级不阻断播放。进入节点写入 `node_opened`，播放/阅读/显式完成写入规范化事件，失败事件由浏览器待发队列重试。`/facade/course/{course_id}/analytics` 已返回掌握等级分布、未知/低置信度及待推荐人数，并提供学生摘要和下钻接口；该 GET 只读统一统计投影，不在请求内写入。正式评分证据会同步 upsert `LearningEvidenceContext`，无法唯一确定来源 release 时保留 unknown。完成接口拒绝非 active release；事件未来时间戳拒绝。发布版本播放器已只输出 `knowledge_point`，chapter/section/example/practice_suggestion 不进入学生轨道和完成分母；教师草稿预览仍可查看完整目录。Outbox/死信/异步投影重放仍未实现，事件路径当前采用同步投影刷新。

> 2026-07-27 同步说明：当前课程 84 仍走旧同步 `upload_document → DoclingDocument / CourseScript / ScriptNode` 链，未产生 `SourceMaterialVersion`、`DocumentParseRun`、`DocumentBlock`、`EvidenceSpan` 或图谱候选。统一上传与解析目标已冻结在 [统一课程建设与解析基线](统一课程建设与解析基线.md)，尚未完成实现，不得将旧讲稿节点视为新 Evidence/图谱链已经可用。

更新时间：2026-07-08

### 2026-08-07 Stage 8 Provider 配置收敛

- **代码事实**：新增 `stage8_provider_runtime.py`，`MEDIA_DEMO_MODE=true` 强制有效
  Provider 为 `fake-demo`；正式模式只接受显式 `STAGE8_TTS_PROVIDER=doubao`，缺失配置
  以 `needs_configuration` fail-closed，不再让 Stage 8 读取通用 `TTS_PROVIDER`。
- **调用链**：健康接口、批量计划/确认、单节点执行/重试与执行服务共用该解析器；浏览器
  的 Provider key 只用于陈旧页面校验，不能选中或切换真实 Provider。
- **页面状态**：建设页读取服务端公开的 `effective_provider`、`billable`、
  `requires_confirmation`；Demo 显示 `fake-demo`，正式豆包才要求费用确认。
- **验证边界**：本次未调用付费 TTS；需以 Fake/Mock 回归与随后教师明确授权的短文本 POC
  继续验证。

### 2026-08-07 媒体发布状态复核与下一步

### 2026-08-07 P5.2 OSS 与旧链隔离
- **代码证据**：Local 上传意图返回 PUT + `exp/sig`；S3 provider 返回 presigned POST `fields`；前端 `uploadAvatarSourceMedia` 分支处理二进制或 FormData；confirm 服务端重新 HEAD/SHA/MIME/时长。Local 内容读取按签名 scope、object key、Course Access 顺序 fail-closed。
- **旧链语义**：`/video-gen` 响应标记 `legacy=true`，附 `Deprecation`/`Sunset`，只保存 `VideoGenerationTask`，教师建设页不再引导，正式发布只接受 `MediaRelease + audio-playlist/v1`。
- **验证边界**：P5.2 自动化覆盖 Local PUT、过期签名、错误 object key、S3 presigned POST fields、SHA 不一致和签名 scope；未调用真实 OSS 或付费 TTS。

- **复核原因**：早期 README 与阶段 8 文档仍写课程 87 “没有 active MediaRelease”，与本地
  `database/smart_class.db` 和播放服务的实际结果矛盾，不能继续作为状态依据。
- **运行证据**：`mrel_623ac8541f944ed79f31f01e6711f996` 为 active；课程发布
  `cr_4ca1bc01166040a8bb7427a6b3f91fb2` 为 published，快照固定
  `playlist_content_hash=e78973d2…`。该 release 的 20 个 `MediaReleaseItem` 均为 `ready`，
  音频/字幕/Cue/PPT 映射快照均为 20/20，`ppt-manifest/v1` 含 44 页，
  `audio-playlist/v1` 为 880448ms。`media_playback_service.get_current_playback(course_id=87)`
  返回 `available=true`，并为 20 个条目签发音频、字幕与 Cue URL。
- **已验证**：2026-08-07 运行 `backend/.venv/Scripts/python.exe -m pytest
  tests/test_media_release.py tests/test_media_stage8_m2m5.py -q`，49 passed；前端媒体单测
  30 passed 和生产构建已在本工作周期通过。所有自动化测试仍为 Fake/Mock，未调用付费服务。
- **浏览器验收**：教师已确认播放/暂停、seek、倍速、跨知识点、PPT/字幕同步和数字人位于左侧
  音频讲解区的手工验收完成；480p/24fps FPS、掉帧和长时播放指标仍未形成量化记录。

### 2026-08-07 豆包 TTS 一次授权 POC

- 教师明确授权后仅调用一次 `backend/scripts/volcengine_tts_poc.py --allow-billable-call`，
  文本 30 UTF-8 bytes，PCM 24kHz/16-bit，字幕开启；未保存音频、原始帧、Key 或 Speaker，
  未写入对象存储和课程媒体版本。
- 会话完整成功，无 Provider 错误；音频 94,876 bytes、PCM 时长 1,976.583ms；返回 9 条
  `words`，最后结束时间 1,785.000ms，误差 191.583ms；`phonemes` 为空（0 条）。
- 结论：当前豆包配置可作为短文本音频和字级字幕时间轴来源，但不能作为精确口型时序来源。
  长文本、复杂公式和真实课程质量仍需单独、限量、再次授权的 POC；当前课程 87 Fake 发布版本
  未被修改。

> 2026-08-07 P5.3 复核：`volcengine_tts_poc.py` dry-run 配置检查通过；本回合教师已明确授权真实调用，但 Codex 运行环境拒绝外部付费请求审批，未发出网络请求，未重试。完整边界与结果见 [阶段8 P5.3](阶段8_P5.3_一次受控豆包验收.md)。

### 2026-08-06 媒体与数字人方案冻结（覆盖早期媒体条目）

- **现行主链**：课程媒体建设使用 `MediaBuildBatch`/`MediaReleaseItem`，经教师一次确认
  后生成 Fake WAV 或 Worker TTS，冻结 `subtitle-manifest/v1`、`avatar-cues/v1`、
  `ppt-manifest/v1` 和 `audio-playlist/v1`，再激活 `MediaRelease` 并在正式课程发布中
  固定 `media_release_id + playlist_content_hash`。
- **学习端事实**：`useMediaPlayback` + `usePlaylistPlayback` 请求 playback API；活动知识点
  的 `<audio>` 是唯一时钟，PPT、字幕和 PixiJS 平台预制角色从该时钟投影。旧视频、旧单
  节点 TTS 和服务端逐帧数字人接口保留兼容/历史语义，不是课程 87 的新发布主链。
- **课程 87**：2026-08-06 已跑通整条媒体主链（见下方实测），当前 active
  `MediaRelease` 为 `mrel_623ac854…`（fake-v1.1-playable），正式发布
  `cr_4ca1bc01…` 已写入 `media_snapshot`（`media_release_id` +
  `playlist_content_hash`）；在 PPT 映射/manifest 与新可播放媒体资产冻结完成前，
  服务端仍必须拒绝 playlist、激活和正式发布。Fake WAV 试听只验证本地解码、权限
  签名和状态机，不代表真实音质或数字人效果。
- **文档迁移**：`docs/phase1/阶段8_媒体TTS数字人PPT_实施规划.md` 为现行基线；旧视频/服务端
  数字人文档已在文档索引标记为“已废弃/仅历史追溯”。

### 2026-08-06 批量媒体建设页门禁修正与课程 87 链路实测

- **主链代码状态核对**：前端消费链完整
  （`LearnPage.vue → useAvatarPlayback.js → AvatarViewport.vue → Sprite2DRenderer.js`，
  PixiJS 图集 + `<audio>` 唯一时钟 + auto/low_resource/compatibility 降级）；后端
  `DhLiveMiniProvider` 仍是调用协议壳（未包装 `data_preparation_mini.py` /
  `data_preparation_web.py` 为 `--action prepare_avatar/playback_manifest`），
  strict 模式下无实测报告时 `health_check` 恒为 unhealthy——与冻结基线一致，不新增证据。
- **建设页真实阻塞修复**：批量（playlist）模式下 `MediaReleaseItem` 全部 `ready` 时，
  `BuildMediaPage.vue` 仍用单节点 `hasFrozenCues`（release 级字段）判断，导致“生成 PPT
  manifest”按钮不显示。已改为批量模式使用 `batchState.status === READY` 或
  `batchItems` 全部 `status === ready` 作为 PPT manifest 前置条件，并在批量面板新增六步
  流水线：Fake WAV → Cue 冻结 → PPT manifest → audio-playlist/v1 → 激活 → 正式发布。
  前端 `oxlint`/`eslint` 与 `vite build` 通过；顺带移除无效 `--space-5` 令牌并删除未使用的
  `draftReleases` 计算属性。
- **PPT manifest 500 修正**：`build_ppt_manifest` 上传课件路径未捕获
  `PptSlideRenderError`，LibreOffice 缺失时端点返回裸 500；已翻译为
  `PptManifestGenerationError`，端点现在返回 409 并携带
  `CONVERTER_UNAVAILABLE: LibreOffice binary not found` 的可操作原因。
- **课程 87 实测（用户 6 = OWNER，Fake TTS）**：`plan`（20 个已映射节点）返回
  `can_confirm=True`、`can_freeze_playlist=True`、无阻塞原因。**v1.1 批次实测已通过**：
  新建 `mbatch_0f745c95…`/`mrel_623ac854…`（fake-v1.1-playable，20/20 条目
  `ready`，WAV 头 `RIFF…WAVE`、`audio/wav` 可解码）；LibreOffice 安装后
  `ppt-manifest/v1` 真实渲染 44 页（主 deck 1 + decks 3）；`audio-playlist/v1`
  冻结 880448ms（sha `e78973d2…`）；release 激活；正式发布
  `cr_4ca1bc01…` 写入 `media_snapshot`（`media_release_id=mrel_623ac854…`、
  `playlist_content_hash=e78973d2…`）；学习端 `playback` 契约
  `available=True`（20 items、44 页、平台注册角色 `platform-instructor-v2@1.0.0`），音频/字幕/Cue/PPT
  页图签名资产全部 200。
- **旧 v3 批次（fake-v1.0）不可播**：08-05 批次 `mbatch_ffb31f…` 全部输出
  `fake-v1.0` 占位 mp3（873 字节、head `FAKE_TTS`，早于 v1.1 修复），浏览器无法解码；
  按媒体不可变纪律保留为历史资产，不修改字节。已用新建 v1.1 批次替代完成学习端音频
  播放验收，不会把占位音频伪装成可播放产物。
- **PPT 页图 404/500 修正（2026-08-06）**：`ppt_slide_render_service.py` 渲染循环
  补充 `MediaAsset` 台账幂等注册（`asset_type=ppt_image`、`resource_version=ppt-manifest/v1`）；
  历史 238 行 PPT_SLIDE_IMAGE 渲染补齐台账。数据修复曾以小写 `backend='local'` 写入，
  与 SQLAlchemy Enum 按成员名（`LOCAL`/`OSS`）存储不一致，读取时
  `LookupError: 'local' is not among the defined enum values`；已统一更新为 `LOCAL`。
  修正后学习端 PPT 页图签名 URL 返回 200（`image/png`）。

状态分类固定为：

- 已完成并接入主流程
- 已完成但未接入主流程
- 部分完成
- 未完成
- 仅文档规划
- 占位代码
- 无法确认

### 2026-08-05 批量媒体播放清单迁移前置

- **现象**：本地数据库仍停留在 Alembic `0036` 时，已部署 P4 代码读取
  `MediaRelease.audio_playlist_object_key` 等 `0037` 字段，`GET /api/v1/media/course/{id}/releases`
  会返回 500；这不是前端签名参数或按钮状态问题。
- **修正**：部署和本地升级必须显式运行 `alembic upgrade head`，由迁移
  `0037_batch_media_playlist` 创建批量建设/条目表并补全媒体版本字段；禁止在应用启动时
  用 `SQLModel.metadata.create_all` 掩盖版本不一致。
- **验证边界**：升级后媒体 P4 定向后端回归、前端 API 契约和生产构建通过；课程 87
  尚缺可冻结 PPT 映射和已发布媒体，尚未进行真实付费 TTS 或目标设备性能验收。

### 2026-08-05 Fake TTS 草稿试听修正

- **现象**：原 Fake TTS 把文本元数据伪装为 `.mp3`，浏览器无法解码；建设页的原生
  `<audio>` 也不会继承 Axios 的 `Authorization` header，导致草稿试听请求 401。旧成功
  TTS 任务还可能没有 `MediaAsset` 台账，下载路由无法按课程权限定位资产。
- **修正**：`FakeTtsProvider` 现在只生成可解码的本地 WAV 节拍诊断轨，版本
  `fake-v1.1-playable` 与旧 `.mp3` 对象隔离；TTS 新生成及缓存复用均补齐课程归属的
  `MediaAsset` 台账。建设页只为同源 `/api/...` 音频 URL 加登录 token，OSS/S3 外部
  预签名 URL 保持不变。
- **验证边界**：后端回归验证 WAV 文件头、`audio/wav` 内容响应、Course Access v1
  token 读取与缓存台账修复；不会调用豆包或任何付费 TTS。该试听仅验证播放器链路，
  不代表真实语音、字幕时序或数字人表现。课程 87 的旧 `fake-v1.0` 不可变资产必须
  保留，需新建 Fake 批次才能做此试听验证。

## 功能状态总表

| 功能 | 状态 | 相关文件路径 | 路由、类、函数或组件 | 调用关系 | 判断依据 | 当前风险 | 是否影响 8 月决赛 | 建议测试方式 |
|---|---|---|---|---|---|---|---|---|
| 用户登录、注册、角色与用户统计 | 已完成并接入主流程 | `backend/app/main.py`、`backend/app/api/v1/endpoints/user.py`、`backend/app/models/user_model.py`、`frontend/src/api/user.js` | `/api/v1/user/login`、`user_login`、`User`、`UserRole` | 前端 `user.js` 调 `/user/*`；后端 endpoint 直接读写 `User` | `main.py:63` 注册；`user.py:33-240` 定义路由；`user_model.py:17` 定义 `User` | endpoint 直接操作模型，`user_service.py` 为空 | 是，登录和角色是全站入口 | API 集成测试使用临时 DB 和 dependency override；单元测试覆盖密码/角色分支 |
| 教师素材管理 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/asset.py`、`backend/app/models/asset_model.py`、`frontend/src/api/asset.js`、`frontend/src/components/profile/LoginIn/menu/TeacherAssetManager.vue` | `/api/v1/asset/upload`、`list_assets`、`TeacherAsset`、`TeacherAssetManager` | 前端素材管理组件通过 `asset.js` 调 endpoint；endpoint 写 `TeacherAsset` | `main.py:71` 注册；`asset.py:124-523` 路由；`asset_model.py:30` 模型 | 文件写入、缩略图、声音复刻外部依赖混在 endpoint | 是，教师素材影响数字人流程 | 单元测试 mock 文件、ffmpeg/时长、voice clone；集成测试用临时目录 |
| 声音复刻 | 部分完成 | `backend/app/api/v1/endpoints/asset.py`、`backend/app/common/tts_client.py` | `/api/v1/asset/{asset_id}/clone-voice`、`VoiceCloneClient` | endpoint 调 `voice_clone_client`，写 `TeacherAsset.clone_status` | `asset.py:411-523`；`tts_client.py:529-656` | 真实火山服务调用风险，状态轮询可测性弱 | 间接影响，若演示数字人声音复刻则影响 | monkeypatch `voice_clone_client`，不调用真实火山 |
| 文档/课件上传解析 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/services/document_service.py`、`backend/app/models/course_model.py`、`frontend/src/views/TeacherDashboard.vue` | `/api/v1/document/upload`、`DocumentService.process_document`、`DocumentParser` | 教师页上传后进入 `document.py`，再调 `document_service` 和模型 | `main.py:64` 注册；`document.py:149`；`document_service.py:1857` | `document.py` 2482 行，职责过多；解析依赖 Docling/Office/PDF 文件系统 | 是，教师建课核心功能 | 集成测试用小型假文件与 monkeypatch 解析器/LLM；冒烟测试上传到临时目录 |
| PPT 转 PDF/图片与页面播放 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/common/slide_converter.py`、`frontend/src/components/chat/player/PptPlayer.vue` | `/document/course/{course_id}/slides`、`/slide/{page_num}` | 后端生成/缓存 slide 图片，前端播放器读取 | `document.py:2106-2250`；前端调用见 `useStudentLearning.js:232` | LibreOffice/PDF 渲染依赖本机环境；导入 endpoint 会创建目录 | 是，分屏播放核心 | monkeypatch `get_or_create_pdf`、`render_pdf_to_images`，用静态假图片 |
| AI PPT 生成 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/ppt_generation.py`、`backend/app/services/ppt_generation_service.py`、`frontend/src/api/ppt_generation.js` | `/api/v1/ppt/generate`、`generate_ppt_sync`、`PPTGenerationService`、`XfyunPPTClient` | 前端对话框调 `/ppt/generate-sync`；service 先调 LLM，再调讯飞 PPT，再下载文件 | `main.py:74`；`ppt_generation.py:100-184`；`ppt_generation_service.py:37-463` | 自动化测试若不隔离会触发付费 LLM/讯飞 API | 是，若决赛演示 AI 生成 PPT | Fake `XfyunPPTClient` + patch `llm_client.chat`，不触网 |
| 知识点与 PPT 页面映射 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/mapping.py`、`backend/app/services/mapping_service.py`、`backend/app/models/mapping_model.py`、`frontend/src/api/mapping.js` | `/api/v1/mapping/{course_id}`、`auto_generate_mapping`、`ai_match_mapping`、`KnowledgePageMap` | 前端映射编辑器调用 mapping API；service 读页面文本和脚本节点，写映射表 | `main.py:72`；`mapping.py:39-217`；`mapping_model.py:13` | AI 匹配依赖 LLM；PPTX 页文本回退逻辑复杂 | 是，影响课件页同步 | 单元测试覆盖自动映射/手动映射；AI 匹配 patch LLM |
| 教学脚本编辑、版本、回滚 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`frontend/src/api/script_editor.js`、`frontend/src/views/TeacherDashboard.vue` | `/document/course/{id}/script/snapshot`、`versions`、`rollback`、`save_course_nodes` | 前端脚本编辑 API 直接打 `document.py` 路由 | `document.py:1128-1260`；`script_editor.js:9-26` | 路由在大文件内，与课程保存逻辑交织 | 是，教师改课件脚本核心 | 集成测试用临时 DB，覆盖 snapshot/version/rollback |
| 课程发布、下架、删除 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`frontend/src/views/TeacherHistory.vue` | `/publish`、`/unpublish`、`DELETE /document/course/{id}` | 教师历史页调用 document API；删除级联清理多个模型 | `document.py:1311-1394`；`TeacherHistory.vue:522-568` | 删除逻辑涉及多表和文件，回归风险高 | 是 | 集成测试用临时 DB，验证级联但不碰生产库 |
| 学生选课、退课、我的课程 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/models/course_model.py`、`frontend/src/composables/useStudentLearning.js`、`frontend/src/views/StudentHome.vue` | `/enroll`、`/unenroll`、`/my-courses`、`StudentEnrollment` | 学生前端进入课程时调 enroll，再加载课程和进度 | `document.py:1553-1761`；`useStudentLearning.js:123-177` | 选课会初始化进度，需隔离 DB | 是 | API 集成测试覆盖重复选课、退课、进度初始化 |
| 分屏播放器初始化与进度保存 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/player.py`、`frontend/src/api/player.js`、`frontend/src/components/chat/player/SplitVideoPlayer.vue` | `/player/init/{course_id}`、`save_player_progress`、`PlayerInitData` | 前端播放器调 player API，后端汇总课程、脚本、视频、进度、映射 | `main.py:73`；`player.py:66-424`；`player.js:14-46` | 聚合多个模型，测试夹具复杂 | 是 | TestClient + 临时 DB + 假视频/幻灯片路径 |
| 学习进度同步、详情、续学 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/progress.py`、`backend/app/services/progress_service.py`、`backend/app/models/progress_model.py`、`frontend/src/api/progress.js`、`frontend/src/composables/useStudentLearning.js` | `/progress/sync`、`/detail/{course_id}`、`/resume/{course_id}`、`LearningProgress`、`NodeProgress` | 前端学习中保存和恢复进度；后端写进度模型 | `main.py:66`；`progress.py:125-469`；`progress_model.py:27-121` | `progress.py` 也直接导入 `llm_client` 用于 continuation | 是 | 临时 DB 集成测试；LLM 分支 monkeypatch |
| 理解度分析与进度续接建议 | 部分完成 | `backend/app/api/v1/endpoints/progress.py`、`backend/app/services/progress_service.py`、`frontend/src/api/progress.js` | `/progress/analyze`、`/progress/continuation`、`UnderstandingAnalyzer` | endpoint/service 调 LLM 生成分析 | `progress.py:29`、`:468`；`progress_service.py:28-260` | LLM 输出质量无法用现有测试证明，不能声明准确率 | 可能影响 AI 问答后的学习反馈 | Mock LLM 返回固定结构，验证解析和入库 |
| AI 问答与测验 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/chat.py`、`backend/app/services/qa_service.py`、`backend/app/models/qa_model.py`、`frontend/src/composables/useStudentLearning.js` | `/chat/ask`、`/chat/quiz`、`QAService` | 学生输入问题后调用 `/chat/ask`，service 结合课程上下文和 LLM | `chat.py:160-411`；`qa_service.py:196-605`；`useStudentLearning.js:505-545` | LLM 和 RAG 上下文需 Mock；准确率不能伪造 | 是，学生互动核心 | 单元测试 Prompt/解析；集成测试 patch LLM |
| 错题/薄弱前置知识重讲跳转 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/prerequisite.py`、`backend/app/services/prerequisite_service.py`、`backend/app/models/progress_model.py`、`frontend/src/composables/usePrerequisiteJump.js` | `/api/v1/prerequisite/analyze-gap`、`jump`、`return`、`LearningJumpHistory` | 学生问答后前端调用分析，再执行跳转栈操作 | `main.py:68`；`prerequisite.py:28-503`；`usePrerequisiteJump.js:22-172` | LLM 判断只能证明流程，不证明效果准确率 | 是，属于差异化演示功能 | TestClient + patch `llm_client.chat`，验证跳转栈 |
| 知识库 CRUD/导入/检索 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/knowledge.py`、`backend/app/services/knowledge_service.py`、`backend/app/models/knowledge_model.py` | `/knowledge/bases`、`/points`、`/search`、`/import/*`、`/relations` | endpoint 调 service 操作知识库模型 | `main.py:67`；`knowledge.py:37-688`；`knowledge_service.py:33-838` | 未证明与学生主流程强绑定；不是 GraphRAG | 对 8 月决赛中等影响 | 服务层单元测试 + API 集成测试 |
| 泛雅 SSO、课程/选课同步、进度回调 | 部分完成 | `backend/app/api/v1/endpoints/platform.py`、`frontend/src/api/platform.js`、`frontend/src/views/SsoCallback.vue` | `/platform/sso/callback`、`syncUser`、`syncCourse`、`syncEnrollment`、`callback/progress` | 前端有 SSO 回调；后端 endpoint 直接处理平台数据和 httpx 推送 | `main.py:77`；`platform.py:72-502`；`platform.js:4-51` | 部分接口使用 `Depends(lambda: {"user_id": 1})`；外部泛雅不可在自动化中真实调用 | 若比赛现场不用泛雅则中等；若演示对接则高 | patch httpx；补 contract 测试 |
| TTS 节点音频合成 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/document.py`、`backend/app/common/tts_client.py`、`frontend/src/components/chat/player/PptPlayer/KnowledgeProgressPage.vue` | `/document/tts/synthesize`、`/node/{node_id}/synthesize-audio`、`TTSClient` | 前端可触发单节点合成；后端调用 `tts_client.synthesize` 并写 `ScriptNode.audio_url` | `document.py:928-1047`、`:2270-2455`；`tts_client.py:444` 有 `MockTTSClient` | 直接 fetch 绕过统一 request；真实 TTS 付费风险 | 是，影响播放体验 | 使用 `TTS_PROVIDER=mock` 或 monkeypatch；验证 URL 和模型更新 |
| 数字人视频生成 | 部分完成 | `backend/app/api/v1/endpoints/video_generation.py`、`backend/app/services/video_generation_service.py`、`backend/app/common/digital_human_client.py`、`backend/app/models/video_generation_model.py` | `/video-gen/course/{course_id}/generate`、`/node/{node_id}/generate`、`VideoGenerationService` | endpoint 健康检查后创建后台任务；service 调 TTS 和数字人 | `main.py:75`；`video_generation.py:32-209`；`video_generation_service.py:43-265` | 真实 Gradio 和 TTS 依赖强；测试中路由命名存在旧断言冲突 | 是，若演示数字人则高 | patch health/generate/TTS；只测状态机和任务入库 |
| 视频播放/上传/远程视频 | 已完成并接入主流程 | `backend/app/api/v1/endpoints/video.py`、`frontend/src/api/video.js` | `/video/stream/{filename}`、`/remote`、`/upload` | 前端视频 API 调后端；后端读写 `videos` 和 `temp_videos` | `main.py:76`；`video.py:53-250` | 远程路径使用 httpx，文件路径需防生产污染 | 是，播放相关 | 临时目录 + patch httpx |
| 前端教师主页面 | 已完成并接入主流程 | `frontend/src/router/index.js`、`frontend/src/views/TeacherDashboard.vue`、`frontend/src/views/TeacherHistory.vue` | `/teacher/create`、`/teacher/course/:courseId`、`/teacher/history` | 路由直接挂教师页面，页面调用 document/asset/mapping/ppt API | `router/index.js:46-65`；`TeacherDashboard.vue` 2760 行 | 页面超大，包含上传、脚本、素材、发布等多职责 | 是 | 前端冒烟：构建、路由打开、关键按钮存在；API 用 mock |
| 前端学生学习页面 | 已完成并接入主流程 | `frontend/src/router/index.js`、`frontend/src/views/StudentDashboard.vue`、`frontend/src/views/StudentPlayer.vue`、`frontend/src/composables/useStudentLearning.js` | `/student`、`/student/course/:courseId`、`/player/course/:courseId` | 路由挂学生页；组合式函数调用 document/progress/chat/prerequisite | `router/index.js:68-83`；`useStudentLearning.js:102-604` | `useStudentLearning.js` 业务集中，状态多 | 是 | 前端冒烟 + API mock；后端集成覆盖进度/问答 |
| 计算机垂类能力 | 未完成 | `backend/app/api/v1/endpoints/codebench.py`、`frontend/src/views/CodeBenchView.vue`、`docs/数据结构与算法可视化智能体教学功能规划.md` | `CodeBenchView`、`codebench.py` | 未接入 `main.py`；前端路由未挂 `CodeBenchView` | `codebench.py` 只有注释；`router/index.js` 未包含 codebench 路由 | 若误报会影响 9 月规划可信度 | 不影响 8 月决赛，禁止本阶段实现 | 不测试实现，只测试不存在于主流程 |
| BKT/HMM/LSTM 认知追踪 | 仅文档规划 | `docs/重构架构设计.md`、`backend/app/api/v1/endpoints/cognitive.py`、`frontend/src/components/cognitive/CognitiveDashboard.vue`、`frontend/src/stores/cognitive.js` | `CognitiveDashboard`、`cognitive.py` | 后端无路由注册；前端是占位 | `cognitive.py` 只有注释；前端 grep 显示占位注释 | 禁止在第一阶段实现 | 不影响 8 月决赛 | 标记为规划，不进入测试范围 |
| GraphRAG / CourseKnowledgeBundle | 已完成并接入后端主流程；前端大图体验部分完成 | `backend/app/api/v1/endpoints/graphrag.py`、`backend/app/services/knowledge_bundle_service.py`、`backend/app/platform/knowledge/`、`backend/app/models/knowledge_bundle_model.py` | `/api/v1/graph/course/{id}/knowledge-bundle/*`、`GraphRagRunner`、`SqlLanceCourseKnowledgeProvider` | Canonical DocumentIR -> GraphRAG -> 稳定 `kn_*` -> Evidence/Citation -> LanceDB -> 原子 Active Bundle；学生、推荐和 TeachingAgent 读取同一 Head | 课程 87 已真实完成 318 Chunk 抽取，并激活 v4：808 节点、1137 关系、512 维真实 LanceDB；定向后端测试覆盖零模型精炼、课程隔离、Citation 闭合、原子激活和 Course Access | 现有前端对 808 节点缺少聚类/渐进展开；语义质量仍需按版本审计 | 是 | 运行 `backend/tests/test_knowledge_bundle_lancedb.py`；读取 Active Head、重启检索、Citation 和回滚核验。详见 `课程知识图谱GraphRAG_Bundle设计与接口说明.md` |
| 复杂多智能体 | 占位代码 | `backend/app/api/v1/endpoints/agents.py`、`frontend/src/components/agents/*`、`frontend/src/stores/agent.js` | `AgentCollaborationViz`、`AgentMessagePanel` | 无后端路由，无前端主路由 | `agents.py` 只有注释；前端组件为占位 | 不得作为功能卖点或测试对象 | 不影响 8 月决赛，禁止实现 | 不进入 M4-M7 |
| 可视化智能体/数据结构可视化 | 仅文档规划 | `docs/数据结构与算法可视化智能体教学功能规划.md`、`backend/app/api/v1/endpoints/visualization.py` | `visualization.py` | 无注册路由 | `visualization.py` 只有注释 | 规划先于实现 | 不影响 8 月决赛 | 不进入测试范围 |

## 无法确认项

| 项 | 无法确认原因 | 现有证据 | 建议 |
|---|---|---|---|
| 真实 LLM 答复质量、准确率 | 本阶段禁止调用真实付费 LLM；现有代码只证明调用链存在 | `llm_client` 多 provider，service 中多处调用 `llm_client.chat` | M4-M7 只验证流程和解析，不声明准确率 |
| 真实 TTS 音质和时长准确性 | 本阶段禁止调用真实 TTS；`MockTTSClient` 可证明隔离路径 | `tts_client.py:444` `MockTTSClient` | 只验证音频 URL、状态、字段更新 |
| 真实讯飞 PPT 生成质量 | 本阶段禁止调用真实 PPT 服务 | `ppt_generation_service.py:37-463` 外部 httpx API | Fake client 验证业务流程 |
| 真实数字人生成质量 | 本阶段禁止调用数字人服务 | `digital_human_client.py:38-206` | Fake client 验证任务状态机 |
| 旧比赛 PDF 中承诺功能的完整度 | PDF 和申报材料不是实现证据 | 实际代码审计为准 | 只在差距分析中引用，不作为完成证据 |

## 批次0 API契约缺口决策记录（2026-07-25）

### logout 接口缺口

- **现状**：后端 `user.py` 无 `POST /api/v1/user/logout` 端点；认证为无状态 JWT（`security.py` 无 token 黑名单/会话存储）。
- **前端**：`api/user.js` 曾声明 `logout()` 指向 `/user/logout`，但全前端无任何组件调用（死导出）。
- **决策**：**移除前端死调用**。前端退出时仅清除本地 token 与登录状态（`PrimaryNav.vue`、`UserInfoCard.vue` 等已如此实现）。不补后端假端点——无状态 JWT 下 logout 端点无法提供真实撤销语义，补端点会造成"看起来完成"的假象（违反 AGENTS.md §3.2.4）。
- **证据**：`frontend/src/api/user.js` 已移除 `logout()` 并保留决策注释；全前端 grep `api.user.logout` 返回零匹配。

### 聊天记录删除接口缺口

- **现状**：后端 `chat.py` 仅有 5 个端点（history/messages/ask/create/quiz），无 DELETE 端点。
- **前端**：`api/chat.js` 曾声明 `deleteChatRecord(chatId, userId)` 指向 `DELETE /chat/{chatId}`，但全前端无任何组件调用（死导出）。
- **决策**：**移除前端死调用**。若后续需聊天删除能力，应先在后端补端点再恢复前端调用。
- **证据**：`frontend/src/api/chat.js` 已移除 `deleteChatRecord()` 并保留决策注释；全前端 grep `deleteChatRecord` 返回零匹配。
### 2026-07-28 受控备课 Agent 补充审计

- **现状**：新增 `POST /api/v1/course-editor/course/{course_id}/controlled-prep/run`，依次执行 `EvidenceSegmenter -> OutlinePlanner -> ScriptWriter -> EvidenceVerifier -> PatchCompiler`。
- **结构化约束**：`backend/app/schemas/controlled_prep.py` 使用 Pydantic `extra="forbid"`、字段约束、`model_json_schema()` 和严格 JSON 校验；LLM 失败只做有限次局部重试。
- **证据边界**：API 只接受当前课程的 ACTIVE `CourseEvidenceRecord` 或 CONFIRMED `EvidenceSpan`；未知、跨课程、失效 Evidence 直接拒绝，LLM 输出中的 Evidence ID 也会再次校验。
- **审核边界**：PatchCompiler 只创建 `PatchProposal`/`PatchProposalOperation`，不直接写课程树或讲稿；教师接受提案时才进入草稿版本，锁定节点仍拒绝覆盖。
- **局部性**：`candidate_id` 可只重跑单个 knowledge point；每个知识点生成独立 `TeachingScriptNodeDraft`，拥有自己的 Evidence、课程定位、前置知识和风格配置。
- **验证**：`backend/tests/test_controlled_prep_workflow.py` 使用 Fake LLM 验证 schema、Evidence 拒绝和局部 Proposal；这不代表真实 LLM 质量或线上效果。

### 2026-07-31 助教智能体交互与运行时补充审计

- **全量一键动作**：`一键整理结构` 与 `一键优化讲解` 已使用
  `POST /api/v1/course-editor/course/{course_id}/prep-agent/batch-actions`。
  两者覆盖最新草稿中全部未锁定的对应节点，完整规划后在单个事务中直接应用，
  并保存 `accepted` PatchProposal 审计记录，不进入待审批列表；节点在规划期间
  被锁定或删除时整批回滚。
- **响应路径**：讲稿与结构批量规划均按最多 20 个节点分块，最多并行 3 个 LLM
  请求；批量动作只改写输入草稿字段，不重复检索或向每个分块发送同一份证据文本。
  普通单节点自然语言指令仍执行课程 Evidence 检索并生成 `pending` Proposal。
- **Runtime/Port**：Incremental Prep endpoint 优先经过
  `AgentGateway -> BaseAgentRuntime -> IncrementalPrepPort -> Provider -> Service`，
  Runtime 会把 `meta.errors` 提升为失败状态；Prep 没有 LLM-facing Tools，
  依赖是 Workflow 确定性调用的 Ports/Providers。
- **上下文现状**：前端聊天消息只保存在当前组件内存，刷新后不恢复。此行为避免
  持久化原始问题、完整回答和 LLM trace，但尚未实现仅保存会话摘要的连续体验。
- **可观测性缺口**：Bootstrap 当前仍注入 `NullAgentRunStorePort` 与
  `NullAgentRunEventPort`，没有 AgentRun/Event 数据模型、SQL Provider 或 SSE
  端点。因此目前可证明同步请求与 PatchProposal 审计闭环，不能声明运行事件持久化、
  断线续传或阶段恢复已完成。
- **验证**：Fake LLM/本地数据库覆盖全量目标、锁定排除、事务回滚、无 pending
  提案和 Runtime 错误映射；未调用真实付费 LLM，不能据此声明真实模型响应耗时或质量。

### 2026-07-31 助教智能体课程级优化与响应审计

- **批量编辑上下文**：`一键整理结构` 与 `一键优化讲解` 不再把课程拆成独立的
  20 节点 LLM 分块。每次动作向模型提供当前草稿中全部未锁定的目录层级
  （`id/parent_id/type/order/title`）和完整原始讲稿（`content/style`），并要求每个
  允许目标恰好返回一项修改；任何漏项、重复、无效项或超过 500 项硬上限都会在写库前
  失败，不会部分应用。锁定目录和讲稿不会发送给模型。
- **课程结构语义**：结构整理 Prompt 明确要求将图号、表号、页码、OCR 碎片和
  `a）/b）/c）` 图注改为真正的教学概念；可根据可见层级判断命名是否合适，但仍不能
  新增、删除、移动或重设父子关系。错误层级只能通过教师手动结构编辑纠正。
- **TTS 讲解语义**：讲稿优化 Prompt 把全套未锁定讲稿视作连续课程讲解，要求自然
  短句、清晰停顿、段间承接、术语先解释后列举，并避免朗读图号和 OCR 片段；它不改变
  课程事实。
- **前端反馈与互斥**：建设工作台共享 `batchRun` 与 `agentMessages`。一键动作开始时
  自动打开助教智能体面板并立即显示处理气泡，完成或失败后原地更新该气泡；运行期间禁用
  结构、讲稿、PPT 映射和面板单点智能优化。
- **服务端互斥**：`course_build_editor.py` 以课程 ID 维护本地进程锁；同一课程已有
  一键动作时，第二个一键动作或自然语言智能修改返回 `409 PREP_AGENT_BUSY`。这是本地
  Demo 的单进程保护，不是多 worker 分布式锁。
- **检索现状**：Prep 单点修改实际使用课程范围内的 SQL 文本包含查询和词项计分，最多
  返回 8 个候选原文块及其 CONFIRMED EvidenceSpan；它**没有**接入 BM25、BERT/
  cross-encoder 重排或 LanceDB 向量检索。项目内的 BM25/BGE/RRF 研究演示和已发布
  课程的 LanceDB 检索不能直接复用于不断变化的草稿，否则会读取过期 release 边界。
- **响应判断**：单点慢的主要来源是同步 LLM 调用、严格 JSON 校验后可能的一次修复重试，
  以及当前 Incremental Runtime 的 120 秒总超时；不是上述不存在的三段检索。批量操作
  改为一次课程级调用后，减少了多请求调度开销，但长课程的完整上下文和输出仍会随课程
  规模增长。自动化验证仅使用 Fake LLM，未测量真实付费模型延迟。

### 2026-07-29 课程创建 P0–P5 路由迁移与发布门禁审计

| 旧入口/行为 | 当前真实入口与迁移语义 | 验证证据 |
|---|---|---|
| 上传第一份文件时隐式创建课程：`POST /api/v1/document/course-imports` | 新建课程与上传材料拆分为 `POST /api/v1/courses`、`POST /api/v1/courses/{course_id}/materials`；旧入口仅保留兼容 façade，不再作为前端主流程。 | `backend/app/api/v1/endpoints/course_creation.py`、`course_material_upload_service.py`、`test_course_import_task.py` |
| `POST /api/v1/course-build/course/{id}/materials` 直接写材料 | 已明确弃用并返回 410；真实多文件上传由 `/api/v1/courses/{course_id}/materials` 承担，防止两套材料/解析任务写入逻辑并存。 | `backend/app/api/v1/endpoints/course_build.py`、`test_course_build.py` 中旧路径断言需按迁移语义更新 |
| 每份材料完成解析后直接生成课程草稿 | 解析完成仅尝试形成 `CourseCorpusSnapshot`，再创建独立 `CourseDraftBuildTask`；同一 corpus 只构建一次，后续变更进入 Proposal。 | `backend/app/platform/tasks/handlers.py`、`course_corpus_service.py`、`test_course_corpus_orchestration.py` |
| 学生读取当前最新解析/RAG 索引 | 学生课程内容、播放与 RAG 先解析 active `CourseRelease`，再读取其冻结的目录、讲稿、PPT、媒体和 release retrieval snapshot；教师候选检索保持独立。 | `course_build_service.py`、`canonical_document_ir.py`、`player.py`、`test_course_release_freeze.py` |
| Warning 默认可发布 | 门禁运行只要有 Warning 即不通过；教师必须使用 `POST /api/v1/course-build/course/{id}/validate/{gate_run_id}/confirm-warnings` 对该次结果留下原因，且 BLOCKER/ERROR 永不可绕过。 | `course_build_service.py`、`course_build.py`、迁移 `0026_quality_gate_warning_override.py` |

P5 门禁当前覆盖：核心知识点 Evidence/Block 覆盖、空讲稿、课程树与图谱孤立节点、前置关系循环、解析警告、未审核 Proposal、PPT 映射缺失/低置信度、冻结学生 RAG chunk 可用性、已发布图谱快照存在性，以及 outline/script/corpus/build task 血缘一致性。发布会自动冻结当前已发布的活动图谱，不能由调用方指定其他图谱版本。自动化验收仅使用 Fake/本地依赖，不调用真实付费 LLM、TTS、PPT 或数字人服务。

## 课程首轮智能整理与展示契约（2026-07-29）

- **事实来源**：每份材料仍先生成 DocumentIR/DocumentBlock；课程结构、基础讲稿和课程级图谱候选只在 `CourseCorpusSnapshot` 完整后生成。解析块、图注和 OCR 碎片不再经旧的规则构建器直接作为教师可见目录。
- **首轮行为**：`course_draft_build` 的 `initial` 分支调用受控备课工作流（证据分段 → 课程树规划 → 讲稿 → 证据校验）。若 LLM 或结构校验失败，任务保持失败并可重试，不写入伪成功草稿；后续已有草稿时仍只创建 Proposal。
- **2026-08-09 材料整理修复**：旧实现只在讲稿阶段拆批，`segment_evidence` 仍单次调用；同时把 `source_text` 与 `evidence[].text` 重复发送，初始结构化请求未显式关闭思考模式。现实现按页合并 OCR/解析碎块，Map 批次受正文/序列化载荷双上限约束并发 2，跨批结果经层级 Reduce；首轮结构化请求附 JSON Schema，初始 Prep 全部关闭思考模式并使用阶段级 token 上限。合成的证据单元 ID 在持久化前展开为真实块 ID，避免污染 `source_block_refs` 和 PPT 页码。
- **验证证据（2026-08-09）**：本地只读语料审计的 21,316 个块形成 591 个页内证据单元，577,863 字全部纳入；最大单元 2,168 字，形成 25 个 Map 批次，单批正文最大 23,966 字、完整载荷最大 30,279 字。`test_controlled_prep_workflow.py` 覆盖 21,275 碎块合成语料、Map/Reduce、并发限制、截断二分、JSON Schema 和原始块 ID 展开；未调用真实付费模型。
- **2026-08-09 Reduce schema 复测修复**：提交 `3f9a5cbb` 部署后的真实页面复测证明截断自动拆分生效，但 Reduce 第 3 个 segment 连续两次为 `examples`、`exercises` 各返回 15 项，超过正式 schema 的 10 项上限并安全终止，且没有写入任何新版本。现将可安全归一化限制在 Reduce 的两个建议列表：模型约束和 JSON Schema 仍声明最多 10 项，解析时保留首次出现顺序、去空去重并裁剪到 10 项，再转换为严格领域结果；证据 ID、segment 数量、标题/主题及额外字段继续严格拒绝。
- **回归证据（2026-08-09）**：`test_reduce_stably_deduplicates_and_caps_examples_and_exercises` 覆盖直接返回 15 项及重复项；`test_reduce_repair_response_with_fifteen_items_is_safely_normalized` 覆盖首次因其他结构错误进入自动修复、修复响应仍返回 15 项的 Provider 路径。测试使用本地 Fake Gateway，不调用真实付费模型。
- **展示契约**：`outline_node_id` 是内部稳定关联键；目录、讲稿和 PPT 映射统一从当前 `outline_version_id` 的课程树计算 `display_number`、`display_label` 与面包屑。教师端不展示哈希 ID 作为标题。
- **PPT 映射**：映射只使用 `primary_courseware` 的 PPT 页证据；教材/PDF 证据不会污染 PPT 页码。被删除的叶节点映射标为 `stale`，有子节点的目录不允许直接删除。
- **验证证据**：`tests/test_course_corpus_orchestration.py` 以 Fake Workflow 验证章节—小节—知识点、基础讲稿和主 PPT 页码隔离；`tests/test_course_build_presentation.py` 验证讲稿与课程结构共享编号标签。测试未调用真实 LLM。

## 可视化 PPT 映射工作台（2026-08-01）

- **教师工作区**：`GET /api/v1/course-editor/course/{course_id}/ppt-mapping/workspace` 按 `material_version_id` 分页返回已解析页的 OCR 摘要和受课程权限保护的页图内容路由；浏览器不接收对象存储键或 PPT 原文件。页图采用当前材料版本，不能把课程级发布 `ppt-manifest/v1` 误认作多份上传 PPT 的共同页码空间。
- **保存语义**：建设页在客户端暂存多条教师选页，`PUT /ppt-mapping` 一次性校验并写入不连续 `page_refs`。手工保存默认设置 `teacher_locked=true`，之后的智能匹配不得覆盖；旧的单节点 PATCH 仅保留兼容。
- **智能匹配与兜底**：`POST /ppt-mapping/match` 提供全部未锁定、当前知识点、所选页面三种受限范围。OCR 是服务端确定性 `DocumentOcrPort` 依赖，不是 LLM-facing Tool；模型只接收指定页的 OCR 文本、知识点和已有映射。当没有可信候选时返回可操作的 `no_reliable_match`，提示教师直接选页或缩小范围重新匹配，而不是伪装为系统失败。

### 原始课件视觉源与学习播放器（2026-08-01）

- **视觉真源**：`ppt_slide_render_service.py` 将教师上传的每个 `SourceMaterialVersion` 的 PPT/PPTX 经 LibreOffice 渲染为 `EvidenceRenderAsset(asset_type=ppt_slide_image)`。对象键带内容哈希，OCR 或证据页图不能覆盖、替换或伪装为教师课件。
- **建设页降级**：映射工作台只读取 `ppt_slide_image`；即使 OCR 尚未完成，也会按小页窗直接生成原始幻灯片供教师选择。OCR 仅在页图下方作为摘要与智能匹配证据。原课件渲染失败时明确提示可重试，不能回退为文字重绘图片。
- **学生播放**：`ppt-manifest/v1` 在保持顶层 `pages` 兼容的同时增加 `decks` 与 `primary_material_version_id`。发布时映射 Cue 冻结 `material_version_id`，播放器据此切换到对应教师课件；主舞台显示原始幻灯片，数字人仅为右下角叠层，音频与字幕用同一个时钟驱动。
- **验证**：`test_course_prep_agent_batch_endpoint.py` 覆盖批量教师保存、所选页 scope 和无匹配恢复；`test_ppt_mapping_optimization.py` 覆盖 scoped OCR/节点输入。均使用 Fake LLM/本地数据库，未调用付费模型。

## 助教智能体动作路由与课程整理修正（2026-08-01）

### 发布质量门禁的重复材料与血缘兼容（2026-08-02）

- `needs_review` 是可进入语料快照的终态解析结果，不再同时报告为“未完成解析”；其风险仍以显式 Warning 要求教师确认。
- 历史重复上传经内容哈希去重后，若旧 outline/script/build task 与当前 corpus 的材料哈希集合完全一致，质量门禁和发布服务将接受该兼容血缘；任一材料哈希变化仍会阻断发布。
- 旧课程若已持久化 materials、outline 或 scripts 产物但七步状态行尚未回填，质量门禁把它标为已启动的信息项，不把这类记录缺口误报为发布 ERROR。

### 发布前检查与教师确认（2026-08-02）

- 面向教师的流程使用“草稿 / 发布前检查 / 正式发布”称呼；旧 API 路径与数据库字段保留兼容，不作为界面文案。
- `BLOCKER` 仍必须先处理；`ERROR` 与 `WARNING` 可由拥有 `course.publish` 权限的教师填写确认理由后正式发布。确认绑定到具体检查记录，并保存确认人、原因、时间。
- `CourseRelease` 保存检查结果快照与发布时的问题清单，便于回看当时教师确认的内容；不能通过仅改前端按钮绕过服务端检查。

- **已纠正的现状描述**：课程结构“一键整理”不再仅限于标题替换；新的受控动作可对未锁定草稿节点提出标题替换、移动、同级排序和删除。它不新增或拆分节点，且删除父节点前必须先迁移所有子节点。
- **统一入口**：按钮和教师自然语言都使用 `PrepAction`。自然语言只负责确定动作和保留教师补充要求，随后进入与按钮相同的 Port/Workflow；缺少单节点范围时返回澄清，不猜测节点。
- **聊天授权语义**：聊天中明确说“一键/全部/全量/批量”整理结构或优化讲解脚本，视为教师对全量动作的明确授权，复用按钮的批量锁、原子应用和 `accepted` 审计提案；其他单节点聊天动作仍先生成 `pending` Proposal。
- **讲解脚本性能语义**：一键优化讲解脚本按每组 5 个、最多 3 组并发调用，而不是将整门课脚本交给单次模型调用；任一组未完整返回时整批不应用。
- **证据现状**：单节点与脚本动作优先读取课程内确认 `EvidenceSpan`，有 Active Bundle 时还会通过 `ActiveBundleCourseRetrievalPort` 获取课程范围内的向量证据。向量检索不可用会降级为课程内词法检索，不伪造引用。
- **应用安全**：Proposal 接受前比对 `before` 快照；结构变更先检测最终树是否成环，再两阶段更新排序并在同一事务内写入。该验证仅使用 Fake LLM/本地数据库，尚未测量真实付费模型的质量或延迟。
### 2026-08-02 备课 Agent 批量结构整理 P0/P1
- 批量结构整理默认启用稀疏 `StructurePlan`，输入只携带 node_id/title/parent/level/order/locked 等树快照；空操作返回 `no_change`，服务端继续执行锁定节点、环、删除后代、顺序冲突、标题和 evidence_refs 校验，并使用现有原子应用。
- Prep Runtime 已接入 SQL-backed `AgentRunStorePort`、`AgentRunEventPort` 和 LLM 诊断 sink。批量响应携带 `run_id`/`trace_id`；`GET /api/v1/course-editor/course/{course_id}/prep-agent/runs/{run_id}/diagnostic` 仅返回脱敏状态、token/长度/finish_reason/截断和字段错误，不返回 prompt 或模型原文。
- 新增迁移 `0036_agent_run_diagnostics`；自动化验证使用本地数据库和 Fake LLM，未调用真实付费模型。

### 2026-08-10 大教材课程骨架恢复复核

- 代码证据：`ControlledPrepWorkflow.plan_outline` 现对重复候选 ID、父子类型、section / knowledge point / 总节点三类硬上限做服务端校验；约束不再只存在于 Prompt。
- 已修复：紧凑大纲重试再次返回 `structured_output_invalid` 时转入确定性兜底，不再直接终止；紧凑重试同步收紧 `target_*` 与 `max_*`；目录回填受剩余节点预算限制。
- 兜底语义：`_compile_deterministic_skeleton` 生成 `chapter -> section -> knowledge_point`，32 个证据分段的本地回归结果为 1 / 10 / 16（27 个节点）。该结果证明结构与预算可收敛，不代表真实付费模型对教材内容的生成质量已经验收。
