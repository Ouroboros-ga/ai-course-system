# 路由契约基线

> 2026-08-12 增补：`PUT /api/v1/course-access/courses/{course_id}/experiment-platform` 是教师设置页的窄权限实验平台开关，体为 `{ "enabled": boolean }`，要求 `course.edit`，只原子同步 `experiment` 与 `coding_sandbox`。前端客户端为 `updateCodeSandboxExperimentPlatform`；全量 `PUT .../capabilities` 仍要求 `permission.manage`，不作为教师设置页的替代。`frontend/src/api/__tests__/apiContracts.test.cjs` 同时锁定前端 URL、后端注册与权限语义。

> 2026-08-09 增补：`POST /api/v1/user/login` 的既有 `username` 字段可传用户名或数字用户 ID；`PATCH /api/v1/user/me/profile` 与 `PATCH /api/v1/admin/users/{user_id}` 均以 `username` 作为唯一账户名称字段，不再暴露或写入 `nickname`。用户名更新保持数值 ID 不变，密码更新仍要求原密码（管理员重置除外）。

> 2026-08-07 增补：平台管理员路由为 `/api/v1/admin/integrations` 和 `/api/v1/admin/users`；外部泛雅·超星 AI 示例协议以可移除的 `/api/v1/compat/*` 参考兼容包提供 `code/msg/data/requestId` 适配外壳。该包由主应用可选发现，并且只在挂载时登记独立签名校验前缀；目录缺失时不挂载、不留该签名豁免。既有 `/api/v1/platform/syncCourse`、`syncUser` 不重复注册，仍作为迁移兼容入口。

更新时间：2026-07-08

## 目的

本文件记录 M4A 对当前真实路由状态的锁定方式。M4A 不修复重复路由和前后端接口不一致，只把现状变成可验证证据，供 M4B-M7 建立回归测试。

## 契约测试文件

路由契约测试位于：

```text
backend/tests/test_m4a_route_contract.py
```

测试通过 FastAPI `app.routes` 和 `app.openapi()` 记录每条有效 `APIRoute` 的。FastAPI 0.141+ 会把 `include_router` 保存为惰性的 `_IncludedRouter`；测试辅助函数会展开该注册对象，因此测试继续反映实际可匹配的完整路径，而不修改路由运行行为：

- HTTP method
- 完整 path
- endpoint 函数名
- endpoint module，作为 router 来源线索
- 注册顺序 index
- 是否出现在 OpenAPI

## 已锁定的关键路由事实

| 路由事实 | 测试 | 预期 |
|---|---|---|
| 路由记录字段完整 | `test_route_contract_records_required_fields` | 每行包含 `index/method/path/endpoint/module/in_openapi` |
| catch-all 不进入 OpenAPI | `test_route_contract_locks_known_non_openapi_catch_all` | `/{path:path}` 的 DELETE/GET/PATCH/POST/PUT 均为 `app.main.catch_all` 且 `in_openapi=False` |
| `document.router` 挂载到 `/api/v1/document` | `test_route_contract_locks_document_router_double_mount` | `/api/v1/document/upload` 存在 |
| `document.router` 再次挂载到 `/api/v1/chat/file` | `test_route_contract_locks_document_router_double_mount` | `/api/v1/chat/file/upload` 存在 |
| `document.py` 的课程路由注册次数 | `test_route_contract_locks_known_duplicate_document_routes` | `GET /api/v1/document/courses` 数量为 1 |
| `document.py` 的保存路由注册次数 | `test_route_contract_locks_known_duplicate_document_routes` | `POST /api/v1/document/course/{course_id}/save` 数量为 1 |
| 当前视频生成真实前缀 | `test_route_contract_locks_video_gen_and_known_absent_old_routes` | `/api/v1/video-gen/health` 存在 |
| 旧测试路径不是当前真实路径 | `test_route_contract_locks_video_gen_and_known_absent_old_routes` | `/api/v1/video-generation/health` 不存在 |
| 前端 `chat.js` 的 `/chat/${chatId}` 风险 | `test_route_contract_locks_frontend_chat_path_mismatch` | `/api/v1/chat/{chat_id}` 不存在，`/api/v1/chat/messages/{chat_id}` 存在 |

## 运行时路由清单生成方式

测试中的 `_route_rows(app)` 生成如下结构：

```python
{
    "index": index,
    "method": method,
    "path": route.path,
    "endpoint": route.endpoint.__name__,
    "module": route.endpoint.__module__,
    "in_openapi": route.path in openapi_paths,
}
```

M4A 暂不导出运行时快照文件，避免引入额外生成物；如后续需要，可在 M4B/M5 将 `_route_rows(app)` 输出为 JSON/Markdown 快照并纳入评审。

## 代码证据来源

主入口注册依据：`backend/app/main.py`

- `/api/v1/document`：`app.include_router(document.router, prefix="/api/v1/document")`
- `/api/v1/chat/file`：`app.include_router(document.router, prefix="/api/v1/chat/file")`
- `/api/v1/video-gen`：`app.include_router(video_generation.router, prefix="/api/v1/video-gen")`
- `/{path:path}`：`catch_all` 兜底路由，不进入 OpenAPI

路由去重依据：`backend/app/api/v1/endpoints/document.py` 与契约测试

- `GET /api/v1/document/courses` 只注册一次
- `POST /api/v1/document/course/{course_id}/save` 只注册一次

前端接口不一致依据：

- `frontend/src/api/chat.js` 使用 `/chat/${chatId}`
- `backend/app/api/v1/endpoints/chat.py` 定义 `/messages/{chat_id}`，未见 `/chat/{chat_id}` 对应 endpoint

## 已验证结果

```text
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_m4a_route_contract.py -q
```

2026-08-07 使用当前锁定的 FastAPI 0.141.1 已验证：

```text
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_m4a_route_contract.py -q
7 passed, 4 warnings in 14.89s
```
