# M0 收尾 + M1 模式真实化验收记录（2026-09-05）

> **执行**：编码智能体（部署经用户授权："再做一次检查，授权部署"）
> **结论**：**通过**——M0-B1/B2 服务器生效、M1-B1~B6 + M1-F1~F3 全部落地；
> 线上实证双模式工具面、mode 路由上下文连续、SSE error 契约、结构化 items。
> 敏感信息声明：不含 Key/DSN/JWT/用户原文。

---

## 0. 部署基线

- 提交：`f10cb569`（M0 收尾 + M1 代码）← `e49e9a61`（P2 计划与 M0-B1 记录归档）
- Release：`/opt/smartcarb/current → e49e9a61`（backend 就绪第 6 次探测）
- Nexus Runtime：`agent.py`/`main.py` 同步 + 重启；`persistence=postgres`
- 部署前检查：Runtime 52/52、Backend 33/33（nexus_proxy + platform_permissions）、
  前端契约 89/89、oxlint 0 error、build ✓

## 1. M0 收尾

- **M0-B1（D1 工具收敛）服务器生效**：health `tool_surface` 按模式上报——
  `research = [plan_reproduction, read_file, run_reproduction, search_arxiv_papers, web_search]`、
  `general = [read_file, web_search]`；`write_file/edit_file/delete/glob/grep/execute/task`
  全部消失（回归锁定：`tests/test_agent_tools.py` 5 条）。
- **M0-B2（D3）**：`.env.example` 全变量 `NEXUS_` 前缀对齐，补
  `NEXUS_REPRO_WORKER_TOKEN`/`NEXUS_API_KEY` 占位；契约测试 3 条锁死
  （每个 example 变量必须被 `Settings` 识别——pydantic `extra="ignore"` 对
  未知变量静默丢弃，属最隐蔽故障类）。

## 2. M1 线上实证（全部走 `https://zsitai.xyz`）

| 项 | 用例 | 实测 |
|---|---|---|
| M1-B1（D2） | POST /chat 带 `mode`/`context` | upstream body 含两字段（MockTransport 契约测试 23/23 之一）；Runtime 白名单归一 `nexus_*` 词形 |
| M1-B2 双 Profile | general / research 实例 | health 双工具面见上；**同 session 跨模式上下文连续**：`m1-accept` 会话先 general 提问（过拟合定义），后 research 追问（"结合刚才的话题"），模型正确引用上一轮话题并调用 arXiv+web 双工具，tool_events 4 条 |
| M1-B3（D5） | Agent 循环异常 | 流尾 `event: error`（code+message），无 `done`（单测注入 `_ExplodingAgent` 断言互斥；本测试过程本身先因缺 import 被 error 事件如实上报——机制自证） |
| M1-B4（D4） | 真实 web_search 流式 | `tool_result` 带 `items`（8 条，单条 300 字符截断），`content` 2510 字符完整合法 JSON——不再腰斩 |
| M1-B5（D6） | todo 规则 | 核实 deepagents 0.7.12 **无** todo/planning 中间件（全包 grep 仅 docstring 提及）→ 按"诚实性优先"删除 SYSTEM_PROMPT 规则 4，附缘由注释 |
| M1-B6 | 取消语义 | 消费者提前 `aclose()` → 生成器取消，不补发假 `done`（`CancelledError` re-raise）；Stop 按钮（前端 AbortController）已存在未改 |
| M1-F1 | error 分支 | `handleEvent` 增 `error` 事件 → `turn.failure = code：message`，流关闭后 finally 复位流式状态 |
| M1-F2 | Stop | 已有实现核实通过（SfxButton v-if streaming + abort → "已停止回答"） |
| M1-F3 | 同源契约 | `NEXUS_MODE_CONFIG.tools` 与 Runtime `RESEARCH_ONLY_TOOLS` 契约断言入 apiContracts（89/89） |

历史投影（`m1-accept/messages`）：`[(user,12),(assistant,103),(user,25),(assistant,1485)]`
——跨模式两轮对话完整可恢复。

## 3. 已知边界（如实记录）

1. **error 事件未做线上人工触发**（不能为验收故意打挂生产链路）——以单测
   注入失败 agent 断言流尾 error 事件为准；线上 error 语义待首次真实故障
   日志回填。
2. **前端浏览器手工点击**（会话切换/Stop/error 展示）未在本验收执行——
   API 链路全部实测，UI 交互靠契约测试 + build 保证；演示前建议手工过一遍。
3. M1-B2 的"同 session 切模式上下文连续"在 memory 降级模式下由共享
   `_fallback_saver` 保证（单实例），PG 模式天然共享同一 saver。

## 4. 回退

- `git revert f10cb569`（Runtime+proxy+前端单提交）；服务器侧
  `agent.py`/`main.py` 从 release `5eeab092` 恢复。数据零迁移（本轮无表变更）。
