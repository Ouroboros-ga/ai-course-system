# M2 知识接入验收记录（2026-09-05）

> **执行**：编码智能体（部署经用户授权）
> **结论**：**通过**——内部检索端点四态实证、Runtime 双工具线上真实触发、
> 权限拒绝路径端到端诚实、CS 权威条目正确引用。
> 敏感信息声明：不含令牌/DSN/JWT 明文；课程证据仅摘要素样。

---

## 0. 部署基线

- 提交：`cfda63ef`（M2 代码）；release `/opt/smartcarb/current → cfda63ef`
- 新配置：`NEXUS_INTERNAL_TOKEN`（`openssl rand -hex 32`，服务器侧生成，
  写入 `backend.env` 与 `nexus.env`（600），未出服务器、未入日志）
- Nexus Runtime：6 文件同步 + 重启；`persistence=postgres`；health tool_surface：
  - research = read_file + web/arxiv/course/cs/plan/run（6 工具）
  - general = read_file + web/course/cs（4 工具）
- 部署前测试：Runtime 61/61、Backend 40/40、契约 89/89、oxlint 0、build ✓

## 1. 内部检索端点（M2-B1，`/api/v1/nexus-internal/*`）四态实证

服务器侧 curl（令牌从 env 文件读入，不回显）：

| 场景 | 结果 |
|---|---|
| 令牌未配置 | 503 `NEXUS_INTERNAL_NOT_CONFIGURED`（fail-closed） |
| 错误令牌 | 401 `NEXUS_INTERNAL_UNAUTHORIZED` |
| 无课程权限用户（stu102）→ course-evidence | 403 `NEXUS_INTERNAL_FORBIDDEN` |
| 有权限用户（user 9，owner/学生成员）→ course_id=15 | **200**：5 条真实证据（`ev_*`/`doc_*`/page 17/代码文本/`kn_*`/`cit_*`/bundle_id 完整链） |
| CS 知识库（任意登录用户） | **200**：真实条目（哈希表：定义/key_points/来源《算法导论》/所属课程） |

> 环境事实澄清：库内唯一课程真实 id=15（非 1）；course_memberships 仅
> user 9（STUDENT）与 user 1（OWNER）——stu101/102 为演示账号但未入课，
> 因此"有权限"路径用 user 9 验证；stu101 的端到端路径走了诚实拒绝（见 §2）。
> 能力开关 `course_capabilities.knowledge_graph=t`（course 15）。

## 2. Runtime 工具端到端（M2-B2/B3，走 `https://zsitai.xyz`）

**拒绝路径（stu101，context.course_id=15，general 模式）**：
`search_course_materials` → Backend 403 → 工具返回 `COURSE_ACCESS_DENIED`
→ 模型**如实告知**"遇到权限问题，无法访问课件内容"，零编造，并给出准确
恢复建议（确认绑定/联系管理员）。tool_events 2 条（call+result）。

**成功路径（CS 检索，无需课程绑定）**：
`search_cs_knowledge("哈希表")` → 200 → 模型整理出定义/复杂度/冲突处理/
典型实例，并**正确引用权威来源**（"《算法导论（原书第 3 版）》第 11 章
散列表；所属课程：数据结构与算法"）。

**合流红线（M2-B3）**：SYSTEM_PROMPT 规则 4 落地——按相关性取舍、无关不
强行引用、禁止加权/打分/合成；工具 description 同样声明边界。

## 3. 前端（M2-F1/F2）

- `nexusCapabilities.js`：course_materials/cs_knowledge → `ready`（集成口径
  如实注明"关键词检索非向量"）——**翻转时序说明**：状态随本部署发布，线上
  复测（本文档 §1/§2）随即通过；若复测失败将回退翻转。
- 信息源面板新增分组：`课程资料 · N（经核实）`（text + resource + page +
  node）、`CS 知识库 · N（权威来源）`（name + source + course）；
  web 组优先消费 M1-B4 结构化 `items`，旧会话回退 content 解析；
  unparsable 哨兵文案更新。
- runTurn 补传 `courseId` 进请求 `context`（course_id 链路的前端源头）。
- 契约 89/89、oxlint 0 error、build ✓；浏览器手工点击建议演示前过一遍。

## 4. 已知边界

1. **课程证据冷启动慢**：首次 `search_evidence`（lance 首加载）请求耗时明显
   高于后续（首测一次 SSH 超时，复测 ~正常返回）。建议后续给内部端点加
   20-30s 超时预算说明或预热；不阻塞验收。
2. stu101 等演示账号未入课是**数据事实**（memberships 只有 user 9/1）——
   演示脚本若要展示"课程资料成功检索"，需用已入课账号或先走入课流程；
   本验收不改业务数据。
3. 三源同轮合流（course+cs+web 同一提问）未在本验收强制构造——工具面与
   prompt 已就位，模型按相关性自行取舍；演示时可按 Phase 12 脚本触发。

## 5. 回退

- `git revert cfda63ef`；服务器：nexus 6 文件从 release `e49e9a61` 恢复、
  backend 从上一 release 恢复；删除 `NEXUS_INTERNAL_TOKEN` 两行 env + 重启
  即回到 M1 稳态（nexus-internal 端点自动 fail-closed 503，无数据残留）。
