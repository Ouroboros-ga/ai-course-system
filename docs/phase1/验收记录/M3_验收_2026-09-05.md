# M3 Artifact 真实化验收记录（2026-09-05）

> **执行**：编码智能体（部署经用户授权）
> **结论**：**通过**——写入链（对象存储 + 元数据表）、下载链（JWT + owner
> 404 防枚举）、端到端产物生成（模型自主双源检索后落盘）全部线上实证。
> 敏感信息声明：不含令牌/DSN/JWT；产物内容仅摘要素样。

---

## 0. 部署基线

- 提交：`878906d0`（M3 代码）+ `d869424a`（热修：`_TABLE` 常量补回——部署时
  服务层 NameError 根因，热修 scp 至当前 release 后恢复正常，提交已同步推送）
- Release：`/opt/smartcarb/releases/878906d0`（backend 就绪第 7 次探测）
- Nexus Runtime：`main.py` + `tools/{artifact.py,__init__.py}` 同步重启；
  `persistence=postgres`；health tool_surface：research=8 工具 / general=5 工具
  （均含 write_artifact）
- 部署前测试：Runtime 66/66、Backend 32 通过+2 跳过（PG-only 涉表留线上）、
  契约 89/89、oxlint 0、build ✓

## 1. 写入链（M3-B1，服务器侧实证）

- `POST /api/v1/nexus-internal/artifacts`（user 43）→ 200：
  `artifact_id=773d989d72b6`、`object_key=nexus-artifacts/u43/773d989d72b6.md`、
  183 字节、sha256 完整；
- 元数据落库实证：`nexus_checkpoints.nexus_artifacts` 行
  `(773d989d72b6|43|markdown|183)`——PG-only 表在真实库幂等建表成功；
- 入参 fail-closed（本地测试锁定）：type 白名单/空标题/空内容/超 512KB → 422。

## 2. 下载链（M3-B2，走 `https://zsitai.xyz`，JWT 域）

| 场景 | 结果 |
|---|---|
| stu101（owner）列表 | 1 条（自己的产物） |
| stu101 下载 `773d989d72b6` | **200**，183 字节，内容与写入一致 |
| stu102 列表 | 空 |
| stu102 下载他人产物 | **404**（防枚举探测，按设计） |
| 未登录 | 401/403（本地测试锁定） |

## 3. 端到端产物生成（E2E，general 模式）

提问："把哈希表的定义和核心要点整理成 markdown 学习笔记并写入产物"→
模型自主执行 **search_cs_knowledge + search_course_materials 双源检索 →
write_artifact**（tool_events 8 条全链）→ 产物 `951839c926e6`
（哈希表学习笔记，4477 字节）落盘，回答内含下载路径。

产物内容核验（摘样）：定义/核心思想/复杂度/冲突处理齐备，来源引用
《算法导论》第 11 章，并**诚实注明**"本会话未绑定课程，故未引用具体课程
讲义；内容来自 CS 学科知识库权威条目"——合流红线（M2-B3）在真实模型
行为中生效。

## 4. 前端（M3-F1，随 release 部署）

- 消息流产物卡（图标+标题+类型/大小+下载 SfxButton）；
- 本机资料 real 模式接 `GET /nexus/artifacts` 服务器列表（逐项下载）；
- demo 模式保持本地统计不变；契约测试锁定两路由 + JWT 头 + D10 门控计数 7。
- 浏览器手工点击（产物卡下载交互）建议演示前过一遍——API 链路已全验。

## 5. 已知边界

1. **热修记录**：首次部署时服务层 `_TABLE` 常量缺失（提交遗漏）→ 写端点
   500；已热修（`d869424a`）并 scp 至当前 release。教训：服务层全局常量的
   删除型重构要整段复核，不能依赖零散 regex 替换。
2. 删除端点 P0 未做（产物 retention 另行评估，见计划 §7.0）。
3. `file_upload` 能力维持 `unwired`（上传通道未建，按能力三态诚实呈现）。

## 6. 回退

- `git revert 878906d0 d869424a`；服务器从 release `cfda63ef` 恢复；
  数据回退：`DROP TABLE nexus_checkpoints.nexus_artifacts` + 删除
  `nexus-artifacts/` 前缀对象（当前仅验收产物 2 项，均为演示数据）。
