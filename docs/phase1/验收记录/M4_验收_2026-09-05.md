# M4 复现体验闭环验收记录（2026-09-05）

> **执行**：编码智能体（部署经用户授权："授权部署测试"）
> **结论**：**通过（验收门达成）**——nanoGPT"提交→查询→完成→指标比较→
> 报告下载"完整链线上闭环；越权 404 双向实证；确定性判定 LLM 零参与。
> 敏感信息声明：不含令牌/DSN/JWT；报告内容摘样入档。

---

## 0. 部署基线

- 提交：`aa77ba41`（M4 代码）；release `/opt/smartcarb/releases/aa77ba41`
- 新配置：`backend.env` 增 `REPRO_WORKER_URL/REPRO_WORKER_TOKEN`（服务器侧
  从 `repro-worker.env` 派生写入，未回显未入档）
- Nexus Runtime：5 文件同步重启（main/artifact_client/repro_report/
  tools/reproduction/tools/artifact）；`persistence=postgres`；双模式工具面不变
- 部署前测试：Runtime 75/75、Backend 44 通过+7 跳过（PG-only 涉表留线上）、
  契约 89/89（含 repro 两路由 + D10 门控计数 9）、oxlint 0、build ✓

## 1. 验收门：nanoGPT 完整链（走 `https://zsitai.xyz`，stu101，research 模式）

| 环节 | 结果 |
|---|---|
| 提交 | chat"执行 nanoGPT 复现" → `plan_reproduction` + `run_reproduction` 成功，**job `c3e71851e513`**；归属登记 best-effort（提交诚实，LLM 摘要准确给出 Job ID/仓库/License） |
| 查询 | 发起人 `GET /nexus/repro/jobs/{id}` → 200（**status=succeeded、5 步、seed_used=true**；日志裁剪 ≤300 字符） |
| 越权 | stu102 查询同一 job → **404**；stu102 请求报告 → **404**（防枚举，双向） |
| 指标比较 | `POST .../report` → **verdict=PASS**（deterministic：val_loss **1.8857** ∈ 期望 1.88±0.06；train_loss_final 1.7648；LLM 零参与） |
| 报告 artifact | 两件落库：`1967f186388d`（report.md，2576B）+ `de5fa219b782`（report.json，3397B）；owner 下载 200 |

报告内容核验（摘样）：标题/结论行（"**结论：PASS**（由确定性指标比较生成，
**非 LLM 判定**）"）/ 仓库与 License（GitHub=MIT / 本地=MIT）/ 种子快照命中
（离线确定性）/ 期望指标来源声明（官方 README CPU 配置 + 容差说明）/ 指标
对比表 / 执行步骤（各步 exit 与耗时）/ 日志尾部——全部齐备。

## 2. 失败与边界语义

| 语义 | 覆盖 |
|---|---|
| 409 JOB_NOT_FINISHED | 单测覆盖（mock worker running 态 → 409）；线上未构造（本次 job 完成快于预期，不为验收故意拖挂） |
| 404 防枚举 | **线上实证**（未登记 job / 他人 job，查询与报告双向） |
| 轮询上限 | 前端 5s × 200 上限 + 单次失败不终止（代码路径）；本次 succeeded 提前命中终止条件 |
| 归属登记失败 | best-effort + `ownership_recorded` 诚实标注（单测覆盖登记调用） |

> 观察记录：本次 pip install 步骤仅 1.2s（此前 97.3s）——pip wheel 缓存命中
> （容器层/缓存目录复用），非异常；如实记录。

## 3. 前端（M4-F1/F2，随 release 部署）

- 复现运行状态卡：阶段流水（命令/exit/超时/耗时）+ 状态徽章 + 失败语义
  （code+detail）+ 判定徽章（PASS 绿/FAIL 红）+ 指标对比行 + "非 LLM 判定"
  标注；受控轮询（5s×200 上限，卸载清理）；
- 报告产物自动进 M3 产物卡（逐项下载）；本机资料计数含报告；
- `nexuslab_repro` → `ready`（integration 注明 Clean Verification 仍为 P2+，
  `reproducible=true` 不由此给出）。契约测试锁定两路由 + D10 门控计数 9。
- 浏览器手工点击（状态卡轮询过程视觉/报告下载交互）建议演示前过一遍。

## 4. 回退

- `git revert aa77ba41`；服务器：backend 从 release `878906d0` 恢复、nexus 5
  文件从同 release 恢复；数据回退：`DROP TABLE nexus_checkpoints.nexus_repro_jobs`
  + 删除报告 artifact 两行（`nexus_artifacts`）与对应对象（均为本次验收产物）。
