# M5（S3 下线 + Harness 压力）验收记录（2026-09-05）

> **执行**：编码智能体（部署与 AGENTS.md 地址变更均经用户明确授权："两个确认"）
> **结论**：**通过**——research 工作台代码下线、410 短路保留、全栈无回归；
> 压力套件 7/7；文档闭环（含 AGENTS.md 部署地址更新）。
> 敏感信息声明：不含令牌/DSN/JWT；worktree 对照细节仅含测试名与布尔结论。

---

## 0. 部署基线

- 提交：`8d868f76`（S3 下线 + 压力套件 + 规格翻转，-8100/+354 行）+
  `f1eb9b27`（AGENTS.md 部署地址更新，单独提交）
- Release：`/opt/smartcarb/releases/f1eb9b27`（backend 重启健康；Nexus
  Runtime 无代码变更，无需 scp）
- 部署前：Runtime 82/82；Backend nexus 相关 51+7 跳过；契约 89/89；
  oxlint 0；build ✓

## 1. S3-B1/B2 线上实证（走 `https://zsitai.xyz`）

| 检查 | 结果 |
|---|---|
| `GET /api/v1/research-agent/...` | 410 `RESEARCH_API_RETIRED`（路由注册已删，deprecated 中间件短路保留） |
| `GET /api/v1/nexus/health`（stu101） | 200：`persistence=postgres`，双模式工具面完整（research=8 / general=5） |
| 真实对话（search_cs_knowledge） | tool_call + success（S3 删除物无下游消费，零回归） |
| `research_*` 表 | 未动（`research_models.py` + Alembic 迁移保留） |

下线清单：`endpoints/research_agent.py`、`platform/agents/research/` 全目录、
providers `{paper_search,workspace,access}`、main.py 路由注册 +
`bootstrap_research_agent`、旧 research 测试 10 文件（Git 历史 `git revert`
即回退）。保留：`providers/research/{question_bank,question_generation,
web_research}`（TeachingAgent 活链）、`web_research` 端点+service（tasks
任务链）、deprecated 中间件、表与迁移。

## 2. M5-B2 压力套件

`nexus/tests/test_harness_stress.py` **7/7 全绿**（设计文档 Phase 9 七类）：
多 Tool 长任务（3 call/3 result/1 done 互斥）/ Tool 500 → error 事件 /
Compact 触发语义+生产接线分层锁定 / restart resume（同 saver 新实例续读）/
Cancel（M1-B6 引用）/ 缺参 tool_call 诚实 error / Worker 超时 fail-closed。
已知边界：Compact 全图触发未命中（deepagents before_model 与 profile 联动，
fake 注 profile 后仍不触发）→ P2+ 与上游核实。

## 3. 既有失败甄别（worktree `aa77ba41` 对照）

- **66 失败全部非 S3 造成**：b433bae3 avatar 移除遗留的死测试（404 接口
  不存在）已删除 4 文件 + 翻转两条路由契约断言为"已下线"；
- **仍失败 2 项（不删不改，单独跟进）**：`test_alembic_migration`
  （course_access 修复脚本权限集断言）、`test_p0_2_async_tasks`
  （handlers 注册断言）——course-access 与任务域，超出 M5 范围。

## 4. M5-D 文档同步

- 前端规格 §8：D1/D2/D3/D4/D5/D6/D8 逐条翻转 ✅（标注修复提交 + 验收记录链接）；
- §11 基数刷新（契约 89 / Runtime 82 / build）；
- P2 计划：§三 M5 ✅、§九 M5-B2/M5-D ✅、§14.9 执行记录、§14.6 基线保留；
- **AGENTS.md 部署地址**（确认后执行）：§1 公网入口 → `https://zsitai.xyz/`
 （SSH 管理 `103.36.223.177`，旧机地址退租声明）；§3.1 同步；§4.1.10
  Repro Worker 部署环境注记新机（`f1eb9b27` 单独提交）。
- 落地计划 §九：随本记录更新 S3 完成（见下节）。

## 5. 回退

- `git revert f1eb9b27 8d868f76`（文档 → 代码）；服务器从 release `aa77ba41`
  恢复。research_* 数据全程未动，表模型可查。
