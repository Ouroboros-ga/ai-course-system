# NX-G1/G2/G3 验收（2026-09-05，本地验证）

> 首批 P0（P2 计划 §3）。本地实现＋测试验证完成；**未提交、未部署**。
> 线上验收（真实 Worker/真实 PG/真实 DSN）待部署后补记，不把本地结果写成线上结论。

## 改动清单（工作区，未提交）

- NX-G1：`nexus/src/nexus/agent.py`（`InvalidNexusMode`＋严格归一）、
  `nexus/src/nexus/main.py`（`_require_mode`＋`approval_id` 字段＋执行上下文）、
  `backend/app/api/v1/endpoints/nexus_proxy.py`（别名镜像＋400）、
  `frontend/src/api/nexusAdapter.js`（4/7 真实工具映射）、
  `NexusPage.vue`（复现入口 Research 门＋effective 接线）。
- NX-G2：`nexus/src/nexus/approvals.py`（新建）、`request_scope.py`（执行上下文）、
  `tools/reproduction.py`（提案＋统一核销核心）、`main.py`（审批端点族）、
  `nexus_proxy.py`（批准/查询/手工执行代理）、`nexus.js`＋`NexusPage.vue`（审批卡）。
- NX-G3：`main.py`（`/health` checks＋TTL 探针）、`config.py`＋`.env.example`
 （`APPROVAL_TTL_S`/`HEALTH_PROBE_TTL_S`）、`nexusCapabilities.js`
 （`resolveEffectiveCapabilities`）、`NexusPage.vue`（effective 渲染）。
- 测试：`test_modes.py`（严格语义＋HTTP 400）、`test_approvals.py`（新建 6）、
  `test_reproduction.py`（提案语义更新）、`test_harness_stress.py`（超时经审批）、
  `test_api.py`（checks 形状）、`test_nexus_proxy.py`（400＋审批代理 3）、
  `apiContracts.test.cjs`（D10 计数 9→12、NX-G2/G3 契约）。

## 验证结果（命令＋输出）

- `uv run pytest tests/ -q`（nexus）：**90 passed**。
- Backend `pytest tests/test_nexus_proxy.py`：**26 passed**。
- 前端 `node --test apiContracts`：**88/89**；唯一失败为既有 S2 过期断言
  （M5 S3 已删 `research_agent` 路由注册，他线遗留，按 AGENTS §4.3 不擅改）。
- `npm run build`：通过（6.08s）。
- `npx oxlint`：30 errors 均为既有文件问题，本批文件零新增。

## 验收映射

- NX-G1：missing/null→General；别名正确；unknown/空串→400（Runtime/Backend 双层，
  模型/SSE 前拒绝，有"必炸桩"测试证明未触达模型）；General 不显示/不能调
  Paper/NexusLab（三层：工具面＋manifest＋前端门）；4/7 映射契约锁定；中性 Base
  为 HEAD 既有（5bd7bd48），本批保留未改。
- NX-G2：提案→ApprovalRequired→暂停→本人批准→服务端验证→恢复，全链测试；
  绑定 user/session/tool/plan hash/预算/有效期；无批准 Worker 零提交（拦截测试）；
  归属提案时落库；一次性（原子转换）、幂等（重试返原 job）、防重复提交；
  拒绝/过期/跨用户/跨会话/篡改矩阵全覆盖；批准/提交超时幂等键＝票据本身。
- NX-G3：effective＝manifest∩mode∩health；掉线/过期→unknown/degraded；
  General 永无 NexusLab（resolver 硬排除）；执行端审批由 NX-G2 服务端强制，
  UI 禁用不算边界（代码注释＋契约锁定）。

## 未验证项（部署后补）

1. 真实 PG 下的审批表创建＋跨重启 pending 恢复（本地只有内存路径测试）。
2. 真实 Worker 端到端：提案→批准→执行→轮询→报告（含复现预设回归）。
3. 线上 `/health` checks 快照与前端 effective 渲染（需真实 DSN/Worker/SearXNG）。
4. 两项既有域失败（`test_alembic_migration`、`test_p0_2_async_tasks`）仍单独跟进。

## 部署与线上只读验证（2026-09-05，commit b34ea2d9）

- 提交推送：`b34ea2d9`（35 文件，diff 无密钥），gitee＋github 双远端一致。
- 发布：release `b34ea2d9`（dist 本地构建上传，含模型下拉＋审批卡标记），
  `current` 已切，后端重启就绪；nexus-runtime 经 scp 同步源码后重启
  （中途发现 scp 嵌套目录已纠正并清理 `__pycache__`）。
- 线上 `/health` 实证：models 清单（deepseek-chat 单模型）、
  checks 四项全 ok、Research 9 工具面（含 read_attachment）、General 6 工具面、
  persistence=postgres；`journalctl -p err` 无错误。
- openapi 含全部新路由（approvals×2、attachments×4bind/download、repro/execute、
  runs×2）；公网首页正常。
- 待补线上验收：真实审批全链、PG 方言表、OCR 回填、附件 E2E（见 NX-A1E1 记录）。

## 附：模型网关 P0（2026-09-05，本地验证，与首批 P0 同分支）

> v1.3 列为 OPTIONAL，用户明确要求提前打通。单模型时如实只显示一个；
> 新增模型改服务端配置即出现在下拉，前端零改动。

- 服务端 allowlist 唯一真相源：`config.llm_available_models`
  （`NEXUS_LLM_MODEL` 默认＋`NEXUS_LLM_MODELS` 扩展，去重保序）；
  `/health` 新增 `models` 清单（`{default, available:[{id,label,default}]}`）。
- 请求 `model` 字段：缺字段→默认模型；未知 id→400 `INVALID_NEXUS_MODEL`
  （Runtime 强制，Backend 只透传不镜像动态清单，注释写明理由）；
  模型 id 大小写敏感（防误命中计费模型）。
- agent 按 `(mode, model)` 索引隔离实例，共享 thread 命名空间（切模型不断上下文）。
- 前端：Composer 模型下拉（选项＝health 清单投影，演示模式退回静态徽标；
  选择记 localStorage 偏好）；`model` 随请求透传。
- 附带修复：`runTurn` 内 `message: msg`（`msg` 不在作用域，真实链路必抛
  ReferenceError）→ `message`，与模型透传同批验证。
- 验证：nexus **94/94**（新建 `test_models.py` 4 项）、backend 代理 **27/27**、
  前端契约 **88/89**（同上，仅既有 S2）、`npm run build` 通过、
  oxlint 本批文件零新增。
