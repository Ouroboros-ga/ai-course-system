# S2 切换期验收记录（旧科研工作台退役）

> **日期**：2026-09-03　**执行**：编码智能体（承接 S1-V1 通过后的 S2 触发条件）
> **结论**：**S2 交付检查清单全部通过**。旧 `/research-agent`、`/web-research`
> 返回 410 Gone + 迁移说明；前端四面板页面、路由与 API client 已删除；
> Nexus AI 是唯一入口。回退方式：前后端各 `git revert` 一个提交。
> 敏感信息声明：本文不含密钥与令牌。

---

## 1. 变更内容（两个提交，对应回退粒度）

| 提交 | 范围 | 内容 |
|---|---|---|
| `6635f106`（S2-B1/B2） | Backend | `deprecation_middleware.py` 从 S1"仅标注头"升级为 **410 短路**（`RESEARCH_API_RETIRED` + `Link: successor-version`，不再携带 RFC 8594 `Deprecation: true`——410 语义是"不存在"而非"仍可用"）；**路由注册与 `bootstrap_research_agent` 保留**（S3 才删，revert 即恢复双轨）；`test_nexus_proxy.py`/`test_research_agent.py`/`test_web_research.py` 的 API 级测试翻转为 410 契约（42/42 通过），service 层单测保留（模块 S3 才删） |
| `8255a4c7`（S2-F1/F2/F3） | Frontend | 删除 `pages/course/research/`（ResearchWorkspacePage + Memory/Notepad/Scope/Todo 四面板）、`api/research_agent.js`、`router.js` 的 `app-course-research` 路由；契约测试翻转为"S2 已删除"断言（89/89 通过，含文件不存在检查）；构建通过 |

注：工作区中另有非 S2 的 NexusPage 流式节流 UX 改动（他处进行中），未纳入本次提交。

## 2. 线上验证（release `8255a4c7`，`smartcarb-release.sh` 第 5 次探测就绪）

```
GET /api/v1/research-agent/courses/1/capabilities
HTTP/1.1 410 Gone
link: </api/v1/nexus/chat>; rel="successor-version"
x-deprecation-phase: S2-research-retired
{"error":"RESEARCH_API_RETIRED","migration":"Use /api/v1/nexus/* instead"}

GET /api/v1/web-research/policy → HTTP 410（同响应体）
GET /api/v1/nexus/health → HTTP 200（llm_configured:true, searxng_configured:true）
```

前端 `/app/course/:id/research` 深链：路由已从 bundle 移除（SPA 兜底渲染，无匹配路由）。

## 3. 开源承接说明（补丁 §12 二分法）

- **deepagents**（MIT）：旧 ResearchAgent 的 LangGraph 0.6 九节点 harness 的全部
  产品能力（任务拆解/子代理/工作区）已由 Nexus Runtime 承接（S1-V1 冒烟实证）；
- **SearXNG**（自部署）：旧 `web_research`（注入型 Port，从未接真实 provider）由
  Nexus `web_search` 真实承接；`paper_search` 新增 `site:arxiv.org` 降级链
  （提交 `167863c6`，nexus 测试 22/22，生产验证返回真实 arXiv ID）；
- **duckduckgo-search**（MIT）：末位降级通道，web_search 与 paper_search 共用；
- **paper-qa / OpenHands / opencode**：S2 不引入（前者 P1 Spike 再评估）；
- **数据**：`research_*` 五表保留不 drop（AGENTS §4.2.3），按 retention 自然过期。

## 4. 后续（S3 下线，待 S2 稳定 ≥1 迭代）

1. 删除 `/research-agent`、`/web-research` 路由注册与 `bootstrap_research_agent`；
2. `platform/agents/research/` 与 `providers/research/` 清理（paper_search 迁移评估
   ——nexus 已有独立实现，预计直接删除）；
3. `research_*` 表模型与 Alembic 迁移**保留**，不新增 down 迁移。
