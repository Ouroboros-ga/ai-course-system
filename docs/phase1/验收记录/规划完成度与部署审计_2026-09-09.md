# 规划完成度与部署审计（2026-09-09）

> **后续功能审查订正（2026-09-09）**：本记录保留原审计时点；§6.2 的“主体实现、5 项未交付”不是完整缺口清单。当前代码与原产品要求对照发现，恢复、完成判定、干净 B、自动准备及正式成果输出存在实质性简化；操作级中断已经写入原 T5。新的功能边界、开源分工与 F1–F9 补齐顺序见[原设计功能补齐与开源集成实施计划](../2026-09-09_Nexus功能补齐与开源集成实施计划.md)。本次只补文档，不表示新功能已实现或线上重新验收。

> 范围：`Nexus_自主实验V1_执行任务书.md`、`2026-09-08_学科知识库自动化构建实施计划.md`（DK0–DK8）、
> `2026-09-08_CS语料向量化与RAG上线实施计划.md`（CR0–CR6）、`CodeNexus_P2开发计划.md`（P2/NX）
> 方法：代码侧分册审计（DK、P2 两路独立审计＋既有 Nexus/CR 审计记录）＋线上实况核查（SSH 只读）；
> 所有结论区分 **代码存在 / 测试锁定 / 已部署 / 线上可用** 四层，不以其中一层冒充另一层。
> 线上基线：`dev-liu` 发布 `054b5f27`（2026-09-09 00:17 部署），DB `dk20260909v2 (head)`。

## 1. 线上实况（2026-09-09 核查）

| 项 | 实况 |
|---|---|
| 发布 | `current` → `releases/054b5f27`；`nexus-runtime/RELEASE_INFO` 同 SHA |
| 服务单元 | `smartcarb-backend`、`nexus-runtime`、`repro-runtime` 已装且 active；**无** `smartcarb-corpus-embedding`、`smartcarb-corpus-worker` |
| env | `backend/database/nexus/postgres/repro-runtime/repro-worker/runtime-paths`；**无** `corpus-embedding.env` |
| 模型 | 仅 `bge-small-zh-v1.5`（历史中文基线）；**无** E5（`intfloat/multilingual-e5-small`） |
| DB 迁移 | `0068 → dk20260909v2`，新建 15 张 `discipline_*` 表（已授权给应用账号，15/15 可读写） |
| `discipline_*` 数据 | **全部 0 行**（文档/分块/构建/工作项/发布/向量/概念/断言…） |
| Nexus 数据 | 提案 41、审批 39、实验 run 9（成功 3 / 失败 4 / 取消 1 / 陈旧 running 1）、运行 22、产物 47、会话偏好 11 |
| 语料文件 | `/opt/smartcarb/shared/knowledge_corpus/`：5 份 JSONL（约 10 GB）＋旧 `corpus_index.sqlite3`（7 GB）＋`manifest.json` |
| 遗留 env | `DISCIPLINE_CORPUS_INDEX_PATH/_TOP_K/_VECTOR_ENABLED/_VECTOR_MODEL_PATH` 指向旧 sqlite 索引与 bge 模型（历史路径） |
| 接口面 | 后端 531 个端点；`/api/v1/discipline-knowledge/{search,overview,nodes,reload}` 在线（需鉴权） |
| 容器 | 无实验任务容器残留（仅 repro-worker/judge0/paddleocr 等常驻服务） |

## 2. 分册结论

### A. Nexus 自主实验 V1（T0–T7＋SR6）

**代码/测试**：已完成并全量审核修复（见[全量审核](Nexus_自主实验V1_全量审核_2026-09-08.md)／[修复计划](Nexus_自主实验V1_修复计划_2026-09-08.md)），nexus 261 绿、backend nexus 域 101 绿。

**部署**：`054b5f27` 已上线并实证——三服务 active、零 error；控制面契约真容器 9/9；
宿主隔离断言 6/6（`verify_host.sh` 首次实跑）；Nexus 侧工具面含 `prepare_experiment`/`run_reproduction`。

**可用性**：**部分可用**。真实模型链路受 DeepSeek key（尾号 `b27d`）失效阻塞；
线上 9 个自主 run 中 2 个成功、4 个失败（含 1 个 `OpenAIAuthenticationError`）、1 个用户取消；
另有 1 个 **陈旧 running run**（`apv_1f3ac3b2dbac`，2026-09-08 13:05 创建、attempts=0、无取消旗）未收敛。

### B. CS 语料向量化与 RAG（CR0–CR6）

**代码/测试**：已完成并全量审核修复（见[全量审核](CS语料RAG全量审核_2026-09-08.md)／[修复计划](CS语料RAG修复计划_2026-09-08.md)），随 `054b5f27` 部署。

**部署**：**代码与迁移已就位，但功能未启用**：
- `rag/config.json` 仍 `draft-unfrozen`（`model.revision`/`files_hash` 为 null）→ CR0 的模型参数冻结未完成；
- 服务器**无 E5 模型**、**无 `corpus-embedding.env`**、**未安装** `smartcarb-corpus-embedding`/`smartcarb-corpus-worker` 单元；
- `discipline_*` 表全 0 行 → 无语料导入、无构建、无 release，检索必然返回"无已发布版本"。

**结论**：代码层可交付，**线上不可用**（与既有声明一致，未夸大）。

> **2026-09-09 晚更新**：本节所述状态已改变——用户授权后已完成语料 RAG 首批上线
> （bge-small-zh 族驱动、`rag/config.json` 冻结、两个单元常驻、cs-textbooks 全链跑通、
> release `dkr_979fe2fdf498` 已激活）。执行结果与 4 个 PG 缺陷修复见
> [修复计划 §7](规划完成度修复计划_2026-09-09.md)；扩量前置条件见
> [扩量方案](CS语料RAG扩量方案_2026-09-09.md)。**语料检索现已线上可用**。

### C. 学科知识库自动化构建（DK0–DK8）

**逐条目（代码侧独立审计＋审核人复核）**：

| DK | 目标 | 声明 | 实际 | 关键证据 |
|---|---|---|---|---|
| DK0 | 范围/抽样/挑战集/ontology | 已实施（7 用例） | **已完成** | `knowledge_data/pipeline/ontology.json`、`benchmark/sample_manifest.json`（1000 chunk_id）、`synthetic_cases.jsonl`（224 行）、`test_sample.py`(7) |
| DK1 | 持久化/导入/身份（13 表） | 已实施（16 用例） | **已完成**（被 CR1/CR3 复用） | 13 个 `__tablename__`、`import_discipline_corpus.py`、迁移 `dk20260908v1`、`test_ingest.py`(7)＋`test_migrations.py`(3) |
| DK2 | 高召回抽取＋来源对齐 | **取消未实现** | **未实现**（声明一致） | 目录/测试/benchmark 脚本均不存在；`work_items.py:48` `DEFERRED_STAGES=("extract","ground","card","index")`，`:592` 抛 `STAGE_DEFERRED` |
| DK3 | 归一/盲核验/裁决 | 已实施（30 用例）→ 退出路线 | **部分完成** | `resolution/validation/adjudication/decisions.py`＋30 测试在；但**无 embedding/LLM 调用**，计划 §4.2 的"四模型角色/盲核验"未落地 |
| DK4 | 独立 worker/增量/预算 | 已实施（65 用例） | **已完成**（被 CR2/CR3 扩展） | `builds.py`/`work_items.py`、`run_discipline_worker.py`、迁移 `dk20260908v2`、`test_worker.py`(12)＋`test_incremental.py`(7) |
| DK5 | 知识卡/自动发布/回退 | **无回执** | **未实现** | `cards.py`/`releases.py`/`test_releases.py` 均不存在 |
| DK6 | 三入口统一读取＋授权 | **无回执** | **部分完成** | 读取侧由 CR3/CR4 覆盖；`DISCIPLINE_KB_MODE`、`/references`、`/sources`、`/builds`、`/quarantine`、`/decisions`、`/releases` 全缺；端点仅 5 个（`discipline_knowledge.py:43,81,93,135,155`，全部只读＋reload） |
| DK7 | 阅读页/构建监控/纠错 | **无回执** | **部分完成** | 页面与 API 存在；`DisciplineBuildPanel`/`DisciplineEvidencePanel`/`DisciplineQuarantinePanel`、`disciplineKnowledgePresentation.js` 不存在 |
| DK8 | 联调/部署/零人工试跑 | **无回执**（计划明示未执行） | **未实现** | 无 `smartcarb-discipline-worker.service`、无 `discipline-preflight.sh`、无 DK 版验收记录 |

**P1（审核人已复核）**：
1. **旧 DK 抽取流水线无任何可运行入口**：`extract/ground/card/index` 为 `DEFERRED_STAGES`，且 `resolve/verify/adjudicate` 只消费 `discipline_mentions`，而全仓无 mentions 生产者 → legacy 阶段只能对测试注入数据生效。
2. **读取模式契约未实现**：`grep DISCIPLINE_KB_MODE backend/` 零命中（计划 §4.6 要求按该配置在 `legacy_json|published` 间切换）。
3. **管理端点整体缺失**：计划 §4.7 的 builds/quarantine/decisions/releases 写接口全无（实测 5 个路由全部只读）；计划点名的 `test_reader_cannot_start_global_build` 文件不存在，且实际会 404 而非 403。

**部署状态**：运行脚本（`import_discipline_corpus.py`/`manage_corpus_index.py`/`run_discipline_worker.py`/`corpus_preflight.py`）随 `054b5f27` 到位，但**无 DK 服务单元、`discipline_*` 表 0 行** → 自动构建流水线未部署。

**但 DK 的读取路径线上实测可用**：概念数据 `knowledge_data/*.json` 随发布到位，`discipline_kb.py` 沿目录向上解析到 `knowledge_data/`，实测加载 **112 个概念节点 / 106 条关系**，检索"动态规划"命中 3 条。即：**DK 概念检索（legacy JSON 路径）在线可用；DK 自动抽取/构建链未启用**。

**文档不一致**：DK5–DK8 在计划中**既无回执也无"取消/未执行"标记**（§9 只勾选 DK0/DK1/DK3/DK4）；计划:11/:14 对 DK1/DK4 的缺口描述已被 CR 系列覆盖而失效；计数口径 65/16 与实测 66/17 不符。

### D. CodeNexus P2 / NX 主线

**§10 状态矩阵（代码侧实测）**：

| 条目 | 文档声明 | 实际 | 证据 |
|---|---|---|---|
| 历史 P2 M0–M5 | 6/6 完成 | **已完成** | preset/worker/审批/Console 链均在（`deploy/repro-worker/worker.py`、`approvals.py`） |
| 后端 A/B/C/D | 4/4 交付；**三项 P1 待收尾** | **已完成；"待收尾"过期** | LB1–LB5 端点＋测试在；三项 P1 已在 `054b5f27` 批次 A 全修 |
| v6 工作台/启动页 | 已提交 | **已完成**（浏览器链未复验） | `NexusPage.vue`/`NexusExperimentWorkspace.vue` |
| 用户论文→实验准备 | T3 已落地 | **已完成** | `experiment_intake.py:197,218`＋13 测试 |
| 独立 Sandbox / Repair / Clean B | T1/T2/T4 已落地；repo2docker 未交付 | **部分完成**（声明一致） | `experiment_sandbox.py`、`deploy/repro-runtime/`、`experiment_clean.py` |
| 完整 Paper Research / Session / Subagent | 部分基础已有 | **未实现**（R1a 薄链除外） | Subagent 显式关闭（`agent.py:137`）；E4 未做 |
| 正式成果输出 NX-O1 | 部分完成 | **部分完成** | `document_output.py` 已产 Word/LaTeX；LaTeX 编译证明缺工具链 |

**编号任务**：NX-G1/G2/G3、NX-A1、NX-H1、NX-E1/E2/E3、NX-LB1–LB5 **已完成且有测试**；
NX-R1/S1/P1 **部分完成**（其中 S1/P1 文档仍标 NEXT，实为已交付主干）；
NX-P2/H2/E4/SR3 **未实现**；SR0/SR6 已交付但文档称"所有 SR 任务 NEXT"；NX-FINAL 未实现。

**P1（与 Nexus 审计同根）**：**Nexus 域 schema 仍靠启动/懒加载 DDL**（`main.py:70-100`、
`nexus_run_service.py:146`、`proposals.py:430`、`approvals.py:198` 等），仓库无 `nexus/migrations/`
与 apply 脚本；本计划 §9.7 明文"**禁止启动时隐式更改 schema**"——文档与实现相悖，且本计划未披露该偏离。

**P2（文档过期，5 条）**：§11 仍写"Word 与 LaTeX 编译/打包/校验未实现"（Word 产物已实现，LaTeX 编译确未）；
§1.1 称 Ask/Auto"尚无该开关实现"（已全链实现）；§2 工具计数 12/18（实测 17 业务工具，Research 19／Ask 18）；
`nexus/.env.example` 缺 `NEXUS_REPRO_CONTROL_URL/TOKEN` 等（`backend/.env.example` 亦无 `NEXUS_RUNTIME_*`）；
§10"三项 P1 待收尾"与修复记录冲突。

**部署状态**：Nexus Runtime 与预设 Worker **线上可用**——`repro-worker` 容器常驻健康，
`nexus_repro_jobs` 16 行、`nexus_runs` 22 行、`nexus_threads` 35 行、`nexus_run_notes` 4 行；
`nexus_checkpoints` 下 12 张表齐备。缺：**显式迁移物**、**env 模板关键变量**、
**发布脚本不覆盖 `deploy/repro-runtime`/`repro-worker`**（每次靠手工 rsync）。

## 3. 汇总矩阵

| 规划 | 代码完成 | 测试 | 已部署 | 线上可用 |
|---|---|---|---|---|
| **Nexus 自主实验 T0–T7＋SR6** | ✅ | nexus 261 / backend nexus 域 101 | ✅ 服务＋控制面＋单元 | ⚠️ 部分（真实模型 key 失效；1 个陈旧 running run） |
| **CS 语料 RAG CR0–CR6** | ✅ | discipline 域 181 绿 | ✅ 代码＋迁移＋两单元常驻 | ✅ **已上线**（bge-zh／config 冻结／cs-textbooks 3899 块，release `dkr_979fe2fdf498`；见 §6.1、§6.3） |
| **学科知识库 DK0–DK8** | DK0/DK1/DK4 完成；DK2 取消；DK3/DK6/DK7 部分；DK5/DK8 未实现 | 66 用例 | ❌ 无 DK 单元、无构建数据 | ⚠️ **概念检索（legacy JSON）线上实测可用**（112 节点/106 关系）；自动构建链未启用 |
| **CodeNexus P2 / NX** | 主干（G/A/H/E/LB）完成；R1/S1/P1 部分；P2/H2/E4/SR3 未实现 | 各套件绿 | ✅ Nexus 侧已上线 | ⚠️ 部分（同上；预设 Worker 可用） |

## 4. 跨规划发现

**P1**
1. **Nexus 域无显式迁移机制**：任务书 T2 与 P2 计划 §9.7 都明文禁止启动时 DDL，实现走 lifespan/懒加载
   `ADD COLUMN IF NOT EXISTS`；线上 `nexus_checkpoints` 表即由此创建。已列为下一批首项（需真实 PG 验证）。
2. **语料/学科自动构建链无数据无服务**：`discipline_*` 15 表 0 行、无 `corpus-embedding.env`、
   未安装 corpus 两个单元、无 E5 模型、`rag/config.json` 仍 `draft-unfrozen` → CR0–CR6 与 DK 构建链
   在线上**均未启用**（读取侧概念检索可用，语料检索不可用）。
3. **陈旧 running run 未收敛**：`apv_1f3ac2…`（09-08 13:05、attempts=0、无取消旗）挂 11 小时；
   T5 恢复语义只覆盖"接管查询"，缺少"零尝试 run 的终态收敛"。

**P2**
4. **发布脚本不覆盖 repro-runtime / repro-worker**：`smartcarb-release.sh` 只同步 `nexus/`；
   控制面与预设 Worker 每次靠手工 rsync（本次即如此）。
5. **环境模板缺关键变量**：`nexus/.env.example`、`backend/.env.example` 均缺 Nexus 运行时/控制面变量，
   按模板部署无法启用自主实验。
6. **文档大面积过期**：P2 §1.1/§2/§4/§5/§9.1/§10/§11 多处与代码矛盾；DK5–DK8 无状态标记；
   任务书 T0–T7 节仍标"（未提交）"（已在本轮回执订正）。

## 5. 建议（按优先级）

1. **Nexus 显式迁移机制**（PG 上验证）：`nexus/migrations/*.sql`＋apply 脚本＋发布脚本调用；
   顺带补 `nexus_checkpoints` 的迁移基线。
2. **语料 RAG 上线三件套**（需授权）：下载 E5 并回填/冻结 `rag/config.json` → 安装
   `smartcarb-corpus-embedding`/`smartcarb-corpus-worker` 单元＋env → 导入语料、建 build、跑 release、
   `verify_corpus_rag` 验收。
3. **陈旧 run 收敛器**：对 `running && attempts==0 && 无沙箱` 的 run 按超时落 `failed`（含原因），
   或在 console 明确标注"未启动"。
4. **发布脚本补齐**：把 `deploy/repro-runtime`（排除 .venv/data/.token）纳入发布流程；
   环境模板补齐 Nexus/控制面变量。
5. **文档回写**：P2 §11/§1.1/§2/§10 计数与状态；DK5–DK8 显式标注"未实现/部分完成"；
   修复计划中"P1-8 迁移"与本节合并为同一跟踪项。

## 6. 专项核查（2026-09-09 晚）：RAG 接入与 Nexus 实验能力

### 6.1 RAG 是否接入两个 Agent —— ✅ 已接入且线上实测通过

三条入口共用同一 `CorpusSearchService`（CR3）与同一已发布版本：

| 消费方 | 接线点 | 线上实测（release `dkr_979fe2fdf498`） |
|---|---|---|
| TeachingAgent（课程内问答） | `discipline_kb.py:108 _search_corpus_layer` → `discipline_corpus.search_corpus_unified:387` → `CorpusSearchService` | `status=ok mode=hybrid n=3`，`is_supplementary=True` |
| Nexus（`search_cs_knowledge` 工具） | `nexus_internal.py:138 GET /cs-knowledge` → `_search_corpus_release:190` | HTTP 200「概念 3 条 + 语料 3 条」，`mode=hybrid`，同一 release |
| 学科页 | `/api/v1/discipline-knowledge/search`（同一服务） | 服务层同源（页面需登录态，未走查） |

契约一致：`is_supplementary=true`、`reference_id={release_id}:{chunk_id}`（单一构造点）、
许可字段透传（如 `CC BY-NC-ND 3.0`）。**结论：接入完成，非"仅代码存在"。**

### 6.2 Nexus 实验 lab 功能设计 —— 主体实现，5 项未交付/未验收

**已实现并上线**（T0–T7＋SR6）：一次确认授权（提案→审批→执行）与 Ask/Auto 门；
独立沙箱（BaseSandbox＋SWE-ReX Docker 控制面，§2 契约补齐）；无 preset 入口
`prepare_experiment`；自主修复循环（脚本化模型＋假容器验证）；持久运行/恢复/Console/取消；
报告与配方（四分量判定、先落盘后回收）；干净 B 重放；前端 Ask/Auto 选择器与工作台。

**未实现 / 未验收**：

| # | 项 | 状态与证据 |
|---|---|---|
| 1 | repo2docker 构建路线 | **未交付**（`ROUTE_NOT_DELIVERED`，fail-closed 不假装集成） |
| 2 | 真实模型端到端 | **未验收**：DeepSeek key 失效；线上 9 个 run＝2 成功／4 失败／1 取消／1 陈旧（已收敛） |
| 3 | LaTeX 编译证明 | **缺工具链**（`TOOLCHAIN_MISSING`）；产物可生成，未编译验证 |
| 4 | 操作级取消 | **未实现**：任务书 §2 只定义 run 级取消；"模型终止自己卡住的命令"无 API |
| 5 | NX 主线（非 T0–T7 范围） | 完整 Paper Research、Subagent（**显式关闭**）、NX-E4（Worker 重启对账）、受控 A/B 均未实现 |

**口径**：按任务书 T0–T7 看——主干实现、无 P0 缺陷；但**不能称"完全实现"**：
第 1/3 项为交付缺口，第 2 项为验收缺口（代码就绪、外部条件阻塞），第 4/5 项为范围内未做。
回执与审核记录的相应标注如实，未翻绿。

### 6.3 检索性能（专项修复，2026-09-09）

同一批修复把检索延迟从 **9.85s 降到 0.4s**（3899 块，约 24×）：
N+1 成员过滤改单次 JOIN（7.29s→0.51s 纯词法）→ 向量列改 pgvector ＋ SQL 侧 `<=>` 排序
（取数 2.0s→毫秒）。**HNSW 暂不做**（用户决策：数据量不大，开销不值）；跨编码器精排待定。
