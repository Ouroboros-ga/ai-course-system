## 待完善功能列表

####           
根据文档需求和当前项目实现状态，以下是5大核心功能的**差距分析框架**：

---

## 功能差距分析总览

| 功能模块    | 当前状态       | 完成度  | 优先级 |
| ----------- | -------------- | ------- | ------ |
| 1. 课件解析 | 基础文字提取 ✅ | **40%** | 🔴 高   |
| 2. 内容重构 | 通用脚本生成 ⚠️ | **30%** | 🔴 高   |
| 3. 智能讲授 | 音频播放框架 🎵 | **30%** | 🟡 中   |
| 4. 实时问答 | 文字问答+选择题 ✅ | **70%** | 🟡 中   |
| 5. 进度接续 | 未开始 ❌       | **0%**  | 🟢 低   |

---

## 1️⃣ 课件解析（PPT/PDF）

### 当前实现

```python
# backend/app/api/v1/endpoints/document.py
async def _parse_with_docling(file_path, filename) -> str:
    # Docling 解析 → Markdown 文本
    converter = DocumentConverter()
    result = converter.convert(str(file_path))
    markdown_content = result.document.export_to_markdown()

# 已存储的数据结构:
# DoclingDocument (主表) + DoclingText (文本) + DoclingPicture (图片)
```

### ❌ 缺失功能

| 子功能            | 现状                                | 需要做的                                                  |
| ----------------- | ----------------------------------- | --------------------------------------------------------- |
| **PPT/PPTX 解析** | 仅基础文本提取                      | 使用 `python-pptx` 提取形状、图表、SmartArt               |
| **PDF 图片识别**  | `DoclingPicture` 表已建但未填充数据 | 调用 OCR API（如百度/阿里云）识别图片中的文字、公式、表格 |
| **PDF 图表提取**  | `DoclingTable` 表已建但未完整使用   | 结构化提取表格数据 → JSON                                 |
| **公式识别**      | 未支持                              | 集成 LaTeX 公式识别（Mathpix / 自研）                     |
| **手写体识别**    | 未支持                              | OCR 手写内容识别                                          |
| **多页合并**      | 单次解析全部内容                    | 支持按页面拆分，便于逐页展示                              |

### 技术方案建议

```
┌─────────────────────────────────────────────────────┐
│                    课件解析流程                      │
│                                                     │
│  上传文件                                            │
│    │                                                 │
│    ├── PDF ──→ Docling ──→ Markdown                 │
│    │              │                                │
│    │              ├── 文本 → docling_texts           │
│    │              ├── 图片 → OCR → 文字描述         │
│    │              └── 表格 → 结构化JSON             │
│    │                                                 │
│    ├── PPTX ──→ python-pptx ──→ 形状/图表          │
│    │              │                                │
│    │              ├── 文本框 → 提取文字              │
│    │              ├── 图表 → 数据结构化             │
│    │              └── SmartArt → 层级结构            │
│    │                                                 │
│    └── DOCX ──→ python-docx ──→ 段落/表格         │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 2️⃣ 内容重构（结构化脚本）

### 当前实现

```python
# AI生成的脚本结构（当前输出格式）
{
  "nodes": [
    {
      "node_type": "lecture",
      "title": "节点标题",
      "content": "原始内容",
      "duration": 60
    }
  ]
}
```

### ❌ 缺失功能

| 子功能             | 现状                      | 需要做的                                    |
| ------------------ | ------------------------- | ------------------------------------------- |
| **开场白生成**     | 无                        | Prompt 增加"生成开场白：引入主题、激发兴趣" |
| **知识点拆分**     | 粗粒度拆分                | 按教学目标细化为：概念→原理→示例→练习       |
| **过渡语生成**     | 无                        | 在节点间插入自然过渡："接下来我们来看..."   |
| **总结语生成**     | 无                        | 每个章节末尾生成要点回顾                    |
| **教师个性化编辑** | 无                        | 前端提供富文本编辑器，支持修改脚本          |
| **脚本版本管理**   | 有 `version` 字段但未启用 | 支持版本对比、回滚                          |

### 目标脚本结构设计

```json
{
  "script_version": "v1.0",
  "sections": [
    {
      "type": "opening",
      "title": "开场白",
      "content": "同学们好！今天我们要学习...",
      "duration": 30,
      "tone": "enthusiastic"
    },
    {
      "type": "knowledge_point",
      "id": "kp_001",
      "title": "核心概念",
      "definition": "...",
      "examples": ["...", "..."],
      "duration": 120,
      "difficulty": "medium",
      "transitions": {
        "prev": "了解了基础知识后...",
        "next": "让我们通过实例来理解..."
      }
    },
    {
      "type": "summary",
      "title": "本节小结",
      "key_points": ["1.", "2.", "3."],
      "next_preview": "下节课我们将学习...",
      "duration": 45
    }
  ]
}
```

### 实现路径

```
当前:  文档 → LLM → 通用脚本
        ↓
优化:  文档 → [预处理] → LLM(结构化Prompt) → 标准脚本
                        ↓
                   教师编辑界面 → 保存版本
```

---

## 3️⃣ 智能讲授（语音合成）

### 当前实现

```vue
<!-- PptPlayer.vue -->
<audio ref="audioRef" :src="currentData.audioUrl" />
<!-- 音频控制条已有：播放/暂停、进度条、倍速、音量、循环 -->
```

### ❌ 缺失功能

| 子功能             | 现状             | 需要做的                              |
| ------------------ | ---------------- | ------------------------------------- |
| **TTS 语音合成**   | 使用静态音频文件 | 对接 TTS API（火山引擎/阿里云/讯飞）  |
| **多音色选择**     | 无               | 支持男声/女声/童声切换                |
| **语速调节**       | 前端有倍速控制   | TTS 侧也需支持变速                    |
| **SSML 标记**      | 无               | 支持 `<break>`, `<emphasis>` 控制节奏 |
| **音频分段**       | 单一长音频       | 按节点拆分，支持跳转播放 | ✅ 已实现：节点音频自动播放 |
| **Web/Mobile适配** | HTML5 Audio      | 移动端自动播放限制处理   | ⚠️ 部分实现：用户交互触发自动播放，被阻止时提示手动播放 |

### 技术方案

```python
# 新增: app/services/tts_service.py
class TTSService:
    """语音合成服务"""
    
    async def synthesize(self, text: str, voice_id: str = "zh_female") -> bytes:
        """
        调用火山引擎 TTS API
        
        参数:
            text: 要合成的文本
            voice_id: 音色ID
            
        返回:
            audio_bytes: 音频二进制数据
        """
        
    async def synthesize_script_node(self, node: ScriptNode) -> str:
        """
        为单个脚本节点生成语音
        返回音频URL
        """
```

### 音频流式播放架构

```
ScriptNode[0] ──→ TTS ──→ audio_0.mp3 ──┐
ScriptNode[1] ──→ TTS ──→ audio_1.mp3 ──┤──→ 播放队列
ScriptNode[2] ──→ TTS ──→ audio_2.mp3 ──┘
                                        ↓
                              当前节点高亮显示
                              自动滚动到对应内容
```

---

## 4️⃣ 实时问答（双入口）

### 当前实现 ✅

```javascript
// ChatPanel.vue - 文字问答已完成
const res = await api.chat.askQuestion({
  question: text,
  chatId: currentChatId.value,
  courseId: props.currentData?.courseId
});
```

### ❌ 缺失功能

| 子功能           | 现状                         | 需要做的                            |
| ---------------- | ---------------------------- | ----------------------------------- |
| **语音输入**     | 无（按钮存在但未实现）       | 集成 Web Speech API 或 讯飞语音听写 |
| **语音回答**     | 无                           | TTS 合成AI回答后播放                |
| **上下文关联**   | 基于 courseId 的文档上下文 ✅ | 增强：关联当前播放的节点位置        |
| **追问引导**     | 无                           | AI 主动提出相关问题引导学生思考     | ✅ 已实现：选择题互动问答，含解析引导 |
| **答案来源标注** | 无                           | 标注引用的文档段落/页码             |

### 语音输入实现方案

```vue
<!-- ChatInput.vue 增强 -->
<template>
  <button class="btn-mic" @click="toggleMic">
    {{ isRecording ? '⏹' : '🎤' }}
  </button>
</template>

<script setup>
// 使用浏览器原生 SpeechRecognition
const recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
recognition.lang = 'zh-CN';
recognition.continuous = false;

const toggleMic = () => {
  if (isRecording.value) {
    recognition.stop();
  } else {
    recognition.start();
    isRecording.value = true;
  }
};

recognition.onresult = (event) => {
  const transcript = event.results[0][0].transcript;
  emit('send', transcript);
};
</script>
```

---

## 5️⃣ 进度接续（智能分析）

### 当前实现 ❌

- **完全未实现**
- `User.last_active_course_id` 字段已预留但未使用
- `User.last_learning_node` 字段已预留但未使用

### 需要新建的功能

| 子功能           | 说明                                | 复杂度 |
| ---------------- | ----------------------------------- | ------ |
| **理解程度分析** | 分析学生提问质量，判断是否真正理解  | 🔴 高   |
| **学习节点定位** | 根据问题自动定位到对应的 ScriptNode | 🟡 中   |
| **进度可视化**   | 学习路径图、完成度百分比            | 🟡 中   |
| **自适应调节**   | 根据理解情况调整讲解深度/速度       | 🔴 高   |
| **断点续学**     | 下次打开时恢复到上次位置            | 🟢 低   |

### 新增数据库表设计

```sql
-- 学习进度记录表
CREATE TABLE learning_progress (
    id INTEGER PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    course_id INTEGER REFERENCES courses(id),
    current_node_id INTEGER,           -- 当前学习的节点ID
    completed_nodes TEXT,              -- 已完成的节点ID列表(JSON)
    total_study_time INTEGER DEFAULT 0, -- 总学习时间(秒)
    understanding_score REAL DEFAULT 0.5, -- 理解程度评分(0-1)
    last_position REAL DEFAULT 0,     -- 最后观看位置(秒)
    created_at DATETIME,
    updated_at DATETIME
);

-- 学生问答分析表
CREATE TABLE qa_analysis (
    id INTEGER PRIMARY KEY,
    chat_message_id INTEGER REFERENCES chat_messages(id),
    user_id INTEGER REFERENCES users(id),
    question_quality VARCHAR(20),     -- good/basic/confused/repeat
    topic_keywords TEXT,              -- 关键词(JSON)
    related_node_id INTEGER,          -- 关联的知识点节点
    understanding_level VARCHAR(10),  -- high/medium/low
    suggested_action TEXT,            -- 建议操作
    analyzed_at DATETIME
);
```

### 进度接续流程

```
学生提问 "这个公式怎么推导？"
    │
    ▼
┌─────────────────────────────────────────┐
│  NLP 分析                               │
│  ├─ 问题分类：概念疑问 / 应用困难       │
│  ├─ 关键词提取：公式、推导              │
│  └─ 困难度评估：中等                   │
└────────────────┬────────────────────────┘
                 ▼
┌─────────────────────────────────────────┐
│  节点定位                               │
│  ├─ 匹配到 ScriptNode: chap02_formula   │
│  ├─ 判断前置知识是否掌握               │
│  └─ 生成个性化回答                     │
└────────────────┬────────────────────────┘
                 ▼
┌─────────────────────────────────────────┐
│  进度更新 & 调节                        │
│  ├─ 更新 learning_progress              │
│  ├─ 若连续困惑 → 降低难度              │
│  └─ 若完全掌握 → 加快进度              │
└─────────────────────────────────────────┘
```

---

## 开发路线图（建议顺序）

### Phase 1: 核心完善（1-2周）

```
□ 完善 PPT/PPTX 解析（python-pptx）
□ 优化 AI 脚本结构（开场白+总结语+过渡语）
□ 实现 TTS 语音合成对接
□ 前端增加脚本编辑器
```

### Phase 2: 体验升级（1周）

```
□ 语音输入集成（Web Speech API）
□ 语音回答播放
□ 音频分段与节点同步
□ 移动端适配优化
```

### Phase 3: 智能化（2-3周）

```
□ 学习进度数据库设计与实现
□ 问题理解分析（NLP）
□ 节点自动定位
□ 进度可视化界面
□ 自适应学习调节
```

---

## 总结：当前 vs 目标对比

| 维度         | 当前状态     | 目标状态         | 差距         |
| ------------ | ------------ | ---------------- | ------------ |
| **解析能力** | 文字提取为主 | 图文混合智能识别 | 需增强OCR    |
| **脚本质量** | 通用内容生成 | 教学结构化脚本   | 需优化Prompt |
| **讲授形式** | 静态音频     | 动态TTS+多音色   | 需接入TTS    |
| **交互方式** | 文字输入     | 文字+语音双模态  | 需加语音     |
| **智能化**   | 基础问答     | 自适应学习       | 需全新开发   |



