# 教育智能体系统页面与交互设计规范

> **文件职责**：定义页面信息架构、页面内容、角色权限、组件分工、交互流程、状态处理与验收标准，回答“页面放什么、用户怎样操作、系统怎样回应”。  
> **视觉职责**：颜色、字体、字号、间距、圆角、边框、阴影和组件外观统一遵循 [`design.md`](./design.md)。本文件不重复定义视觉令牌。  
> **适用对象**：产品经理、交互设计师、Figma 设计人员、前端开发、后端接口设计人员、测试人员、比赛演示编排人员。  
> **版本**：v0.4  
> **产品主张**：让课程回应学习，做更了解学生的智能体系统

---

# 0. 如何使用本文件

## 0.1 规范用语

本文使用以下强度：

- **必须**：不可自由发挥，执行时应严格遵守；
- **应该**：默认采用，只有明确业务原因时才能偏离；
- **可以**：可选增强，不阻塞第一阶段实现；
- **禁止**：会破坏信息架构、可信度或安全边界，不应实现。

当页面设计稿、前端实现与本文件冲突时：

1. 页面内容与交互以本文件为准；
2. 视觉外观以 `design.md` 为准；
3. 真实能力边界以产品功能文档和后端实现为准；
4. 未明确的地方不得擅自包装成已完成能力，应显示“实验能力”“研究预览”或暂不展示。

## 0.2 本文件不是什么

本文件不是：

- 数据库表设计；
- API 字段的最终契约；
- 算法实现说明；
- 单纯的页面清单；
- 比赛宣传文案合集。

它是一份**可以从页面直接落到交互原型和前端任务拆分的执行规范**。

## 0.3 产品核心因果链

所有页面最终都应服务于以下链路：

```text
课程理解当前内容
→ 学生在学习中产生行为或问题
→ 系统检索课程与学科证据
→ 系统形成可解释的学习判断
→ 选择讲解、可视化、实验、补学或验证
→ 记录学习证据
→ 返回原课程位置继续学习
```

页面不能把“聊天、图谱、代码、视频、任务”设计成互不相干的功能岛。

---

# 1. 产品空间、用户与权限

## 1.1 两种首页必须分离

系统必须区分：

```text
/       公开产品展示首页
/app    登录后的工作首页
```

### 公开产品展示首页

面向：

- 首次访问者；
- 比赛评委；
- 学校或企业客户；
- 尚未登录的教师和学生。

目标：解释产品价值和核心技术链路，引导体验或登录。

### 登录后工作首页

面向已经有明确课程、任务和角色的用户。

目标：让用户快速继续最重要的学习、建设、审核或实验工作。

**禁止**把公开宣传首页直接作为登录后的日常 Dashboard。日常用户不应每次登录都重新观看产品宣传。

## 1.2 平台身份

平台仅保留：

```text
用户
管理员
```

管理员是拥有平台治理权限的用户，不形成独立的“管理员产品世界”。

## 1.3 课程角色

课程内部仅保留：

```text
学生
教师
```

角色属于“用户与课程的关系”，而不是全局身份。

一个用户可以同时：

- 在“数据结构与算法”中是学生；
- 在“人工智能通识”中是教师；
- 在实验室进行自主实验；
- 在资源库管理自己的文件。

因此：

- 禁止设置全局“学生模式 / 教师模式”切换器；
- 课程卡片和课程页必须明确显示当前课程角色；
- 进入课程后，导航根据当前课程角色自动变化。

## 1.4 教师预览学生视角

教师可以使用“学生视角预览”，但必须满足：

- 页面始终显示“预览模式”标记；
- 不创建正式学生成员关系；
- 不写入正式学习进度；
- 不进入学生统计和认知模型；
- 退出预览后返回教师原页面和原位置。

## 1.5 权限展示原则

- 用户完全无权访问的入口应直接隐藏；
- 暂时不可用但用户理解上需要知道的功能，可以禁用，并说明条件；
- AI 候选、教师确认、正式发布、实验能力、研究预览必须明确区分；
- 权限不足不能只显示空白页，必须解释原因并提供返回路径。

---

# 2. 信息架构与推荐路由

> 路由是推荐命名，用于消除页面理解歧义。工程可调整路径字符串，但不可改变页面层级。

## 2.1 平台一级空间 L1

```text
首页｜我的课程｜实验室｜资源库｜智能体
```

管理员额外显示：

```text
平台管理
```

推荐路由：

```text
/app                         登录后首页
/app/courses                 我的课程
/app/lab                     实验室
/app/resources               资源库
/app/eduagent                 教学智能体
/app/admin                   平台管理，仅管理员
```

## 2.2 “我的课程”二级空间 L2

```text
我学习的｜我建设的｜课程大厅
```

右上角主操作：

```text
[加入课程]
```

推荐路由：

```text
/app/courses/learning        我学习的
/app/courses/building        我建设的
/app/courses/hall            课程大厅
```

“加入课程”默认不是二级页面，而是右侧聚焦抽屉。只有未来加入申请发展成独立管理空间时，才允许升级为页面。

## 2.3 进入课程后的 L2

学生：

```text
概览｜学习｜知识｜实验任务
```

教师：

```text
概览｜学习｜建设｜知识｜实验任务｜成员｜设置
```

推荐路由：

```text
/app/course/:courseId/overview
/app/course/:courseId/learn
/app/course/:courseId/build
/app/course/:courseId/knowledge
/app/course/:courseId/experiments
/app/course/:courseId/members
/app/course/:courseId/settings
```

学生不能看到“建设、成员、设置”。

## 2.4 平台实验室 L2

```text
实验大厅｜课程任务｜我的实验｜学习记录
```

推荐路由：

```text
/app/lab/hall
/app/lab/course-tasks
/app/lab/my-experiments
/app/lab/records
```

## 2.5 资源库 L2

```text
我的文件｜课程引用｜最近使用｜回收站
```

推荐路由：

```text
/app/resources/files
/app/resources/course-links
/app/resources/recent
/app/resources/trash
```

## 2.6 任务中心 L2

```text
我的待办｜我发起的｜系统任务｜已完成
```

推荐路由：

```text
/app/tasks/todo
/app/tasks/created
/app/tasks/system
/app/tasks/completed
```

---

# 3. 导航深度原则

## 3.1 顶部最多两层菜单

必须遵循：

```text
L1：平台空间
L2：当前空间的主要任务
L3：左侧 Local Rail
L4：禁止继续增加菜单层级
```

顶部只允许：

1. 一级平台导航；
2. 当前空间或课程的二级导航。

禁止出现第三排顶部 Tab。

## 3.2 第三级统一进入 Local Rail

当一个二级页面内部存在多个真正不同的工作空间时，必须使用左侧可收缩 `Local Rail`。

例如课程“知识”：

```text
结构视图
原文引用
候选审核
版本记录
```

例如课程“建设”：

```text
资料
课程结构
教学讲授
页面映射
媒体生成
课程校验
发布记录
```

例如课程“设置”：

```text
基础信息
加入与发布
智能体
安全与合规
沙箱权限
平台集成
```

## 3.3 Local Rail 何时使用

只有满足以下任一条件才使用：

- 子项会切换主要工作区；
- 子项具有独立 URL 和返回价值；
- 子项具有自己的保存、权限、异常和空状态；
- 用户需要频繁在子项间切换；
- 子项是一条长期流程中的不同阶段。

下列内容不是 Local Rail：

- 全部 / 待审核 / 已通过，这属于筛选器；
- 探索 / 审核 / 编辑，这属于工作模式；
- 发布 / 运行 / 保存，这属于动作；
- 日期、学科、状态、课程等条件，这属于过滤条件。

## 3.4 Local Rail 的展开行为

### 管理、建设、治理页面

默认展开，例如：

- 课程建设；
- 知识审核；
- 成员；
- 设置。

用户手动收缩后，主工作区扩展。系统记住该用户在该设备上的选择。

### 沉浸任务页面

默认收缩，例如：

- 课程学习；
- 代码实验；
- 图谱全屏探索；
- 原文对照阅读。

用户点击展开时，Local Rail 必须以悬浮层覆盖在主区上方，不能再次压缩已经进入专注状态的主工作区。

## 3.5 面包屑

允许显示：

```text
我的课程 / 数据结构与算法 / 知识 / 候选审核
```

面包屑用于定位，不算菜单层级。

要求：

- 不与二级导航争夺视觉权重；
- 最后一级不可点击；
- 点击上级应回到对应页面，而不是仅修改筛选器；
- 学习页全屏时可以隐藏面包屑。

---

# 4. App Shell 详细规范

## 4.1 桌面端基本结构

```text
┌──────────────────────────────────────────────────────────────────┐
│ L1 一级导航：Logo / 首页 / 我的课程 / 实验室 / 资源库 / 智能体     │
├──────────────────────────────────────────────────────────────────┤
│ L2 二级导航或课程导航                                            │
├──────────────────────────────────────────────────────────────────┤
│ 页面上下文：面包屑 / 页面标题 / 状态 / 主操作                     │
├──────────────┬──────────────────────────────────┬────────────────┤
│ Local Rail   │ 主工作区                         │ 右侧回应区     │
│ 按需出现     │ 页面真正任务                     │ 按需滑入       │
└──────────────┴──────────────────────────────────┴────────────────┘
```

## 4.2 L1 一级导航内容

左侧：

- 产品 Logo；
- 首页；
- 我的课程；
- 实验室；
- 资源库；
- 智能体。

右侧：

- 全局搜索；
- 通知入口；
- 当前账号头像与菜单；
- 管理员入口，仅管理员显示。

要求：

- 一级导航在登录后页面保持稳定；
- 进入课程后仍保留一级导航；
- 课程名不能替代全局 Logo；
- 当前一级空间必须有明确选中状态。

## 4.3 L2 二级导航内容

进入“我的课程”时显示：

```text
我学习的｜我建设的｜课程大厅
```

进入课程时显示：

- 左侧课程名和课程角色；
- 中间课程二级导航；
- 右侧课程级主操作，例如“预览课程”“继续建设”。

进入实验室、资源库、任务时显示各自二级导航。

## 4.4 页面上下文区

页面上下文区不是第三层导航，应包含：

- 面包屑，可选；
- 页面标题；
- 一句必要说明，可选；
- 当前对象状态；
- 一个主操作；
- 最多两个次操作，更多操作进入“更多”菜单。

例如课程建设“页面映射”：

```text
数据结构与算法 / 建设 / 页面映射
页面映射                       自动保存于 10:32
将教学节点与课件页面建立对应关系
[运行自动映射] [预览课程]
```

## 4.5 右侧回应区

右侧区域统一承担临时、上下文化内容：

- 教学智能体回应；
- AI 候选；
- 原文引用详情；
- 成员详情；
- 资源详情；
- 失败原因与日志；
- 图谱节点详情；
- 高风险确认。

右侧区域有两种形态：

### 固定辅助面板

适用于持续编辑与审核，例如课程建设、知识候选审核。

- 可以与主区并排；
- 可手动收起；
- 切换主对象时内容同步更新。

### 临时抽屉

适用于加入课程、查看详情、确认发布、查看日志。

- 从右侧滑入；
- 关闭后焦点回到触发按钮；
- 不改变主页面 URL，除非详情本身需要可分享链接。

## 4.6 页面返回行为

- 浏览器返回必须回到用户上一层真实页面，不应丢失筛选、滚动位置和选中项；
- 关闭抽屉不触发整页返回；
- 学习分支返回必须回到原课程时间点或知识节点；
- 从课程进入实验后，完成按钮必须明确写“返回课程”，不能只写“关闭”。

---

# 5. 通用页面模板

## 5.1 浏览列表页模板

适用：

- 我学习的；
- 我建设的；
- 课程大厅；
- 资源库；
- 智能体；
- 任务；
- 成员列表。

结构：

```text
页面标题 + 主操作
搜索与筛选工具条
内容列表 / 表格 / 卡片
分页或加载更多
右侧详情抽屉，按需出现
```

要求：

- 搜索、筛选、排序必须在同一区域，不分散到不同角落；
- 首屏只显示帮助决策的字段；
- 一个条目只能有一个默认主操作；
- 批量操作只有在用户选中条目后才出现；
- 点击条目空白区域打开详情，点击主按钮执行主流程。

## 5.2 建设与治理页模板

适用：

- 课程建设；
- 知识治理；
- 设置；
- 安全策略。

结构：

```text
左侧 Local Rail
中央主编辑区
右侧候选 / 引用 / 冲突 / 说明面板
顶部保存与状态
```

要求：

- 中央区只编辑一个当前对象；
- 右侧不能成为第二个完整页面；
- 修改后必须显示保存状态；
- 离开存在未保存修改的页面时必须确认；
- AI 生成内容必须先以候选出现，不自动覆盖教师正式内容。

## 5.3 连续学习页模板

适用：

- 课程学习；
- 课程内实验；
- 补学分支。

结构：

```text
轻量课程上下文
可收缩学习轨道
自适应课程舞台
底部教学行动工具坞
提问时出现右侧课程智能体面板
```

要求：

- 用户始终知道当前课程、知识点和返回位置；
- AI 不永久占据屏幕；
- 每个学习分支都要定义进入原因、完成条件、返回位置；
- 全屏讲解不等于退出课程，退出全屏后恢复原布局。

## 5.4 实验工作区模板

结构：

```text
顶部实验上下文和安全状态
左侧任务说明或文件结构，按需收缩
中央编辑器 / 可视化画布
右侧测试、诊断或提示面板
底部运行状态，可与右侧合并
```

要求：

- 首屏必须看到实验目标和完成条件；
- 运行前可看到当前沙箱权限；
- 运行后必须区分编译错误、运行错误、测试失败、超时和安全阻断；
- Agent 提示必须分层，不默认直接给完整答案；
- 课程实验完成后可返回课程原位置。

## 5.5 聚焦抽屉模板

适用：

- 加入课程；
- 创建课程；
- 发布课程；
- 创建实验任务；
- 高风险操作确认；
- 资源详情。

要求：

- 标题说明当前动作；
- 内容按单一目标组织；
- 底部固定主操作与取消；
- 多于三个主要步骤时改成独立分步页面；
- 关闭时保留用户已输入但未提交的草稿，或明确提示将丢失。

---

# 6. 通用组件内容契约

> 本节定义组件“包含什么”和“怎样工作”，外观遵循 `design.md`。

## 6.1 `CourseRoleBadge`

必须显示：

- `学生` 或 `教师`；
- 预览时显示 `学生视角预览`。

禁止使用模糊的“创建者”“参与者”代替课程角色。

## 6.2 `StatusBadge`

状态文字必须可直接理解，不只放颜色点。

通用状态：

```text
草稿
处理中
待确认
已发布
已完成
失败
存在冲突
实验能力
研究预览
```

## 6.3 `CourseCard`

### 学习课程卡

必须包含：

- 课程名称；
- 当前角色：学生；
- 教师或课程来源；
- 学习进度；
- 上次学习位置；
- 未完成任务数量；
- 主操作：继续学习。

可选：

- 下一截止时间；
- 最近课程回应。

禁止在课程卡上塞入完整知识图谱、学情雷达图或大量统计。

### 建设课程卡

必须包含：

- 课程名称；
- 当前角色：教师；
- 发布状态；
- 当前建设阶段；
- 待确认项数量；
- 失败任务数量；
- 主操作：继续建设。

## 6.4 `LocalRailItem`

可包含：

- 图标；
- 名称；
- 当前状态；
- 待处理数量；
- 完成标记；
- 异常标记。

点击后：

- 切换当前三级工作区；
- 更新 URL；
- 保留当前二级导航；
- 不打开右侧抽屉。

## 6.5 `FilterToolbar`

顺序必须稳定：

```text
搜索 → 核心筛选 → 排序 → 视图切换 → 批量操作
```

要求：

- 当前筛选条件可见；
- 支持一键清除；
- 筛选结果为空时显示“没有符合条件的结果”，不能误显示首次空状态。

## 6.6 `RightDetailDrawer`

必须包含：

- 标题；
- 对象状态；
- 关键信息；
- 相关操作；
- 关闭入口。

禁止在抽屉中再打开第二个抽屉。更深详情改为抽屉内分区或独立页面。

## 6.7 `SystemResponsePanel`

用于 AI、规则系统或任务系统的上下文回应。

至少包含：

1. 系统观察到什么；
2. 依据是什么；
3. 建议用户做什么；
4. 用户可以接受、修改、忽略或查看依据。

禁止只展示一段没有来源、没有动作的长文本。

## 6.8 `CitationBlock`

必须包含：

- 来源类型：PPT / 教材 / 讲义 / 教案 / 代码规范等；
- 文件名或资料名；
- 页码、章节或位置；
- 引用片段；
- 与当前回答的关联说明；
- 查看原文操作。

状态必须区分：

```text
精确引用
近似匹配
来源已更新
来源失效
仅教师可见
```

学生端统一命名为“原文引用”，不显示内部术语 `Evidence Viewer`。

## 6.9 `LearningTrack`

用于课程学习页左侧。

必须显示：

- 章节；
- 当前知识点；
- 已完成、当前、未开始状态；
- 分支学习的返回标记；
- 可跳转节点，仅在教师允许时启用。

它不是建设页的 Local Rail。两者可以复用外观骨架，但数据语义不同。

## 6.10 `LearningActionDock`

固定操作：

```text
提问｜试一试｜看可视化｜做笔记｜原文引用
```

规则：

- 普通讲解状态显示；
- 全屏讲解时默认隐藏，移动鼠标或键盘操作后短暂显示；
- 当前正在使用的活动显示选中状态；
- 不加入“聊天”“Agent”“工具箱”等模糊入口；
- 不把“下一节”塞入工具坞，下一节属于课程导航。

## 6.11 `CapabilityMaturityTag`

用于展示真实能力成熟度：

```text
产品能力
实验能力
研究预览
```

必须用于：

- 公开首页技术演示；
- 尚未正式接管主流程的知识图谱与认知算法；
- Shadow / Dry-run 能力；
- 试验性多图 RAG。

---

# 7. 公开产品展示首页 `/`

## 7.1 页面目标

公开首页必须在不要求用户理解技术名词的前提下，说明：

1. 静态资料如何成为智能课程；
2. 课程如何理解知识关系；
3. 回答如何找到可信原文；
4. 系统如何观察学习证据；
5. 课程如何选择下一步教学行动；
6. 教师如何保持控制；
7. 计算机垂类如何将知识变成可执行验证。

首页不是后台功能大合集，也不是自动播放的动画展馆。

## 7.2 页面区块顺序

必须按以下顺序组织：

### A. Hero

内容：

```text
主标题：让课程回应学习
副标题：将静态教学资料转化为能够理解内容、引用原文、感知学习状态并采取下一步教学行动的智能课程。
主操作：体验一节会回应的课
次操作：进入我的课程
```

Hero 演示区只展示一个小型、可理解的学习回应，不展示完整后台。

示例：

```text
学生：为什么二分查找要求数组有序？
课程：先用一个无序数组验证一次，再回到区间排除原理。
```

随后展示课程从讲解切换到可视化或代码验证。

### B. 从资料到课程

展示固定流程：

```text
PPT / PDF / 教案
→ 文档解析
→ 章节与知识点
→ 教学脚本
→ 页面映射
→ 原文引用
→ 可发布课程
```

交互：

- 用户点击某一步，查看该步骤输入、输出和教师控制点；
- 默认只展开一个步骤；
- 明确显示 AI 输出是“候选”，教师可修改和确认。

### C. 知识图谱浏览器

必须使用真实局部知识图谱，而不是随机粒子图。

默认场景：二分查找。

默认显示：

```text
中心：二分查找
先修：数组、有序性、循环
核心：区间收缩、中点计算
易错：边界更新、死循环、溢出
延伸：lower_bound、二分答案
```

可操作：

- 点击节点聚焦；
- 展开一跳；
- 切换“先修 / 易错 / 原文”图层；
- 点击原文标记查看真实引用；
- 查看“为什么推荐补学”。

限制：

- 默认节点数应保持可读；
- 不自动无限扩展；
- 图谱能力未正式上线时标记“实验能力”。

### D. 多图 RAG 检索流程

使用同一个问题贯穿：

```text
二分查找为什么要求数组有序？
```

动画阶段：

```text
1. 理解问题和当前课程上下文
2. 限定 course_id / 课程资料范围
3. 命中“二分查找”和“有序性”
4. 在课程知识图中扩展关系
5. 在文档证据图中召回 PPT / 教材 / 例题
6. 根据当前学习状态选择解释深度
7. 重排并校验证据
8. 生成带原文引用的回答
```

每一步必须显示真实输入输出摘要，不能只用光点流动代替过程。

### E. 学习证据与认知判断

不要以泛化神经网络为主体。主体应是学习事件如何变成教学判断。

输入事件：

- 回答是否正确；
- 代码测试通过数；
- 重复错误类型；
- 使用提示层级；
- 能否解释原理；
- 隔天复测结果。

中间状态：

```text
概念理解
程序实现
迁移应用
调试能力
稳定程度
提示依赖
```

输出示例：

```text
概念理解基本稳定
边界迁移证据不足
代码实现仍依赖二级提示
建议：先看边界可视化，再完成一个变式实验
```

如果模型仍处于研究阶段，显示“研究预览”。

### F. 课程采取教学行动

展示同一课程界面在以下状态间切换：

```text
正常讲解
提问解释
看可视化
试一试
验证完成
返回课程
```

这一区块是整个首页价值链的收束，必须明确说明：系统不是只生成回答，而是选择下一种教学活动。

### G. 计算机垂类实验

展示：

```text
课程智能体
→ Coding Agent
→ 安全沙箱
→ 编译 / 运行 / 测试
→ 执行轨迹
→ 分层提示
→ 学生修改
→ 复测
```

第一阶段只展示计算机能力，不展示空壳化学、医学实验室。

### H. 教师治理与安全

必须展示：

- AI 候选等待教师确认；
- 原文引用要求；
- 课程版本；
- 安全围栏；
- 沙箱网络策略；
- CTF 隔离靶场；
- 高风险调用审批；
- 回滚能力。

主文案：

```text
AI 提出候选，教师决定什么进入正式课程。
```

### I. 结束行动区

操作：

- 体验示例课程；
- 进入我的课程；
- 教师创建课程。

根据登录状态显示不同主操作。

## 7.3 动画规则

- 每个视口只允许一个主要动画；
- 动画进入视口后播放一次；
- 支持暂停和重播；
- 不自动循环干扰阅读；
- 减少动态效果模式下改为分步静态图；
- 技术动画必须支持用户手动逐步查看。

## 7.4 公开首页验收

- 不登录也能在三分钟内理解产品主张；
- 不依赖“多图 RAG、认知模型”等术语才能理解价值；
- 每个技术演示都能看到真实输入、过程、输出和成熟度；
- 页面不存在随机节点、无来源数字或伪造实时数据；
- 最终能回到“课程如何采取行动”，而不是停留在技术展示。

---

# 8. 登录后工作首页 `/app`

## 8.1 页面目标

回答：

> 用户现在最值得继续什么？

首页不做管理驾驶舱，不堆大量图表。

## 8.2 信息优先级

系统按以下顺序生成首页内容：

1. 最近中断且可继续的学习或建设；
2. 即将截止的课程任务；
3. 需要用户确认或处理的异常；
4. 教师待审核项；
5. 最近实验、资源与记录。

## 8.3 页面结构

### A. 继续进行

最多展示 3 个条目。

学生条目：

- 课程名；
- 角色；
- 当前章节与知识点；
- 上次位置；
- 进度；
- 主操作：继续学习。

教师条目：

- 课程名；
- 角色；
- 当前建设步骤；
- 待确认数量；
- 最近保存时间；
- 主操作：继续建设。

### B. 需要处理

按紧急程度排序：

- 即将截止任务；
- 失败系统任务；
- 待审核加入申请；
- 缺失原文引用；
- 安全策略未配置；
- 实验等待复测。

每条只提供一个直接处理入口。

### C. 系统回应

最多展示 1 到 2 条高价值洞察，例如：

```text
“边界条件”问题在本课程近期集中出现。
依据：12 次提问、7 次相同测试失败。
建议：为 3.2 节补充边界可视化。
[查看学习信号]
```

禁止展示泛化鼓励语或没有可执行动作的 AI 摘要。

### D. 最近内容

三组轻量入口：

- 最近课程；
- 最近实验；
- 最近资源。

## 8.4 空状态

### 新用户无课程

展示：

- 加入课程；
- 浏览课程大厅；
- 教师创建课程，若账号有教师权限；
- 体验示例课程。

### 有课程但无待办

显示“当前没有需要立即处理的事项”，并提供继续最近课程，不显示空白。

## 8.5 首页验收

- 首屏能看到一个明确主任务；
- 同一课程的学生和教师角色不会混淆；
- 每张卡片只有一个主操作；
- 不出现没有解释意义的统计大图；
- 点击继续后准确回到上次位置。

---

# 9. 我的课程

# 9.1 我学习的 `/app/courses/learning`

## 页面目标

集中管理用户作为学生参与的课程，并快速恢复学习。

## 页面结构

### 页面标题区

- 标题：我学习的；
- 说明：当前作为学生参与的课程；
- 主操作：加入课程。

### 工具条

- 搜索课程名或教师；
- 状态筛选：学习中、已完成、已归档；
- 排序：最近学习、即将截止、课程名称；
- 视图：卡片 / 紧凑列表，可选。

### 课程条目

必须展示：

- 课程名；
- 教师；
- 角色：学生；
- 学习进度；
- 上次位置；
- 下一待办；
- 最近学习时间；
- 主操作：继续学习。

次操作放入更多菜单：

- 查看课程概览；
- 查看实验任务；
- 归档；
- 退出课程，仅在课程规则允许时。

## 交互

- 点击课程卡空白区域进入课程概览；
- 点击“继续学习”直接进入上次学习位置；
- 点击任务信息进入对应测验或实验；
- 退出课程必须显示影响说明，不能一键直接退出。

## 空状态

- 首次空：引导加入课程和浏览课程大厅；
- 筛选空：显示清除筛选；
- 同步异常：显示泛雅同步状态和重试入口。

# 9.2 我建设的 `/app/courses/building`

## 页面目标

集中管理用户作为教师建设或共同建设的课程。

## 页面结构

### 页面标题区

- 标题：我建设的；
- 主操作：创建课程；
- 次操作：导入泛雅课程，可选。

### 工具条

- 搜索；
- 状态：草稿、校验中、已发布、已归档；
- 异常：有失败任务、有待审核项、缺少安全配置；
- 排序：最近编辑、最近发布、课程名称。

### 课程条目

必须展示：

- 课程名；
- 角色：教师；
- 发布状态；
- 当前建设步骤；
- 最近编辑时间；
- 待确认候选数；
- 缺失原文引用数；
- 失败任务数；
- 主操作：继续建设。

次操作：

- 课程概览；
- 学生视角预览；
- 发布记录；
- 归档。

## 交互

- “继续建设”进入最近编辑的 Local Rail 子页面；
- 有失败任务时点击数量直接进入对应任务详情；
- 学生视角预览打开新页面状态，并持续显示预览标记。

# 9.3 课程大厅 `/app/courses/hall`

## 页面目标

发现课程并理解加入条件，不直接承担正式学习。

## 页面结构

### 搜索区

支持：

- 课程名称；
- 教师；
- 学科；
- 学校或组织，可选。

### 筛选

- 学科；
- 加入方式：直接加入、申请审核、邀请码；
- 是否包含实验；
- 开课状态；
- 来源：本平台、泛雅同步。

### 课程卡

展示：

- 课程名；
- 教师；
- 简介摘要；
- 学科；
- 开课时间；
- 加入方式；
- 是否有实验任务；
- 当前人数，可选；
- 主操作：查看详情或加入。

### 课程详情抽屉

必须展示：

- 完整简介；
- 教师；
- 课程大纲；
- 学习要求；
- 开课时间；
- 加入规则；
- 实验与沙箱说明；
- 数据与隐私说明；
- 加入操作。

## 加入状态

- 可直接加入；
- 需要申请；
- 需要邀请码；
- 已加入；
- 申请中；
- 已拒绝；
- 已关闭加入。

# 9.4 加入课程抽屉

## 入口

- “我的课程”右上角；
- 登录后首页；
- 课程大厅；
- 邀请链接。

## 内容顺序

```text
1. 输入邀请码
2. 浏览课程大厅
3. 查看泛雅同步发现课程
4. 查看当前申请状态
```

## 邀请码结果

必须区分：

- 邀请码有效，可加入；
- 邀请码不存在；
- 邀请码过期；
- 已经加入；
- 课程关闭加入；
- 当前账号无资格；
- 需要补充申请信息。

成功后：

- 显示课程名和角色；
- 主操作“进入课程”；
- 次操作“留在当前页面”。

---

# 10. 课程空间通用规范

## 10.1 进入课程时

页面必须获取并展示：

- 课程名称；
- 当前用户课程角色；
- 课程发布状态；
- 是否预览模式；
- 当前学期或班级，可选。

## 10.2 学生课程导航

```text
概览｜学习｜知识｜实验任务
```

## 10.3 教师课程导航

```text
概览｜学习｜建设｜知识｜实验任务｜成员｜设置
```

“学习”对教师表示课程预览与内容体验，不写入学生进度。

## 10.4 课程上下文保持

在课程内切换二级页面时必须保持：

- course_id；
- 当前课程角色；
- 最近访问位置；
- 未保存状态提示；
- 课程版本信息。

---

# 11. 课程概览

# 11.1 学生概览

## 页面目标

让学生知道课程学到哪里、下一步做什么、近期系统如何回应。

## 页面结构

### A. 继续学习

必须展示：

- 当前章节；
- 当前知识点；
- 上次时间点或阅读位置；
- 主操作：继续学习。

### B. 课程进度

展示：

- 已完成知识点 / 总知识点；
- 当前章节进度；
- 已完成实验与测验。

不展示未经验证的“综合能力百分比”。

### C. 当前待办

- 测验；
- 实验任务；
- 补学分支；
- 截止时间。

### D. 最近课程回应

示例：

```text
你在“左闭右开区间”上出现两次相同错误。
建议先查看边界可视化，再返回代码实验。
[继续这个学习分支]
```

必须说明依据和建议动作。

### E. 课程结构摘要

展示当前章前后位置，不在概览中加载完整知识图谱。

# 11.2 教师概览

## 页面目标

展示课程当前健康状态和最需要教师处理的事项。

## 页面结构

### A. 课程状态

- 草稿 / 已发布；
- 当前版本；
- 最近发布时间；
- 预览课程；
- 继续建设。

### B. 需要处理

按优先级：

- 课程校验失败；
- 媒体任务失败；
- 缺少原文引用；
- 知识候选待审核；
- 加入申请；
- 实验异常提交；
- 安全策略缺失。

### C. 学习信号

仅展示可解释聚合：

- 集中提问知识点；
- 高频错误类型；
- 实验完成率；
- 课程中断位置。

不得把学生永久贴成“差生”“低能力”。

### D. 快捷入口

- 继续建设；
- 查看知识；
- 查看实验任务；
- 查看成员。

---

# 12. 课程学习：Adaptive Learning Canvas

# 12.1 页面目标

课程学习页不是固定播放器，也不是“播放器 + 聊天框 + 代码编辑器”的三栏拼盘。

它是一块根据教学活动切换形态的课程舞台。

## 12.2 稳定区域

必须长期保留：

1. 顶部轻量课程上下文；
2. 左侧学习轨道；
3. 中央课程舞台；
4. 底部教学行动工具坞。

### 顶部课程上下文

显示：

- 课程名；
- 章节和知识点；
- 当前播放时间或阅读进度；
- 当前活动名称；
- 全屏入口；
- 返回课程概览，可选。

顶部不得过高，不放复杂统计和多个按钮。

### 学习轨道

显示：

- 章节层级；
- 当前节点；
- 完成状态；
- 当前分支；
- 返回点。

正常讲解可展开。进入提问、实践、可视化、笔记或原文时自动收缩。

### 教学行动工具坞

固定顺序：

```text
提问｜试一试｜看可视化｜做笔记｜原文引用
```

## 12.3 学习状态机

```text
LEARN 正常讲解
UNDERSTAND 提问理解
PRACTICE 试一试
VISUALIZE 看可视化
NOTE 做笔记
CITATION 原文引用
VERIFY 验证完成
```

每次只能有一个主状态。

## 12.4 LEARN 正常讲解

### 页面内容

中央课程舞台可承载：

- PPT + 讲解；
- 视频；
- 数字人；
- 讲解文本；
- 教师插入的提问点；
- 小型随堂题。

### 交互

- 播放、暂停、进度跳转；
- 教师允许时点击章节跳转；
- 一键全屏；
- 点击工具坞进入其他活动。

### AI 行为

AI 默认保持安静。只有以下情况出现轻量提示：

- 学生主动提问；
- 教师配置了互动点；
- 学生多次失败；
- 课程需要确认是否理解。

禁止常驻大聊天面板。

## 12.5 UNDERSTAND 提问

### 触发

- 点击“提问”；
- 选中课程文本后选择“提问”；
- 在时间点标记处输入问题。

### 布局变化

必须按以下顺序：

1. 学习轨道自动收缩；
2. 原课程内容继续保留；
3. 原课程内容整体向左平移并适度缩窄；
4. 教学智能体面板从右侧滑入；
5. 当前课程时间点保持暂停或由用户选择继续播放。

### 智能体面板内容

顶部：

- 当前锚点：课程时间、PPT 页或选中文本；
- 当前知识点；
- 关闭面板。

对话区：

- 学生问题；
- 课程智能体回答；
- 原文引用；
- 建议教学行动。

底部操作：

```text
换一种解释
举个例子
看可视化
用代码验证
出一道小题
```

### 回答要求

回答必须：

- 结合当前课程上下文；
- 区分课程内明确答案和超出课程范围；
- 有来源时显示原文引用；
- 无可靠来源时明确说明；
- 不自动触发实验或跳转，需学生确认。

### 退出

关闭面板后：

- 课程内容恢复原宽度；
- 学习轨道保持用户进入前状态；
- 播放状态按用户选择恢复；
- 对话记录保留在当前知识点上下文。

## 12.6 PRACTICE 试一试

### 触发

- 点击“试一试”；
- 智能体建议代码验证并经用户确认；
- 教师在课程中插入实践活动。

### 布局变化

1. 学习轨道自动收缩；
2. 原课程面板从左侧滑出；
3. 实践面板从右侧滑入；
4. 实践面板占据中央主舞台；
5. 不再保留原课程画面并排显示；
6. 顶部保留课程、知识点、进入原因和返回位置。

### 首屏内容

必须包含：

- 为什么进入这个实践；
- 学习目标；
- 任务说明；
- 完成条件；
- 返回课程位置；
- 当前沙箱权限摘要。

### 代码实践内容

- 编程语言；
- 开始代码；
- 编辑器；
- 运行；
- 测试；
- 测试结果；
- Coding Agent 提示；
- 复测。

### 完成条件

可以是：

- 指定测试全部通过；
- 正确解释关键步骤；
- 完成可视化操作；
- 教师配置的组合条件。

完成后进入 VERIFY，不直接跳回课程。

## 12.7 VISUALIZE 看可视化

### 布局变化

与 PRACTICE 相同：原课程左出，可视化右入，中央独占。

### 首屏内容

- 当前知识点；
- 可视化目标；
- 控件说明；
- 当前步骤；
- 返回位置。

### 可视化必须支持

- 播放 / 暂停；
- 单步前进 / 后退；
- 重置；
- 速度调整；
- 关键变量或状态说明；
- 与当前课程原理的对应解释。

### 禁止

- 只播放无法控制的装饰动画；
- 没有状态解释；
- 与课程代码或知识点脱节。

## 12.8 NOTE 做笔记

### 布局变化

原课程左出，笔记工作区右入。

### 自动上下文

系统自动绑定：

- 课程；
- 章节；
- 知识点；
- 当前时间点或页码；
- 触发来源。

### 笔记内容

- 标题；
- 正文；
- 标签；
- 插入当前问题；
- 插入实验结论；
- 插入原文引用；
- 插入截图或代码片段，可选。

### 保存行为

- 自动保存草稿；
- 手动“完成笔记”后返回课程；
- 关闭前若保存失败必须提示；
- 保存成功后显示笔记入口，但不弹出庆祝页面。

## 12.9 CITATION 原文引用

### 布局变化

原课程左出，原文阅读区右入。

### 内容

左侧或顶部来源列表：

- PPT；
- 教材；
- 教案；
- 讲义；
- 代码规范；
- 相关例题。

正文区：

- 原始页面或文本；
- 高亮引用片段；
- 页码或章节；
- 关联说明；
- 返回课程。

### 状态

- 精确匹配；
- 近似匹配；
- 来源更新；
- 来源失效；
- 当前无可用原文。

无原文时禁止伪造引用，应提供“向教师反馈缺失引用”。

## 12.10 VERIFY 验证完成

### 进入条件

- 实验完成；
- 可视化观察完成；
- 小题验证完成；
- 教师配置的完成条件满足。

### 内容

- 本次做了什么；
- 获得了什么结果；
- 哪些证据被记录；
- 仍需关注什么；
- 下一步。

操作：

```text
返回课程
继续下一节
再练一题
查看本次记录
```

默认主操作是“返回课程”，并明确返回时间点。

## 12.11 学习分支上下文

进入任何分支时必须创建：

```text
source_course_id
source_section_id
source_knowledge_point_id
source_position
trigger_action
learning_goal
completion_condition
return_target
```

用于保证返回正确位置，而不是仅依赖浏览器历史。

## 12.12 全屏讲解

全屏时：

- 隐藏 L1、L2 和普通工具区域；
- 保留极简退出、进度和播放控制；
- 工具坞默认隐藏；
- 用户移动鼠标或键盘操作后短暂显示控制；
- 退出后恢复进入全屏前布局和播放位置。

---

# 13. 课程智能体与 Coding Agent

## 13.1 前端统一人格

学生只面对“课程智能体”。

前端可以显示当前调用能力，例如：

```text
正在检索课程原文
正在运行代码测试
正在分析执行轨迹
```

但禁止让学生在多个聊天人格之间选择“课程 Agent / Coding Agent / RAG Agent”。

## 13.2 课程智能体职责

- 理解当前课程内容；
- 理解当前学习目标；
- 判断学生更需要解释、例子、可视化、实验、补学还是验证；
- 保持分支返回路径；
- 组织最终对学生的统一回应。

## 13.3 Coding Agent 职责

- 读取当前代码和任务；
- 编译、运行、测试；
- 分析错误和首次分歧；
- 观察变量、调用栈或执行轨迹；
- 提供分层提示；
- 复测修改结果。

## 13.4 结构化 Learning Context

Agent 之间只共享必要结构化上下文：

```text
course_id
section_id
knowledge_point_id
learning_goal
source_position
student_question
candidate_difficulties
used_explanations
used_hint_levels
citations
experiment_task
current_code
compile_result
run_result
test_result
teacher_requirements
safety_policy
completion_condition
return_target
```

禁止默认把全部历史对话、全部课程资料和全部学生数据无边界传递给每个 Agent。

---

# 14. 教师课程建设 `/app/course/:courseId/build`

## 14.1 Local Rail

固定顺序：

```text
资料
课程结构
教学讲授
页面映射
媒体生成
课程校验
发布记录
```

每项可以显示：

- 完成；
- 当前；
- 待处理数量；
- 失败；
- 未开始。

## 14.2 建设页通用布局

```text
左：建设 Local Rail
中：当前步骤主工作区
右：AI 候选 / 原文 / 冲突 / 任务状态
```

顶部始终显示：

- 当前课程版本；
- 保存状态；
- 预览课程；
- 当前步骤主操作。

## 14.3 资料

### 页面目标

管理课程源资料，并观察解析状态。

### 主工作区

资料列表字段：

- 文件名；
- 类型；
- 来源；
- 上传者；
- 解析状态；
- 页数或大小；
- 最近更新时间；
- 被课程引用位置。

主操作：上传资料。

次操作：

- 从资源库选择；
- 重新解析；
- 替换版本；
- 查看解析结果；
- 移除课程引用。

### 解析状态

```text
等待解析
解析中
已完成
部分完成
失败
来源更新待重新解析
```

失败必须显示真实原因和重试入口。

### 右侧面板

选中文件后显示：

- 文件概要；
- 解析产物；
- 识别问题；
- 课程引用影响；
- 原文件入口。

## 14.4 课程结构

### 页面目标

将资料组织为章节、教学节点和学习顺序。

### 主工作区

左中区域显示结构树：

```text
课程
└── 章节
    └── 教学节点
        └── 学习活动
```

操作：

- 新增；
- 重命名；
- 拖动排序；
- 合并；
- 拆分；
- 设置预计时长；
- 设置是否允许跳过；
- 标记前置要求。

### AI 候选

右侧显示从资料解析得到的候选结构。

教师可以：

- 接受；
- 修改后接受；
- 忽略；
- 批量接受低风险候选。

候选不能自动覆盖正式结构。

### 冲突

结构修改影响页面映射、知识关系或媒体时，必须列出受影响对象。

## 14.5 教学讲授

### 页面目标

编辑每个教学节点的教学目标、讲稿和活动。

### 节点列表

显示：

- 节点名；
- 状态；
- 预计时长；
- 是否有讲稿；
- 是否配置活动；
- 是否有原文引用。

### 编辑区

字段：

- 节点标题；
- 教学目标；
- 讲稿；
- 关键概念；
- 关键问题；
- 易错点；
- 预计时长；
- 可选教学活动；
- 返回与完成条件。

### AI 右侧回应

- 讲稿候选；
- 解释方式候选；
- 活动建议；
- 缺失引用提示；
- 与资料冲突提示。

## 14.6 页面映射

### 页面目标

将教学节点与 PPT 页、PDF 页或资料片段建立明确对应。

### 布局

```text
左侧：教学节点列表
中央：课件页面预览
右侧：映射候选和冲突
```

### 操作

- 选择节点；
- 选择起止页；
- 添加多个来源片段；
- 接受自动映射；
- 调整映射；
- 标记无对应页面；
- 预览播放效果。

### 状态

- 已映射；
- 部分映射；
- 未映射；
- 冲突；
- 来源失效。

## 14.7 媒体生成

### 页面目标

管理 TTS、数字人、AI PPT 和其他媒体任务。

### 主列表字段

- 教学节点；
- 媒体类型；
- 输入版本；
- 任务状态；
- 进度；
- 失败原因；
- 输出预览；
- 更新时间。

### 操作

- 单个生成；
- 批量生成；
- 重试失败；
- 取消排队；
- 使用预生成素材；
- 查看日志；
- 重新绑定输出。

### 失败原则

禁止把失败伪装成成功。必须保留：

- 原始失败原因；
- 重试次数；
- 降级选项；
- 对发布的影响。

## 14.8 课程校验

### 页面目标

在发布前集中执行质量门禁。

### 校验组

```text
课程结构
页面映射
讲稿与教学目标
媒体就绪
原文引用
知识候选
实验任务
安全策略
外部依赖
```

每一项显示：

- 通过；
- 警告；
- 阻断；
- 不适用。

阻断项必须提供“前往修复”。

### 发布规则

- 存在阻断项时禁止正式发布；
- 警告项允许发布，但必须确认；
- 实验能力或研究预览不得伪装为正式质量项通过。

## 14.9 发布记录

### 内容

- 版本号；
- 发布时间；
- 发布者；
- 变更摘要；
- 学生影响范围；
- 索引和图谱版本；
- 回滚状态。

### 操作

- 查看版本；
- 对比差异；
- 回滚；
- 复制为新草稿；
- 查看发布日志。

回滚必须说明对学生进度、引用和实验任务的影响。

---

# 15. 课程知识空间 `/app/course/:courseId/knowledge`

## 15.1 Local Rail

教师：

```text
结构视图
原文引用
候选审核
版本记录
```

学生：

```text
知识结构
原文引用
```

学生不显示候选审核和版本记录的禁用入口，直接隐藏。

## 15.2 结构视图

### 页面目标

让用户理解知识关系，并从当前知识点进入相关内容、补学或原文。

### 默认视图

围绕当前节点展示：

- 当前知识点；
- 一跳先修；
- 一跳后继；
- 易错点；
- 核心原文；
- 关联实验。

### 页面区域

```text
左：Local Rail 和关系筛选
中：图谱画布
右：节点详情
```

### 画布操作

- 点击节点聚焦；
- 双击或明确按钮展开一跳；
- 收起分支；
- 回到当前课程节点；
- 搜索节点；
- 查看最短学习路径；
- 切换本章 / 本课程范围；
- 切换关系图层。

### 节点详情

必须显示：

- 名称；
- 类型；
- 定义；
- 当前状态；
- 先修与后继；
- 易错点；
- 相关课程位置；
- 原文引用；
- 关联实验；
- 成熟度状态。

### 教师模式

画布顶部工作模式：

```text
探索｜审核｜编辑
```

- 探索：只浏览；
- 审核：处理候选；
- 编辑：新增、修改和删除正式结构。

工作模式不是导航，不改变 Local Rail。

## 15.3 原文引用

### 页面目标

集中治理知识点、讲稿和回答所依赖的原文来源。

### 列表字段

- 知识点或教学节点；
- 来源文件；
- 页码或章节；
- 引用片段；
- 引用状态；
- 是否正式；
- 最近校验时间。

### 筛选

- 缺失引用；
- 近似匹配；
- 来源失效；
- 已确认；
- AI 候选。

### 操作

- 查看原文；
- 重新绑定；
- 确认为正式；
- 驳回候选；
- 批量检查来源更新。

## 15.4 候选审核

### 页面目标

审核 AI 提出的节点、关系、别名和引用候选。

### 结构

```text
左：候选列表
中：候选变更预览
右：依据、影响范围和操作
```

### 每个候选必须显示

- 候选类型；
- 原值与新值；
- 来源依据；
- 置信或规则说明，可选；
- 影响节点；
- 当前状态。

### 操作

- 接受；
- 修改后接受；
- 驳回；
- 暂缓；
- 查看原文；
- 查看下游影响。

禁止“一键全部通过”默认成为主操作。

## 15.5 版本记录

展示：

- 图谱快照版本；
- 创建时间；
- 创建者；
- 节点和关系变更；
- 对检索的影响；
- 是否正式发布；
- 回滚入口。

版本对比必须能看到：新增、删除、修改和冲突。

## 15.6 真实能力标记

必须区分：

```text
正式发布
教师确认
AI 候选
证据不足
实验能力
研究预览
```

---

# 16. 课程实验任务 `/app/course/:courseId/experiments`

## 16.1 学生视图

页面内筛选：

```text
待完成｜进行中｜已完成
```

这属于筛选器，不是 Local Rail。

实验任务条目必须展示：

- 名称；
- 关联知识点；
- 实验类型；
- 截止时间；
- 当前进度；
- 最近运行结果；
- 完成条件；
- 主操作：开始或继续。

完成后可查看学习记录和教师反馈。

## 16.2 教师视图

当功能达到多个稳定工作空间时启用 Local Rail：

```text
任务列表
创建任务
提交情况
安全策略
```

### 任务列表

字段：

- 任务名；
- 关联知识点；
- 状态；
- 截止时间；
- 提交人数；
- 异常数；
- 安全策略；
- 操作。

### 创建任务

字段：

- 名称；
- 描述；
- 关联知识点；
- 实验类型；
- 编程语言；
- 开始代码；
- 测试用例；
- 提示层级；
- 完成条件；
- 截止时间；
- 是否计入学习证据；
- 沙箱预设；
- 网络与文件权限；
- 发布范围。

创建任务超过三个步骤时使用独立分步页面：

```text
基本信息 → 实验内容 → 评测与提示 → 安全策略 → 预览发布
```

### 提交情况

字段：

- 学生；
- 状态；
- 首次通过数；
- 修改次数；
- 提示层级；
- 最终结果；
- 异常标记；
- 教师反馈。

教师查看的是课程任务范围内证据，不默认查看学生全部自主实验。

---

# 17. 成员 `/app/course/:courseId/members`

## 17.1 Local Rail

当功能完整时：

```text
成员列表
加入申请
课程分组
泛雅同步
同步记录
```

第一阶段功能不足时，可以只实现成员列表和加入申请，但不能放空壳菜单。

## 17.2 成员列表

字段：

- 姓名；
- 账号；
- 课程角色；
- 加入来源；
- 加入时间；
- 最近参与；
- 状态。

操作：

- 邀请；
- 修改课程角色；
- 移除；
- 查看课程参与摘要；
- 导出。

批量操作需先选中成员。

## 17.3 加入申请

字段：

- 申请人；
- 申请时间；
- 申请说明；
- 来源；
- 当前状态。

操作：

- 通过；
- 拒绝；
- 请求补充信息。

必须记录审核者和时间。

## 17.4 课程分组

可用于班级、小组或实验分组。

要求：

- 一个学生可以属于多个学习小组，仅在业务允许时；
- 分组不能改变课程角色；
- 删除分组不删除成员。

## 17.5 泛雅同步

显示：

- 最近同步时间；
- 新增成员；
- 计划移除；
- 冲突；
- 本地保留关系；
- 同步来源课程。

同步前必须预览变化，禁止静默覆盖本地成员关系。

---

# 18. 课程设置 `/app/course/:courseId/settings`

## 18.1 Local Rail

```text
基础信息
加入与发布
智能体
安全与合规
沙箱权限
平台集成
```

## 18.2 基础信息

字段：

- 课程名称；
- 简介；
- 封面；
- 学科；
- 学期；
- 教师；
- 课程语言；
- 归档状态。

课程 ID、创建时间等只读信息放在次级区域。

## 18.3 加入与发布

配置：

- 是否公开出现在课程大厅；
- 加入方式；
- 邀请码；
- 申请审核；
- 开课和结束时间；
- 发布后是否通知学生；
- 学生是否可退出；
- 是否允许跳转章节。

## 18.4 智能体

配置：

- 课程智能体名称，可选；
- 回答范围：仅课程 / 允许学科扩展；
- 是否必须原文引用；
- 无依据时的行为；
- 允许的教学行动；
- 提示层级策略；
- 教师自定义说明；
- 试验能力是否启用。

禁止教师直接修改平台底层系统提示词全文。应通过受控字段配置。

## 18.5 安全与合规

### 三层边界

#### 平台硬边界

教师不可关闭：

- 宿主与容器隔离；
- 内网保护；
- 资源限制；
- 审计；
- 恶意持久化限制；
- 高风险系统调用限制。

#### 课程安全策略

预设：

```text
基础教学
专业课程
网络安全课程
CTF 隔离课程
```

教师可配置：

- 禁答主题；
- 必须引用主题；
- 课程白名单；
- 高风险任务需确认；
- 关键词辅助规则；
- Dry-run 观察；
- 正式启用。

关键词例如：

```text
ctf
漏洞利用
提权
端口扫描
恶意代码
```

关键词不能单独决定阻断。必须结合：

- 课程类型；
- 用户意图；
- 工具调用目标；
- 当前沙箱策略；
- 是否为隔离靶场。

#### 运行时围栏

学生实验中明确展示当前网络、文件、CPU、内存、时长和审计状态。

### 安全策略状态

```text
策略草稿
Dry-run 观察
正式启用
存在冲突
```

## 18.6 沙箱权限

教师选择预设：

```text
基础编程
算法实验
数据处理
网络安全隔离靶场
CTF 隔离环境
```

配置：

- 语言；
- 网络：关闭 / 白名单 / 隔离靶场；
- 文件：仅临时 / 课程文件；
- 第三方包白名单；
- CPU；
- 内存；
- 单次运行时长；
- 环境销毁策略；
- 日志保留周期。

教师不能直接设置宿主机路径、特权容器或公共互联网任意访问。

## 18.7 平台集成

配置：

- 泛雅课程绑定；
- 成员同步；
- 内容同步；
- 回调或任务状态；
- 最近同步；
- 冲突处理。

外部服务不可用时必须保留本地课程可用性和错误记录。

---

# 19. 平台实验室

# 19.1 实验大厅 `/app/lab/hall`

## 页面目标

让用户在不依附课程任务的情况下进行自主实验。

## 分类

第一阶段只展示真实实现的计算机能力：

```text
算法理解
代码诊断
数据结构操作
复杂度分析
编程练习
```

## 实验卡

展示：

- 名称；
- 类型；
- 难度；
- 预计时间；
- 支持语言；
- 是否包含可视化；
- 安全预设；
- 主操作：开始实验。

点击卡片进入详情抽屉，展示目标、先修知识和完成条件。

# 19.2 课程任务 `/app/lab/course-tasks`

聚合所有课程实验任务。

筛选：

- 课程；
- 截止时间；
- 状态；
- 实验类型。

条目必须显示课程来源，避免与自主实验混淆。

# 19.3 我的实验 `/app/lab/my-experiments`

存储：

- 自主实验；
- 课程任务副本；
- 草稿代码；
- 测试用例；
- 最近运行；
- Agent 提示记录；
- 可视化状态；
- 提交版本。

操作：

- 继续；
- 创建副本；
- 重命名；
- 移动文件夹；
- 关联课程；
- 删除。

课程任务原件不可直接删除，只能隐藏或创建副本。

# 19.4 学习记录 `/app/lab/records`

记录过程证据：

- 实验名；
- 关联知识点；
- 首次测试结果；
- 修改次数；
- 提示层级；
- 最终结果；
- 重复错误；
- 完成时间；
- 相关课程。

学生可以查看自己的完整过程。

教师仅能查看课程任务范围内必要记录。

# 19.5 实验工作区

## 顶部上下文

必须显示：

- 自主实验或课程任务；
- 来源课程；
- 实验目标；
- 完成条件；
- 沙箱权限；
- 保存状态；
- 返回位置。

## 运行结果分类

```text
编译错误
运行时错误
测试失败
超时
内存超限
安全策略阻断
通过
部分通过
```

## Coding Agent 提示层级

建议默认：

```text
L1：指出问题区域或现象
L2：解释错误机制并给出方向
L3：提供伪代码或关键修改建议
L4：教师允许时展示完整参考实现
```

每次升级提示前需学生主动请求或满足教师规则。

---

# 20. 资源库

# 20.1 我的文件 `/app/resources/files`

## 页面目标

管理用户上传到平台的资料，不等同于某门课程的资料页面。

## 支持类型

- PPT；
- PDF；
- Word；
- 图片；
- 音频；
- 视频；
- 代码文件；
- 数据集；
- 教案。

## 列表字段

- 文件名；
- 类型；
- 所有者；
- 解析状态；
- 大小；
- 原文引用数量；
- 被课程引用数量；
- 更新时间。

## 操作

- 上传；
- 新建文件夹；
- 预览；
- 查看解析；
- 引用到课程；
- 替换版本；
- 移动；
- 删除。

## 删除规则

删除前必须显示：

- 被哪些课程引用；
- 哪些原文引用会失效；
- 是否可以仅移入回收站；
- 是否由其他用户共同使用。

# 20.2 课程引用

按课程查看资源引用关系。

必须区分：

- 原文件；
- 课程引用；
- 课程内派生内容；
- 来源已更新；
- 引用失效。

课程引用资源时创建引用关系，不复制多个失控版本。

# 20.3 最近使用

按用户最近打开、编辑、引用排序。

# 20.4 回收站

显示删除时间、影响范围和恢复期限。

永久删除必须二次确认。

---

# 21. 任务中心

# 21.1 我的待办 `/app/tasks/todo`

包含用户需要亲自完成的任务。

学生：

- 继续课程；
- 测验；
- 实验；
- 补学；
- 申请补充材料。

教师：

- 审核加入申请；
- 确认知识候选；
- 补充原文引用；
- 处理失败媒体；
- 审核实验提交；
- 发布课程。

列表字段：

- 任务标题；
- 来源课程或系统；
- 类型；
- 优先级；
- 截止时间；
- 状态；
- 主操作。

# 21.2 我发起的 `/app/tasks/created`

展示用户创建或触发的任务，例如媒体生成、课程发布、成员同步。

# 21.3 系统任务 `/app/tasks/system`

任务类型：

- 文档解析；
- TTS；
- 数字人；
- AI PPT；
- 索引构建；
- 图谱候选；
- 泛雅同步；
- 沙箱环境创建。

字段：

- 任务名；
- 来源；
- 进度；
- 创建时间；
- 最近更新时间；
- 状态；
- 重试次数；
- 日志入口。

失败任务必须提供：

- 明确原因；
- 影响范围；
- 重试；
- 降级方案；
- 返回相关页面；
- 日志或审计入口。

# 21.4 已完成

保留可追溯历史，不与当前待办混合。

---

# 22. 全局状态规范

每个重要页面必须设计以下状态。

## 22.1 Loading

- 首次加载使用骨架或明确加载状态；
- 不用无限旋转图标遮住整个 App Shell；
- 长任务显示阶段和进度；
- 用户可以离开页面，任务继续运行时要说明。

## 22.2 Empty

区分：

- 首次空状态；
- 筛选结果为空；
- 权限导致不可见；
- 外部同步未完成；
- 内容尚未建设。

每种空状态提供一个明确下一步。

## 22.3 Error

必须包含：

- 发生了什么；
- 可能影响什么；
- 用户能做什么；
- 是否已保留数据。

## 22.4 Partial Success

例如 10 个媒体任务中 8 个成功。

页面必须显示成功和失败明细，禁止整体标记为“完成”。

## 22.5 Permission Denied

解释：

- 当前身份；
- 所需权限；
- 返回入口；
- 可联系的人，可选。

## 22.6 Unsaved Changes

- 页面显示未保存；
- 离开时确认；
- 自动保存失败时明确提示；
- 不允许假装保存成功。

## 22.7 Conflict

适用：

- 资料版本更新；
- 多人编辑；
- 泛雅同步；
- 图谱版本；
- 课程发布。

必须支持查看差异和选择处理方式。

## 22.8 Experimental / Research Preview

- 明确标记；
- 不写入正式课程或学习状态，除非用户确认；
- 失败时不破坏主流程；
- 提供反馈入口。

## 22.9 Degraded Demo Mode

比赛演示中外部服务不可用时：

- 明确显示“演示降级模式”；
- 使用预生成素材；
- 不伪装成实时生成；
- 保留真实错误记录。

---

# 23. 页面连续性与状态保存

## 23.1 必须保存的页面状态

- 列表筛选和排序；
- 页面滚动位置；
- Local Rail 展开状态；
- 当前选中对象；
- 学习时间点；
- 实验草稿；
- 未完成笔记；
- 课程建设编辑草稿；
- 右侧面板是否由用户主动打开。

## 23.2 URL 应表达的状态

应进入 URL：

- 当前一级与二级页面；
- 当前 Local Rail 子页面；
- 课程 ID；
- 可分享的知识节点或资源详情；
- 当前实验 ID。

不必进入 URL：

- 临时 Toast；
- 普通确认弹窗；
- 小型筛选面板展开状态；
- 未提交输入内容。

## 23.3 跨空间返回

- 从课程知识节点进入原文，返回时回到该节点；
- 从课程进入实验，返回时回到触发位置；
- 从任务中心进入失败媒体，修复后返回任务中心并更新状态；
- 从资源库进入课程引用，返回时保留资源筛选。

---

# 24. 响应式与设备范围

## 24.1 优先级

第一阶段以桌面端学习、教师建设和比赛演示为主。

推荐优先支持：

- 1440px 设计基准；
- 1280px 正常可用；
- 1024px 基础可用。

## 24.2 窄屏行为

- L1 导航收进菜单，但当前空间仍可见；
- L2 可横向滚动或使用紧凑下拉，但不得出现三层顶部菜单；
- Local Rail 默认收缩为图标或抽屉；
- 右侧固定面板改为全高抽屉；
- 学习页提问时优先保留课程上下文，必要时上下分区；
- 代码编辑器不强行与完整课程画面并排。

## 24.3 移动端限制

移动端第一阶段可以只支持：

- 课程浏览与学习；
- 提问；
- 原文引用；
- 笔记；
- 任务查看。

复杂课程建设、图谱编辑、代码调试可提示“建议使用桌面端”，但不能直接崩溃或显示空白。

---

# 25. 无障碍与可操作性

必须满足：

- 所有操作可用键盘访问；
- 焦点顺序与视觉顺序一致；
- 抽屉打开后焦点进入抽屉，关闭后回到触发点；
- 颜色不单独承担状态；
- 动画支持减少动态效果；
- 视频和数字人内容支持字幕；
- 图谱关键关系有文本替代列表；
- 可视化过程有步骤说明；
- 图标按钮必须有可访问名称；
- 错误信息与对应字段绑定。

---

# 26. 前端实现拆分建议

## 26.1 全局骨架组件

```text
AppShell
PrimaryNavigation
SecondaryNavigation
CourseContextHeader
PageContextHeader
LocalRail
RightDetailDrawer
SystemResponsePanel
GlobalSearch
NotificationCenter
```

## 26.2 浏览组件

```text
FilterToolbar
CourseCard
ResourceTable
TaskList
MemberTable
StatusBadge
CapabilityMaturityTag
EmptyState
ErrorState
```

## 26.3 学习组件

```text
AdaptiveLearningCanvas
LearningTrack
LearningActionDock
LectureStage
CourseAgentPanel
PracticeStage
VisualizationStage
NoteStage
CitationStage
VerifyStage
ReturnContextBar
```

## 26.4 教师建设组件

```text
BuildRail
SourceMaterialList
CourseStructureTree
TeachingScriptEditor
PageMappingWorkspace
MediaTaskTable
QualityGatePanel
PublishHistory
CandidateReviewPanel
```

## 26.5 知识组件

```text
KnowledgeGraphCanvas
KnowledgeNodeDetail
RelationFilter
CitationList
CandidateDiffViewer
GraphVersionHistory
```

## 26.6 实验组件

```text
ExperimentWorkspace
CodeEditor
TestResultPanel
ExecutionTrace
HintPanel
SandboxPolicySummary
ExperimentRecord
```

---

# 27. 核心页面验收标准

## 27.1 导航

- 顶部是否最多两层；
- 第三级是否进入 Local Rail；
- 筛选器、模式和动作是否没有冒充导航；
- 是否不存在第四级菜单；
- 浏览器返回是否符合用户预期。

## 27.2 学习页

- 提问是否保留原课程内容；
- 提问时学习轨道是否自动收缩；
- 试一试、可视化、笔记、原文是否让原课程左出、新舞台右入；
- 所有分支是否显示进入原因、完成条件和返回位置；
- 讲解是否支持全屏；
- Agent 是否默认保持安静；
- 原文缺失时是否诚实说明。

## 27.3 教师建设

- AI 内容是否先作为候选；
- 是否存在保存状态；
- 失败任务是否显示真实原因；
- 发布前是否经过质量门禁；
- 版本是否可追溯和回滚；
- 资料更新是否提示下游影响。

## 27.4 知识空间

- 默认是否为局部图谱而非节点海洋；
- 节点是否能查看定义、关系、原文和课程位置；
- 候选和正式关系是否清楚区分；
- 图谱编辑是否有影响范围；
- 学生是否看不到教师治理入口。

## 27.5 实验与安全

- 运行前是否能看到沙箱权限；
- 编译、运行、测试、超时和安全阻断是否区分；
- CTF 是否使用隔离靶场而非公共互联网；
- 教师是否只能在平台硬边界内配置；
- 提示是否分层；
- 课程实验是否能返回课程原位置。

## 27.6 可信性

- 原文引用是否可核查；
- AI 候选是否有依据；
- 研究预览是否明确标记；
- 降级演示是否不伪装实时；
- 不支持的能力是否没有空壳入口。

---

# 28. 第一阶段必须完成的页面范围

为避免前端铺得过大，第一阶段建议锁定：

## P0：App Shell 与基础空间

- 公开首页基本叙事；
- 登录后首页；
- 我的课程：我学习的、我建设的、课程大厅、加入课程；
- 课程概览；
- 两层顶部导航；
- Local Rail；
- 全局状态组件。

## P0：课程核心链路

- 教师建设：资料、课程结构、教学讲授、页面映射、课程校验、发布；
- 学生学习：正常讲解、提问、试一试、看可视化、做笔记、原文引用、验证完成；
- 返回上下文；
- 原文引用组件。

## P1：知识与实验

- 局部知识图谱浏览；
- 原文引用治理；
- AI 候选审核；
- 课程实验任务；
- 实验工作区；
- 基础 Coding Agent 与沙箱状态。

## P1：平台支撑

- 资源库；
- 任务中心；
- 成员与加入申请；
- 基础安全与沙箱设置。

## P2：后续增强

- 多图 RAG 交互演示；
- 认知模型多维状态；
- 图谱版本和冲突治理；
- 泛雅深度同步；
- CTF 隔离靶场；
- 高级执行轨迹和算法可视化。

---

# 29. 最终页面设计原则

1. **顶部决定空间，左侧决定工作位置，中央完成主任务，右侧承接临时回应。**
2. **页面顶端菜单最多两层，三级进入可收缩 Local Rail，禁止第四级菜单。**
3. **提问贴着课程展开，实践、可视化、笔记和原文切换整个学习舞台。**
4. **不是所有状态都保留原课程画面，但所有状态都必须保留课程上下文。**
5. **AI 默认保持安静，出现时必须说明观察、依据和下一步。**
6. **AI 输出先是候选，教师决定什么进入正式课程。**
7. **课程实验和自主实验共用工具，但课程实验必须写回课程学习证据并能返回课程。**
8. **知识图谱默认展示局部关系，不把节点数量当作价值。**
9. **原文引用、任务失败、模型成熟度和安全边界必须真实可核查。**
10. **页面设计首先服务真实学习和建设，其次才服务比赛展示。**
